<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>Young&#39;s Blog</title>
  
  
  <link href="https://young-eng.github.io/YoungBlogs/atom.xml" rel="self"/>
  
  <link href="https://young-eng.github.io/YoungBlogs/"/>
  <updated>2024-12-20T14:58:14.693Z</updated>
  <id>https://young-eng.github.io/YoungBlogs/</id>
  
  <author>
    <name>Young</name>
    
  </author>
  
  <generator uri="https://hexo.io/">Hexo</generator>
  
  <entry>
    <title>StrongSORT</title>
    <link href="https://young-eng.github.io/YoungBlogs/2024/12/17/StrongSORT/"/>
    <id>https://young-eng.github.io/YoungBlogs/2024/12/17/StrongSORT/</id>
    <published>2024-12-17T13:31:22.000Z</published>
    <updated>2024-12-20T14:58:14.693Z</updated>
    
    <content type="html"><![CDATA[<h3 id="strongsort-make-deepsort-great-again1">StrongSORT: Make DeepSORTGreat Again<sup>[1]</sup></h3><blockquote><p>作者是来自北邮、上海AI Lab和英国Brunel University London的Yunhao Du,Zhicheng Zhao等人，论文引用[1]:</p></blockquote><h3 id="time">Time</h3><ul><li>2023.Feb</li></ul><h2 id="key-words">Key Words</h2><ul><li>missing association and missing detection</li><li></li></ul><h3 id="总结">总结</h3><ol type="1"><li>最近，MOT吸引了很多的注意力，有了很多的进展。然而，现有的方法倾向于用多个基本的模型(检测器和嵌入模型)和不同的训练或者推理tricks。因此，构建一个好的baseline用于公平的对比是重要的。本文中首先回顾经典的trackerDeepSORT，然后从不同的角度进行改进，例如目标检测，特征嵌入和轨迹关联。提出的tracker称之为StrongSORT，为MOT社区贡献了一个强的和fair的baseline。更多的，两个轻量的和即插即用的算法被提出来了，用于解决MOT缺失的问题：缺失的关联和缺失的检测。特别地，不同于大多数的方法，用很高的计算复杂度将短的tracklet关联成完整的轨迹。作者提出了一个appearance-freelinkmodel(AFLink)，不需要外观信息进行全局关联。实现了速度和精度的很好的平衡。另外，提出了高斯平滑插值，基于高斯过程回归来缓解丢掉的检测。AFLink和GSI可以很容易地插入到多个tracker中，有可以忽略的额外的计算成本。最后，通过将StrongSORT和AFLink和GSI融合，得到最终的tracker(StrongSORT++).</li></ol><span id="more"></span><ol start="2" type="1"><li><p>MOT旨在逐帧地检测和跟踪目标的所有类别。近年来，MOT任务被tracking-by-detection(TBD)范式所主导，执行逐帧地检测，将MOT问题表述为一个数据关联的任务。TBD方法倾向于首先提取外观和运动嵌入，然后执行bipartitegraph匹配，受益于高性能的目标检测器，TBD方法因为它们卓越地性能，得到了青睐。作为目标检测和ReID的下游任务，最近的工作倾向于用多种检测器和ReID模型来增加MOT的性能，使得构建一个它们之间的公平的对比变得困难。另一个问题是阻止了公平的对比是用额外的数据集用于训练。另外，一些训练和推理的tricks也用来提高跟踪性能。为了解决上述问题，这篇文章提出了一个简单但有效的MOTbaseline StrongSORT。重新回顾经典的TBD trackerDeepSORT。选择deepsort是因为它的简单有效。首先给DeepSORT用了更强的检测器和嵌入模型，然后，从近期的工作中搜集了一些推理的tricks，进一步提高它的性能。简单地装备DeepSORT，就得到了StrongSORT，它在很多数据集上实现了SOTA。StrongSORT的动机如下：</p><ul><li>可以作为一个baseline用于不同跟踪方法之间公平的对比，特别是tracking-by-detectiontrackers。</li><li>相比于weak baseline，一个更强的baseline展示了方法的有效性。</li><li>精巧的推理tricks可以用在其它的tracker上，不需要重新训练模型。这里有两个缺失的问题：missing association和missing detection。missingassociation意味着同一个物体分散在超过1个的tracklet中。这个问题是很普遍，因为它们在关联阶段缺乏全局信息。missingdetection，也是falsenegatives，指将目标识别为背景。通常是由于低分辨率和遮挡造成的。</li></ul><p>首先，对于缺失的关联问题，提出了一些方法，用一个全局的linkmodel，将短的tracklets关联成轨迹。它们通常首先产生精确但是不完整的tracklets，然后以离线的方式将它们用全局信息进行关联。虽然这些方法提高了跟踪性能，它们依赖于computation-intensivemodels，特别是外观嵌入。相比之下，提出了一个appearance-free linkmodel(AFLink)，只利用时空信息来预测两个输入的tracklets是否属于同一个ID。没有外观模型，AFLink实现了更好的速度和精度之间的平衡。</p><p>其次，线性插值广泛用于missingdetections的补偿。然而，它忽视了插值期间的运动信息，限制了插入位置的精度。为了解决这个问题，提出了高斯平滑插值，用高斯过程回归算法解决了插值bboxes。GSI是一种检测噪声滤波器，能够产生更多精确和稳定的定位。</p><p>AFLink和GSI都是轻量化，即插即用，model-independent和appearance-free的模型。贡献如下：</p><ul><li>提出了一个StrongSORT，给DeepSORT用了更强的modules(检测器和嵌入模型)和一些推理tricks，它可以作为一个强的公平的基线用于其它MOT方法</li><li>提出了两个新的轻量化的算法AFLink和GSI，可以插入到多个trackers，提高性能</li><li>大量的实验表明了提出的方法的有效性。另外，提出的StrongSORT和StrongSORT++在多个benchmarks上实现了SOTA。</li></ul></li><li><p><strong>分开和联合的trackers</strong>：MOT方法能够分为分开的和联合的trackers。分开的trackers跟着tracking-by-detection范式，首先对目标进行定位，然后基于外观的信息、运动信息进行关联。受益于目标检测的快速发展，分开的trackers广泛地应用在MOT任务中，最近，一些联合跟踪的方法提出来，将detection和其它组件例如motion,embedding，association一起训练。这些trackers的主要的优势是计算成本低和类似的性能。同时，一些最近的研究抛弃了外观信息，只依赖于高性能的检测器和运动信息，实现了高速和SOTA的性能。然而，抛弃外观特征在更多复杂场景可能导致差的鲁棒性。在本文中，采用类似DeepSORT的范式，从多个角度配备先进的技术，来确认这个经典框架的有效性。</p></li><li><p><strong>Global Link inMOT</strong>：缺失的关联是MOT中的一个重要问题，为了挖掘丰富的全局信息，一些方法用一个globallink 模型来refine tracking结果。它们首先用时空或者外观信息，产生精确但是不完整的tracklets，然后以离线的方式，通过利用全局信息将这些tracklets连接起来，TNT是用多尺度的TrackNet设计的，来测量两个tracklets之间的连接。用一个多尺度的卷积核，在一个统一的框架中编码运动和外观信息。TPM用了tracklet-plane匹配过程，将混淆的tracklets送到不同的tracklet-plane，帮助降低了tracklet匹配阶段的混淆。ReMOT将不完美的轨迹分成tracklets，然后将它们和外观特征融合。GIAOTracker提出了一个复杂的globallink算法，用提高的ResNet50-TP模型，编码tracklet外观特征，通过空间和时序cost关联tracklets。尽管这些方法产生了显著的提高，它们依赖于外观特征，带来了高的计算成本。相比之下，提出的AFLink模型只利用运动信息来预测两个tracklets之间的linkconfidence。通过设计一个合适的框架和训练过程，AFlink以几乎可以忽略的额外成本，提升了多种最先进的跟踪器的性能。AFLink和LGMTracker有相似的动机，通过<strong>运动信息将tracklets进行关联</strong>。LGMTracker采用了一种有趣但复杂的重建到嵌入策略，通过基于 GCN 和 TGC模块的轨迹关联，旨在解决潜在空间不相似性的问题。然而，AFLink表明，通过精心设计框架和训练策略，一个更简单、更轻量的模块仍然可以表现良好。</p></li><li><p>MOT的插值：线性插值被广泛用于填补缺失检测中恢复轨迹的空白。尽管他的简单和高效，线性插值忽略了运动信息，限制了恢复的bboxes的精度。为了解决这个问题，提出了一些策略来利用时空信息。V-IOUTracker通过在缺失检测发生时回退到单目标跟踪来扩展 IOUTracker。MAT通过采用周期性伪观测轨迹填充策略，非线性地平滑了线性插值的轨迹。为了预测缺失的位置，额外需要一个相机运动补偿（CMC）模型和一个卡尔曼滤波器。MAATrack 通过仅应用 CMC模型简化了这一过程。<strong>所有这些方法都引入了额外的模型，即单目标跟踪器、CMC和卡尔曼滤波器，以换取性能的提升</strong>。相反，我们提出了基于高斯过程回归（GPR）算法对非线性运动进行建模的方法。在没有额外耗时组件的情况下，我们提出的GSI 算法在准确性和效率之间达到了良好的平衡。和GSI相似的工作，利用GPR蒜贩平滑没有插值的tracklets for精确的速度预测。然而，在监控视频中的时间检测任务中表现很好。相比之下，研究MOT任务，采用GPR来修正插值的定位。另外，提出了一个自适应的光滑的factor，而不是提前设定一个超参数。</p></li><li><p>StrongSORT：在这个部分，提出了一些路线，更新DeepSORT，得到StrongSORT。</p><ul><li>Review of DeepSORT:DeepSORT包含两个分支，一个是外观分支，一个是运动分支。在外观分支，给定每帧中的detections。深度外观描述器，在一个reid数据集上进行预训练，用于提取它们的外观特征。它利用一个featurebank机制，来存储每个tracklet的最近的100帧。当新的detections来的时候，第i个tracklet的featurebank <span class="math inline">\(B_i\)</span> 和第 j个dtection的特征<span class="math inline">\(f_j\)</span>之间的余弦距离计算如下：</li></ul><p><span class="math display">\[d(i,j)=min\{1-f_j^Tf_k^{(i)}\midf_k^{(i)}\in B_i\}.\]</span> 这个距离用于关联阶段的匹配损失</p><p>在运动分支，卡尔曼滤波算法用于预测当前帧中的tracklets的位置。它有两个阶段：状态预测和状态更新。给定tracklets的运动装填和新的detections，<strong>Mahalanobis距离</strong>用于测量它们之间的时空的差异。DeepSORT将这个运动距离作为一个gate，来过滤掉不可能的关联。</p><p>之后，匹配级联算法用于解决关联的任务，提出将关联任务作为一系列子问题而不是全局的分配问题。核心的idea是给更频繁出现的目标更大的匹配优先度，每个关联子问题用匈牙利算法解决。</p><ul><li><p><strong>StrongSORT</strong>：基于deepsort的提高包括先进的模块和一些推理tricks。</p><ul><li>先进的模块：DeepSORT用了优化的FasterRCNN作为检测器，训练一个简单的CNN作为嵌入模型。这里，用YOLOX代替它。另外，一个更强的外观特征提取器BoT，替换原始的CNN，能够提取更有区分度的特征。</li><li>EMA：尽管DeepSORT中的featurebank机制能够保留长期的信息，它对于detectionnoise是敏感的。为了解决这个问题，用feature updating测量代替featurebank机制，在帧t中的第i个tracklet，以指数移动平均(EMA)更新外观状态 <spanclass="math inline">\(e^t_i\)</span>：</li></ul><p><span class="math display">\[e_i^t=\alphae_i^{t-1}+(1-\alpha)f_i^t,\]</span></p><p><span class="math inline">\(f^t_i\)</span>是当前匹配的detection的外观嵌入，<spanclass="math inline">\(\alpha\)</span>是动量term。EMA更新测量利用帧间特征变化的信息，降低detection noise。</p><ul><li>ECC：相机运动存在于多个benchmarks中，采用增强的相关系数最大化(ECC)模型用于相机运动补偿。它是一个工具，用于参数化图像对齐，能够估计相邻帧的全局的旋转和变换。特别地，基于下列标准，它基于以下标准来量化变形变换的性能：</li></ul><p><spanclass="math display">\[E_{ECC}(\mathbf{p})=\left\|\frac{\overline{\mathbf{i_r}}}{\left\|\bar{\mathbf{i_r}}\right\|}-\frac{\overline{\mathbf{i_w}}(\mathbf{p})}{\left\|\bar{\mathbf{i_w}}(\mathbf{p})\right\|}\right\|^2,\]</span></p><p><span class="math inline">\(||.||\)</span> 表示欧式norm，<spanclass="math inline">\(p\)</span> 是 warping参数。<spanclass="math inline">\(\overline{\mathbf{i_r}}\)</span> 和 <spanclass="math inline">\(\overline{\mathbf{i_r}}\)</span> 是参考图像 <spanclass="math inline">\(i_r\)</span> 和warped image <spanclass="math inline">\(\mathbf{i_r}\)</span> 的零均值版本。图像对齐问题通过最小化 EECC(p)来解决，使用提出的前向加法迭代算法或逆向组合迭代算法。由于其高效性和有效性，ECC广泛用于补偿多目标跟踪（MOT）任务中由相机运动引起的运动噪声。</p><ul><li>NSAKalman：普通的卡尔曼滤波器对于低质量的detections是脆弱的，忽略了检测噪声尺度上的信息。为了解决这个问题，从GIAOTracker中借鉴NSAKalman，提出了一个公式，自适应地计算噪声协方差：</li></ul><p><span class="math display">\[\widetilde{R}_k=(1-c_k)R_k,\]</span></p><p><span class="math inline">\(R_k\)</span>是预设的常熟测量噪声协方差，<span class="math inline">\(c_k\)</span>是状态k的检测置信度分数。直觉上来说，当噪声比较小的时候，detection的分数比较高，导致低<span class="math inline">\(\widetilde{R}_k\)</span>。更低的 $<spanclass="math inline">\(\widetilde{R}_k\)</span>的意味着检测在装填更新阶段有更高的权重，反之亦然。这能够帮助提高更新状态的精度。</p><ul><li>motion cost DeepSORT在第一次关联阶段，只采用外观特征距离作为matchingcost，运动距离只作为gate。相反，通过外观和运动信息解决分配问题。costmatrix C 是 外观cost <span class="math inline">\(A_a\)</span>的 和motioncost <span class="math inline">\(A_m\)</span> 的加权求和。</li></ul><p><span class="math display">\[\begin{array}  {c}C=\lambda A_a+(1-\lambda)A_m,  \end{array}\]</span></p><ul><li>Vanillamatching。一个有趣的发现是尽管在DeepSORT中，匹配级联算法重要，它限制了tracker的性能。理由是当tracker变强的时候，它对于confusing的关联更加鲁棒。因此额外的先验限制了匹配精度。通过用普通的全局线性分配替换匹配级联解决这个问题。</li></ul></li></ul></li><li><p>两个即插即用的模块：AFLink和GSI，来进一步解决缺失的关联和缺失的检测的问题。</p><ul><li>AFLink：tracklets的全局的link用来追求高的精度的关联。然而，它们通常依赖于高计算的模块和大量的超参数进行微调。例如GIAOTrack利用提高的ResNet50-TP来提取tracklet 3D特征，执行和额外的空间时序距离进行关联。它有六个超参数需要设置。3个阈值和3个权重参数，导致大量的调优实验和较差的鲁棒性。另外，<strong>过度依赖外观特征导致对遮挡比较脆弱</strong>，受此启发，设计了一个appearance-free的模型，AFLink，通过只依赖时空信息，预测两个tracklets之间的连接。</li></ul><p>图中展示了AFLink的模型的两个分支，采用两个tracklets <spanclass="math inline">\(T_i\)</span> 和 <spanclass="math inline">\(T_j\)</span> 作为输入，<spanclass="math inline">\(T_{*}\)</span> 包含了最近30帧的frame id <spanclass="math inline">\(f^{*}_k\)</span> 和 位置 <spanclass="math inline">\((x^{*}_k, y^{*}_k)\)</span>，用zero-padding用于少于30帧的tracklets。一个时序模块通过沿着时间维度的 <spanclass="math inline">\(7 \times 1\)</span> kernels进行卷积，来提取特征。包含四个Conv-BN-ReLU层。然后，融合模块，是一个有BN和ReLU 的 单个 <span class="math inline">\(1 \times 3\)</span>的卷积层，用于从不同的特征维度继承信息，称之为f, x和y。这两个特征图进行池化和squeeze，得到featurevector，然后进行concatenate，包含了丰富的时序信息。最后，一个MLP用于预测置信度分数用于关联。注意到，两个分支的权重在时序和融合模块是不共享的。</p><p>在训练期间，关联阶段表述为一个二分类任务。然后，用二值交叉熵损失进行优化。</p><p>在关联期间，用时序限制，过滤掉不合理的trackletpairs。然后，将全局链路求解为一个具有预测连通性分数的线性分配任务。</p><ul><li>GSI：插值普遍用于填充由于缺失的detections造成的轨迹的gaps。线性插值由于它的简洁导致了很流行。然而，它的精度受限，因为它没有用运动信息。虽然提出了一些策略用于解决这个问题，它们引入了额外的耗时的模块，例如单目标跟踪，卡尔曼滤波和ECC。相比之下，提出了一个轻量化的插值算法，用了高斯过程回归来建模非线性运动。</li></ul><p>将GSI 模型表述为第i个轨迹：</p><p><span class="math inline">\(p_t=f^{(i)}(t)+\epsilon,\)</span></p><p><span class="math inline">\(t \in F\)</span> 是frame id，<spanclass="math inline">\(p_t \in P\)</span> 是 帧t的位置坐标变量。 <spanclass="math inline">\(\epsilon\)</span> 是高斯噪声。给定跟踪的和线性插值的，长度为L的轨迹 <spanclass="math inline">\(S^{i}\)</span>，非线性运动建模的任务通过拟合函数<span class="math inline">\(f^{(i)}\)</span>解决。假定它遵循一个高斯过程：</p><p><span class="math display">\[f^{(i)}\inGP(0,k(\cdot,\cdot)),\]</span></p><p><span class="math inline">\(k(x,x^&#39;)\)</span> 是一个radial basis函数核。基于高斯过程的性质，给定一个新的frame set <spanclass="math inline">\(F^*\)</span>，它的光滑的位置 <spanclass="math inline">\(P^*\)</span> 通过这个公式进行预测：</p><p><spanclass="math display">\[P^*=K(F^*,F)(K(F,F)+\sigma^2I)^{-1}P,\]</span></p><p><span class="math inline">\(K(.,.)\)</span> 是一个协方差函数，基于k(.,.)。</p><p>另外，超参数 <span class="math inline">\(\lambda\)</span>控制轨迹的光滑度，和它的长度相关。简单地将其设计为一个长度为l的函数：</p><p><span class="math display">\[\lambda=\tau*log(\tau^3/l),\]</span></p><p>图4展示了GSI和线性插值的不同。raw tracked results 通常包含noisyjitter，LI忽略了运动信息。GSI通过一个自适应的光滑factor，光滑了整个轨迹，解决了这个问题。</p></li></ol><p><imgsrc="https://figures.semanticscholar.org/faaf6f4b4f831c50453c72858bc4656269039928/4-Figure2-1.png"alt="Framework" /> <span class="math inline">\(Fig.1^{[1]}\)</span>DeepSORT和StrongSORT之间的对比</p><p><imgsrc="https://figures.semanticscholar.org/faaf6f4b4f831c50453c72858bc4656269039928/5-Figure3-1.png"alt="Framework of the two-branch AFLink model" /> <spanclass="math inline">\(Fig.2^{[1]}\)</span> 采用两个tracklets, <spanclass="math inline">\(T_i\)</span> 和 <spanclass="math inline">\(T_j\)</span> 作为输入。 <spanclass="math inline">\(T_*\)</span> 包含近30帧的位置和frameid。然后，时序的模块沿着时间维度提取特征，融合模块沿着特征维度继承信息。这两个trackletsfeatures被池化，squeezed，concatenated，然后输入给分类器预测关联分数。</p>]]></content>
    
    
    <summary type="html">&lt;h3 id=&quot;strongsort-make-deepsort-great-again1&quot;&gt;StrongSORT: Make DeepSORT
Great Again&lt;sup&gt;[1]&lt;/sup&gt;&lt;/h3&gt;
&lt;blockquote&gt;
&lt;p&gt;作者是来自北邮、上海AI Lab和英国Brunel University London的Yunhao Du,
Zhicheng Zhao等人，论文引用[1]:&lt;/p&gt;
&lt;/blockquote&gt;
&lt;h3 id=&quot;time&quot;&gt;Time&lt;/h3&gt;
&lt;ul&gt;
&lt;li&gt;2023.Feb&lt;/li&gt;
&lt;/ul&gt;
&lt;h2 id=&quot;key-words&quot;&gt;Key Words&lt;/h2&gt;
&lt;ul&gt;
&lt;li&gt;missing association and missing detection&lt;/li&gt;
&lt;li&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id=&quot;总结&quot;&gt;总结&lt;/h3&gt;
&lt;ol type=&quot;1&quot;&gt;
&lt;li&gt;最近，MOT吸引了很多的注意力，有了很多的进展。然而，现有的方法倾向于用多个基本的模型(检测器和嵌入模型)和不同的训练或者推理tricks。因此，构建一个好的baseline用于公平的对比是重要的。本文中首先回顾经典的tracker
DeepSORT，然后从不同的角度进行改进，例如目标检测，特征嵌入和轨迹关联。提出的tracker称之为StrongSORT，为MOT社区贡献了一个强的和fair的baseline。更多的，两个轻量的和即插即用的算法被提出来了，用于解决MOT缺失的问题：缺失的关联和缺失的检测。特别地，不同于大多数的方法，用很高的计算复杂度将短的tracklet关联成完整的轨迹。作者提出了一个appearance-free
link
model(AFLink)，不需要外观信息进行全局关联。实现了速度和精度的很好的平衡。另外，提出了高斯平滑插值，基于高斯过程回归来缓解丢掉的检测。AFLink和GSI可以很容易地插入到多个tracker中，有可以忽略的额外的计算成本。最后，通过将StrongSORT和AFLink和GSI融合，得到最终的tracker(StrongSORT++).&lt;/li&gt;
&lt;/ol&gt;</summary>
    
    
    
    <category term="Papers" scheme="https://young-eng.github.io/YoungBlogs/categories/Papers/"/>
    
    
    <category term="object tracking" scheme="https://young-eng.github.io/YoungBlogs/tags/object-tracking/"/>
    
  </entry>
  
  <entry>
    <title>SAMURAI</title>
    <link href="https://young-eng.github.io/YoungBlogs/2024/12/17/SAMURAI/"/>
    <id>https://young-eng.github.io/YoungBlogs/2024/12/17/SAMURAI/</id>
    <published>2024-12-17T06:53:09.000Z</published>
    <updated>2024-12-27T09:13:17.984Z</updated>
    
    <content type="html"><![CDATA[<h3id="samurai-adapting-segment-anything-model-for-zero-shot-visual-tracking-with-motion-aware-memory1">SAMURAI:Adapting Segment Anything Model for Zero-Shot Visual Tracking withMotion-Aware Memory<sup>[1]</sup></h3><blockquote><p>作者是来自华盛顿大学的Cheng-Yen Yang, Hsiang-WeiHuang等人。论文引用[1]:</p></blockquote><h3 id="time">Time</h3><ul><li>2024.Nov</li></ul><h3 id="key-words">Key Words</h3><ul><li>zero-shot tracking</li></ul><h3 id="总结">总结</h3><ol type="1"><li>SAM2在目标分割任务上展示了强大的能力，但是在视觉目标跟踪上面临挑战，特别是当一些快速移动或者遮挡的目标的场景的时候。另外，原始模型中的固定窗口记忆方法没有考虑选择的记忆的质量来约束下一帧的图像特征，导致视频中的误差传播。本文介绍了SAMURAI，一个SAM2的增强的版本，用于视觉目标跟踪。通过引入<strong>时序运动线索</strong>和提出的<strong>运动感知选择机制</strong>，SAMURAI能够有效的预测目标的运动和修正掩膜选择，实现鲁棒的、精确的跟踪，不需要重新训练或者微调。SAMURAI能够实时操作，展示了强大的zero-shto的性能。</li></ol><span id="more"></span><ol start="2" type="1"><li><p>SAM2引入了流记忆架构，使得能够顺序地处理视频帧，同时在长的序列上维持上下文。SAM2展示了强大的视频目标分割能力，产生精确的像素级的掩膜，仍然面临着挑战。VOT主要的关注点是保持一致的目标id和定位，即使遮挡，外观变换或相似目标出现。然而，SAM2当预测序列帧的掩膜的时候，忽略了运动线索导致一些快速移动的场景中的不精确。这个限制在拥挤的场景中很常见。<strong>SAM2倾向于外观相似，而不是空间和时间一致性，导致跟踪误差</strong>。有两个常见的失败的模式：拥挤场景中的混淆和遮挡期间的不高效的记忆利用。</p></li><li><p>为了解决这些限制，提出将<strong>运动信息引入到SAM2的预测过程中，通过利用目标轨迹的历史信息，可以增强模型区分视觉上相似的目标，在遮挡的情况下，维持跟踪的精度</strong>。另外，优化SAM2的记忆管理是重要的，当前的方法不加选择的在memorybank中存储最近的frames，在遮挡期间引入了不相关的特征，降低了跟踪性能。解决这些挑战对于适应SAM2丰富的掩码信息以实现稳健的视频目标跟踪至关重要。</p></li><li><p>为了这个目的，提出了SAMURAI，一个基于SAM的统一的、鲁棒的、zero-shot的、带有运动感知的实例级记忆的zero-shot视觉跟踪器。提出了方法引入了两个关键的提升：(1)<strong>一个运动建模的系统，修正了掩码选择</strong>，使得在复杂场景下更精确的目标位置预测；(2)<strong>一个优化的记忆选择机制，采用混合评分系统，结合了原始掩膜相似度、物体得分和运动得分，以保留更多相关的历史信息</strong>，从而增强模型的整体跟踪可靠性。总结的贡献如下：</p><ul><li>通过运动建模，引入运动信息，来高效地处理快速移动和遮挡的物体，增强SAM2的精确度。</li><li>提出了运动感知的记忆选择机制，通过选择性的存储由运动和相似度分数决定的相关的frames，降低了拥挤场景下的误差</li></ul></li><li><p>视觉目标跟踪(visual object tracking, VOT)旨在跟踪视频序列中的目标，包括一些目标大小、遮挡、复杂背景等变换，来提升跟踪算法的鲁棒性和精确。基于Siamese和基于Transformer的跟踪器通过学习嵌入相似度。然而，由于在singleforwardpass评估策略中，这些跟踪器缺乏自纠正，很容易偏移到干扰物上。为了这个目的，最近的工作进一步引入了memorybank和注意力，来找到当前帧和历史信息之间的映射。</p></li><li><p>SAM启发了大量的后续的研究。SAM引入了一个基于prompt的分割的方式，用户可以输入points/bboxes/text来指导模型分割图像中的任意目标。SAM有很广的应用，像是视频理解。然后，很多基于SAM的工作，例如，SAM2将模型的能力扩展到视频分割，引入了记忆机制，用于在动态视频序列中跟踪目标。另外，也尝试了创造更多的SAM的变体，用于资源受限的环境，旨在降低它的计算需求。医学图像中的研究也采用SAM用于特定的任务。最近，SAM2Long用了基于Tree的记忆来增强用于长视频的目标分割。然而，它们的高fps的视频序列和更深的memorytree架构需要更多的计算资源和存储消耗。另一方面，提出的SAMURAI模型，已经在大规模的分割数据集上训练过了，确保了很好的泛化性。</p></li><li><p>运动建模是跟踪任务中一个重要的组件，能够分类为启发式的可学习的方法。启发式的方法例如广泛使用的卡尔曼滤波，依赖于固定的运动先验和预定义的超参数来预测目标轨迹。KF在很多benchmarks上有效，但是在密集或者abruptmotion中表现不好。其他的方法尝试在应用基于KF的预测之前，应用相机运动补偿。然而，这些标准和noisescaleadaptive的KF有很多的超参数，限制了它们在特定运动类型中的有效性。相反，可学习的运动模型吸引了很多的注意，因为它们是数据驱动的，Trackor是第一个用trajecorybox,就像FasterRCNN中的RoI一样，来提取特征，回归目标的位置。MotionTrack通过学习过去的轨迹表征来预测将来的运动，增强跟踪。MambaTrack进一步探索了不同的基于学习的运动模型架构例如Transformer和state-spacemodel(SSM)。作者的方法也是基于学习的、带有增强启发策略的运动建模。</p></li><li><p>SAM2包含一个图像encoder，一个mask decoder with promptencoder，一个记忆注意力层，一个记忆编码器。引入了SAM2的一些preliminaries。</p><ul><li>prompt encoder：promptencoder设计跟着SAM，有两种类型的prompts，包括稀疏的points和bboxes和密集的masks，通过promptencoder输出的prompt tokens表示为 <span class="math inline">\(x_{prompt}\in N_{tokens} \times d\)</span>。在视觉目标跟踪中，第一帧 <spanclass="math inline">\(t_0\)</span>中的目标的真值的bbox提供了，SAM2对top-left和bottom-right的points进行位置编码作为输入，然而，剩余的序列用从之前帧中预测的mask作为promptencoder的输入。</li><li>Mask Decoder：memory decoder是将memory attentionlayer产生的memory-conditioned image embeddings和prompttokens作为输入。它的多头分支能够产生一系列的预测的masks，along with对应的掩码相似度分数，和目标分数作为输出。SAM2的相似度掩码分数预测是MAEloss进行监督的，能够表示整体的mask的置信度。然而目标预测是用交叉熵loss进行监督的，来决定一个mask是否存在于frame中。在原始的执行中，最后输出的mask。基于所有的输出的mask中最高的相似度分数进行选择。然而，相似度分数不是一个非常鲁棒的indicator，特别是拥挤场景中。引入了一个额外的<strong>motionmodeling</strong>，来保持目标的运动track，提供一个额外的运动分数，来辅助的预测的选择。</li><li>Memory Attention Layer: Memory attention block首先用frameembeddins执行自注意力，然后执行image embeddings和memorybank内容之间的交叉注意力。无条件的imageembedings，和之前输出的masks、之前的输入的prompts和objectpointers置于上下文的理解中。</li><li>Memory Encoder和Memory bank：在maskdecoder产生输出的mask之后，输出的mask通过一个memory encoder来得到memoryembedding，在每个frame处理之后，创造了一个新的memory。这些memoryembeddings添加到memory bank中。在序列中给定的时刻 t，可以得到memory bank<span class="math inline">\(B_t\)</span>。将过去的<spanclass="math inline">\(N_past\)</span> frames的输出 <spanclass="math inline">\(m\)</span> 作为memorybank的组件。直接的固定窗口的memory可能导致错误编码或者低置信度的目标，当在长序列中的时候，可能导致误差传播，提出的<strong>motion-awarememory selection</strong> 将会代替原始的Memory bank，来确保更好的memoryfeatures能够被保留，在图像特征上进行限制。</li></ul></li><li><p>SAM2展示了强大的VOT和VOS能力。然而，<strong>原始的模型可能错误地编码或者低置信度的目标</strong>，导致在长的序列中的大量的误差传播。为了解决上述问题，提出了基于<strong>卡尔曼滤波的运动建模,on top of the multi-masksselection</strong>，和一个基于混合打分的，结合相似度和运动分数的系统的一个增强的记忆选择，这个增强能够增强模型的能力，在复杂视频场景中精确地跟踪目标。重要的是，这个方法不需要微调，也不需要额外的训练，能够直接集成到现有的SAM2中。通过提高预测masks的选择，提供了一个可靠的实时的onlineVOT。</p></li><li><p>运动建模很长时间以来是VOT中解决关联模糊的一个有效的方式。用线性的KF作为baseline，来展示运动建模的引入提高了跟踪精度。在视觉跟踪框架中，集成了KF来增强bboxes位置和维度预测，有助于从M个候选中选择N个，并从中挑选出最可靠的掩码。对于每个mask，对应的bbox<span class="math inline">\(d_i\)</span>是通过计算mask的非零的pixels的坐标的最大最小值得到的。KF-IoU分数通过计算预测的mask和从KF中预测的state推导出来的bbox的IoU来得到。然后选择最大化KF-IoU分数和原始相似度分数的加权求和的mask。</p></li></ol>]]></content>
    
    
    <summary type="html">&lt;h3
id=&quot;samurai-adapting-segment-anything-model-for-zero-shot-visual-tracking-with-motion-aware-memory1&quot;&gt;SAMURAI:
Adapting Segment Anything Model for Zero-Shot Visual Tracking with
Motion-Aware Memory&lt;sup&gt;[1]&lt;/sup&gt;&lt;/h3&gt;
&lt;blockquote&gt;
&lt;p&gt;作者是来自华盛顿大学的Cheng-Yen Yang, Hsiang-Wei
Huang等人。论文引用[1]:&lt;/p&gt;
&lt;/blockquote&gt;
&lt;h3 id=&quot;time&quot;&gt;Time&lt;/h3&gt;
&lt;ul&gt;
&lt;li&gt;2024.Nov&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id=&quot;key-words&quot;&gt;Key Words&lt;/h3&gt;
&lt;ul&gt;
&lt;li&gt;zero-shot tracking&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id=&quot;总结&quot;&gt;总结&lt;/h3&gt;
&lt;ol type=&quot;1&quot;&gt;
&lt;li&gt;SAM2在目标分割任务上展示了强大的能力，但是在视觉目标跟踪上面临挑战，特别是当一些快速移动或者遮挡的目标的场景的时候。另外，原始模型中的固定窗口记忆方法没有考虑选择的记忆的质量来约束下一帧的图像特征，导致视频中的误差传播。本文介绍了SAMURAI，一个SAM2的增强的版本，用于视觉目标跟踪。通过引入&lt;strong&gt;时序运动线索&lt;/strong&gt;和提出的&lt;strong&gt;运动感知选择机制&lt;/strong&gt;，SAMURAI能够有效的预测目标的运动和修正掩膜选择，实现鲁棒的、精确的跟踪，不需要重新训练或者微调。SAMURAI能够实时操作，展示了强大的zero-shto的性能。&lt;/li&gt;
&lt;/ol&gt;</summary>
    
    
    
    <category term="Papers" scheme="https://young-eng.github.io/YoungBlogs/categories/Papers/"/>
    
    
    <category term="object tracking" scheme="https://young-eng.github.io/YoungBlogs/tags/object-tracking/"/>
    
  </entry>
  
  <entry>
    <title>MOTIP</title>
    <link href="https://young-eng.github.io/YoungBlogs/2024/12/17/MOTIP/"/>
    <id>https://young-eng.github.io/YoungBlogs/2024/12/17/MOTIP/</id>
    <published>2024-12-17T06:52:58.000Z</published>
    <updated>2024-12-20T02:27:52.351Z</updated>
    
    <content type="html"><![CDATA[<h3 id="multiple-object-tracking-as-id-prediction1">Multiple ObjectTracking as ID Prediction<sup>[1]</sup></h3><blockquote><p>作者是来自南大、上海AI Lab和中国移动的Ruopeng Gao、Yijun Zhang和LiminWang。论文引用[1]:Gao, Ruopeng et al. “Multiple Object Tracking as IDPrediction.” ArXiv abs/2403.16848 (2024): n. pag.</p></blockquote><h3 id="time">Time</h3><ul><li>2024.Mar</li></ul><h3 id="key-words">Key Words</h3><ul><li>end-to-end in-context ID prediction</li></ul><h3 id="总结">总结</h3><ol type="1"><li>在多目标跟踪中，tracking-by-detection方法经历了很多测试，将这个过程分为检测和关联阶段，利用鲁棒的单帧检测器，通过人工设计的启发式算法和代理任务，将目标关联视为一个后处理步骤。然而，启发式方法的本质是阻止了对端到端训练数据的利用，导致当面临复杂或者新的场景的时候，增加的人为的修改。在本文中，将目标关联任务视为一个端到端的in-contextID预测问题，提出了一个baseline称之为MOTIP。特别地，当考虑将对应的IDs作为in-contextprompts时，将targetembeddings嵌入到历史轨迹信息中，然后直接预测当前帧的目标的IDlabels。MOTIP能够直接从训练数据中学习trackingcapabilities，将其从繁重的人工算法中解放出来。</li></ol><span id="more"></span><ol start="2" type="1"><li><p>MOT旨在定位每一帧中感兴趣目标的位置，然后再整个视频序列中分配它们对应的id。作为一个基础的视觉任务，用在了很多下游任务上。<strong>再MOT的发展历史上，tracking-by-detection范式成为了一个shiningstar，基于这个问题定义，它们自然地将这个过程分成两个子任务：目标检测和关联，用单帧的检测器来检测当前帧中的所有目标，然后用后处理算法将历史轨迹和这些detections进行关联，实现在线tracking</strong>。启发式的方法被用来作为ID分配的最优解。它们主要依赖于运动信息，利用卡尔曼滤波器用于线性运动估计和计算IoUcostmatrix，一些方法也引入了Re-ID特征，用余弦相似度用于相似度计算，尽管这些方法实现了很好的结果，<strong>它们过分第依赖于手工设计和人工的先验假设仍然是一个让人担忧的问题</strong>。例如，卡尔曼滤波器依赖于线性运动假设，它努力在复杂的运动模式中准确地适应目标轨迹。类似地，基于linearlyseparablefeatures的假设，当面对不可区分的外观的时候，余弦相似度也不可靠。因此，在面对复杂或者新的场景的时候，<strong>tracking-by-detection方法总是需要通过人工分析，更新启发式的方法，这个做法可能导致日益膨胀和复杂的代码库</strong>。</p></li><li><p>现在，很多研究将DETR扩展到MOT中，trackedobjects表示为queries，逐帧地进行propagate，用来回归目标的位置。通过端到端的过程，<strong>这些方法能够直接从训练数据中学习必要的tracking策略</strong>，使得它们能够在复杂场景中获得优势，尽管实现了很好的性能，它们仍然在一些方面不是很完美。<strong>例如tracking-by-query范式使得在训练阶段进行逐帧的处理，类似于神经网络运作的方式，这个本质阻止了长期的训练，导致训练和推理之间的差异，最终限制了模型的性能</strong>。另外，同时处理检测和跟踪queries可能导致冲突和相互抑制，导致综合性能下降。</p></li><li><p>这篇文章引入了一个全新的视角，<strong>通过将MOT视为一个端到端的ID预测任务。特别地，给定所有跟踪目标的历史轨迹，模型需要检测当前帧中所有目标，直接预测它们的IDlabels。实际上，作者倾向于利用DETR作为检测器，因为它直接提供了每个目标的embeddings，使得不需要考虑特征提取方法例如RoI，pooling等，就能够构建历史轨迹。为了表示不同trackedtargets的ID，构建了一个可学习的ID词典。基于此，将目标的embeddings和它对应的IDembeddings进行concate，得到历史的轨迹信息。用一个直接的transformerdecoder架构用作ID预测器，将当前帧的物体作为输入，基于历史信息直接预测它们的IDlabels</strong>。</p></li><li><p>一方面，相比于tracking-by-detection方法，作者的方法绕过了代理任务和启发式算法，简化了跟踪管道，并使得能够从特定场景中端到端地利用跟踪能力。另一方面，检测器和ID预测器是高度并行的，消除了tracking-by-query方法中的串行处理。另外，没有在单个module中同时处理检测和关联也避免了冲突。</p></li><li><p><strong>Tracking-By-Detection</strong>：是MOT中用的最广的范式，很多方法采用了启发式算法，将当前帧的检测结果和历史轨迹关联起来，然后逐帧地实现在线MOT，因为传统MOT主要关注于行人跟踪，行人展示出了相对简单地运动模式，一个自然的方式是估计它们的运动用于目标关联。例如SORT和ByteTrack通过利用卡尔曼滤波用于线性运动估计，实现了很好的效果。然而，这个线性运动假设无法捕捉无序的轨迹，OC-SORT进一步更新了这个算法，使得它能够适应突然的终止和而非线性的运动，提高了性能。一些方法尝试引入目标外观相似度用于测量。Deep-SORT和DeepOCSORT引入了额外的Re-ID模块，来得到Re-ID特征，相比之下，像FairMOT，JDE，用同一个框架用于检测和特征提取。另外，BoT-SORT通过估计相机位移，纠正了运动估计的结果；TrackFlow通过深度信息增强了模型。<strong>由于线性运动假设不完美的本质，Hybrid-SORT最近引入了基于目标高度和检测置信度的状态估计，增强了跟踪的能力，特别是在DanceTrack中，尽管修补启发式算法仍然能取得比较好的结果，它还是会将代码库弄的臃肿，然而，手动制作代理任务和依赖于先验假设在特定场景中学不到优化的策略，导致当面对没有考虑过的场景的时候，需要很大的努力调整参数和算法</strong>。</p></li><li><p><strong>Tracking-by-Query</strong>：受最近DETR的启发的一个跟踪算法，将detectqueries扩展到MOT任务，用track queries来表示trackedtarget，通过视频序列进行传播。TransTrack构建了一个Siamese Transformerdecoder用于检测和跟踪。TrackFormer和MOTR利用相同的Transformerdecoder通过同时处理detect和trackqueries来同时处理检测和跟踪。MQT采用了多个queries来表示一个trackedobject，更多地关注未知类别的跟踪。MeMOT构建了一个巨大的记忆库来存储目标历史信息。MeMOTR用了一个长期的记忆注入机制，一个简单有效的方式来提高跟踪性能，然而，<strong>最近的研究突出了新的方法和trackedobject之间的冲突对于tracking-by-query范式仍然是一个严重的问题。CO-MOT和MOTRv3尝试平衡两者insupervision，能够缓解这个矛盾</strong>。</p></li><li><p>这篇文章中，作者将MOT视为一个IDPrediction的问题，称之为MOTIP。<strong>MOT作为一个ID预测问题</strong>。MOT旨在产生通过每个identityk，逐帧产生有顺序的bboxes。这个暗示了，对于每帧的在线处理，它努力将检测到的物体和之前的轨迹相匹配。将这些历史轨迹表示为一个轨迹集合。<spanclass="math inline">\(\mathcal{T} ={\mathcal{T}^1,\mathcal{T}^2,...,\mathcal{T}^K}\)</span>。每个 <spanclass="math inline">\(\mathcal{T}^k =(\tau^k_1,\tau^k_2,...,\tau^k_T)\)</span>表示第k个识别到的目标的完整的轨迹。<spanclass="math inline">\(\tau^k_t\)</span> 表示在时刻t的tracklet context信息。实际中， <span class="math inline">\(\tau^k_t\)</span>的组成依赖于每个不同的方法。例如有的方法总是利用位置、速度和加速度来描述每个tracklet，也有一些ReID的方法引入了目标特征用于外观匹配。之前，给定历史轨迹 <spanclass="math inline">\({\mathcal{T}_{1&quot;t-1}}\)</span>，tracking-by-detection的方法通过代理任务计算costmatrix。然而tracking-by-query的方法通过传播trackqueries，继承ID信息。本文中，引入了新的角度，将MOT视为ID预测问题。特别地，给定当前帧<span class="math inline">\(I_t\)</span> 的检测结果 <spanclass="math inline">\(\mathcal{D}_t\)</span>，基于历史轨迹，直接预测它们的ID，表示如下：<spanclass="math display">\[\mathrm{ID}(O_t)=\theta(\mathcal{T}_{1:t-1},\mathcal{D}_t),\]</span></p><p><span class="math inline">\(\mathcal{T}_{1:t-1}\)</span>表示第t帧之前的轨迹。<span class="math inline">\(\theta\)</span>是一个端到端的可学习的module，有一个简单的分类头。不需要任何人工的代理任务，通过分类任务直接预测ID。</p></li><li><p>MOTIP架构：整个的MOTIP架构很简单，包含3个主要部分：<strong>一个DETR检测器检测目标和提取它们的嵌入；一个可学习的ID词典，将不同的identities表示为C维的embeddings；一个IDdecoder预测新检测的目标的ID</strong>。</p></li><li><p><strong>DETR检测器</strong>：用DETR作为图像检测器；输入原始图像<span class="math inline">\(I_t\)</span>，CNN backbone和Transformerencoder提取和增强图像特征。接下来，decoder从N个可学习的detectqueries中产生输出的embeddings。它们通过bbox和clshead被编码成bboxes和分类的置信度。用一个置信度阈值 <spanclass="math inline">\(\tau_{det}\)</span> 来过滤掉negativedetections，保留 <span class="math inline">\(M_t\)</span> 个activetargets。用DETR进一步简化了方法，使得能够利用解码的embeddings <spanclass="math inline">\(O_{t}=\{o_{t}^{1},o_{t}^{2},\cdots,o_{t}^{M_{t}}\}\)</span>来表示对应的目标，不许哟啊考虑层次化或者RoI技巧，用于特征提取和融合。</p></li><li><p><strong>可学习的ID 词典</strong>：一个直白的方式是用one-hotlabel表示ID，然而，一方面，离散的值不利于神经网络的学习；另一方面，one-hot形式不太实际，当将模型扩展到有大量目标的场景的时候，导致维度很大。因此，创建一个ID词典<span class="math inline">\(\mathcal{I}\)</span>，包含 <spanclass="math inline">\(K+1\)</span>个可学习的words来表示这些identities，如下： <spanclass="math display">\[\mathcal{I}=\{i^1,i^2,\cdots,i^K,i^{spec}\}.\]</span>每个word i 是一个可学习的C维embedding。具体地，前K个words <spanclass="math inline">\({i^1,i^2,...,i^K}\)</span> 是常规的tokens，表示对应的identities。最后一个word <spanclass="math inline">\(i^{spec}\)</span>是一个特殊的token，用于表示当前帧中检测到的、没有任何ID的新的目标。实际中，K的值比单帧中目标的平均数量大得多，依赖于不同的数据集。</p></li><li><p><strong>层次化的轨迹</strong>：MOT方法通常基于它们的要求，用了多种contexts来表示层次化的轨迹，例如位置、运动、Re-ID特征；像很多基于Transformer的方法，直接利用从decodingdetect queries得到的输出embeddings表示tracked targets。实际上，targetembedding e 是通过一个简单的FFN结构从输出embedding o推导出来的。因为embedding e 不包含identity信息，引入对应的IDembedding，来完成tracklet context： <spanclass="math display">\[{\tau}^{k_t} = concat(e^{k}_t,i^k)\]</span> <spanclass="math inline">\({\tau}^{k}_t\)</span> 是时刻t的、identity为k的tracklet object的tracklet context。<spanclass="math inline">\(e^k_t\)</span>是它的目标embedding。因此，每个tracklet context是一个2D维的vector，可以被拼接成历史轨迹 $ =(,_{t-1}<sup>{k},<em>{t}^{k},</em>{t+1}</sup>{k},)$。在实验中，只保留最近T帧的轨迹。</p></li><li><p><strong>IDDecoder</strong>：为了处理不同长度的输入，利用一个带有相对时序位置编码的6层transformerdecoder作为ID 预测器，将轨迹和detections作为输入，将identityfield引入到detection中，形成2C维的token： <spanclass="math display">\[d^m_t = concat(o^m_t,i^{spec})\]</span></p><p><span class="math inline">\(o^m_t\)</span> 是 当前帧中第m个activeDETR 输出embedding，<span class="math inline">\(i^{spec}\)</span>是ID词典中特殊的token，表示ID是未知的，然后，将这些detection tokens<spanclass="math inline">\(\mathcal{D}=\{d_{t}^{1},d_{t}^{2},\cdots,d_{t}^{M_{t}}\}\)</span>输入到ID decoder中作为Q，轨迹tokens <spanclass="math inline">\(\mathcal{T}_{t-T:t-1}\)</span>视为K和V。基于此，历史轨迹中的ID tokens作为一种in-contextprompt，将特定的身份信息传播到对应的检测。之后，解码的 <spanclass="math inline">\(\widehat{d_{t}^{m}}\)</span>通过一个线性projection 网络，变换为ID概率。因此，将ID分配的步骤转换成一个K+1的分类任务，可以通过交叉熵损失，端到端的监督。</p></li><li><p><strong>训练</strong>：对每个训练iteration，采样一个videoclip，包含 <span class="math inline">\(T+1\)</span>帧。这个模型在每个时刻t预测IDs，除了第一帧。为了遵循在线跟踪协议，在训练期间用一个因果注意力掩码，来确保只有之前帧是可见的。对于提出的ID预测器，采用直接的交叉熵。因为每一帧种的目标的数量不一样，通过取所有目标的平均来计算最后的id loss。 <spanclass="math display">\[\mathcal{L}_{id}=\frac{-\sum_{t=2}^{T+1}\sum_{m=1}^{M_t}\sum_{k=1}^{K+1}y_m^k\log(p_m^k)}{\sum_{t=2}^{T+1}M_t},\\y_{m}^{k}=\left\{\begin{array}{ll}1 &amp; \mathrm{gt} \ id \ \mathrm{of} \ m^{th} \ \mathrm{object~is}\ k, \\0 &amp; \mathrm{else},\end{array}\right.\]</span></p><p><span class="math inline">\(T+1\)</span> 表示一个trainingclip中的帧数，在第t帧中有 <span class="math inline">\(M_t\)</span>ground-truth 目标。根据每个目标的identity ground truth, <spanclass="math inline">\(y^k_m\)</span> 是一个indicator函数。在实际中，同时端到端的训练目标检测器和ID预测器，因此，利用整个的loss函数<span class="math inline">\(\mathcal{L}\)</span> 来监督两部分：</p><p><spanclass="math display">\[\mathcal{L}=\lambda_{cls}\mathcal{L}_{cls}+\lambda_{L1}\mathcal{L}_{L1}+\lambda_{giou}\mathcal{L}_{giou}+\lambda_{id}\mathcal{L}_{id},\]</span></p><p><span class="math inline">\(\mathcal{L}_{cls}\)</span> 是focalloss，<span class="math inline">\(\mathcal{L}_{1}\)</span> 和 <spanclass="math inline">\(\mathcal{L}_{giou}\)</span> 表示 L1 loss和generalized IoU loss。<span class="math inline">\(\lambda\)</span>是对应的权重。</p></li><li><p><strong>推理</strong>：在视频序列的第一帧，检测到的目标的置信度大于<span class="math inline">\(\tau_{new}\)</span>为记为新的目标，然后分配唯一的身份。在每个时刻t，首先用阈值 <spanclass="math inline">\(\tau_{det}\)</span>过滤掉来自DETR的结果，之后，这些active detections 和来自最近T帧的历史轨迹 <span class="math inline">\(\mathcal{T}_{t-T:t-1}\)</span>送到ID decoder中，预测对应的ID。在这些ID预测中，只有超过特定阈值的 <spanclass="math inline">\(\tau_{id}\)</span> 会被采用，任何检测置信度大于<span class="math inline">\(\tau_{new}\)</span>的目标，但没有分配任何跟踪ID的会被考虑为一个新的目标，给一个新的id。另外，一旦长视频中的id的数量超过了K，ID词典中的tokens<span class="math inline">\(\mathcal{I}\)</span>将会被重复利用。</p></li><li><p><strong>训练中的轨迹增强</strong>：多个目标轨迹重视面临挑战性的情况，例如遮挡、模糊等。这些挑战导致推理期间的ID分配错误，损害历史轨迹的可靠性。然而，在训练期间这些情况不会出现因为所有的ID是来自groundtruth用bipartitematching得到的，类似于DETR，因此，在训练和推理之间有差异，可能降低跟踪性能。为了缓解这个问题，在训练期间引入了两个轨迹增强的技巧。首先，在同一帧中交换两个历史目标的ID；第二，以<span class="math inline">\(\lambda_{drop}\)</span>的概率随机扔掉给定轨迹中的 l个连续的tokens，l是一个从均匀分布中采样得到的随机长度。这两个方法能够模拟训练期间的目标遮挡和IDfailures，因此增强了模型的robustness。</p></li></ol><p><imgsrc="https://figures.semanticscholar.org/188f2b8234c7acb473ae305f832bfbb968b1d8f1/2-Figure1-1.png"alt="MOT的pipeline" /> <span class="math inline">\(Fig.1^{[1]}\)</span>Tracking-by-detection范式总是利用代理任务来计算cost matrix用于IDmatching。Tracking-by-query方法用trackqueries来表示跟踪的目标，然后逐帧传播，作者的方法直接预测当前detections的IDlabel。</p><p><imgsrc="https://figures.semanticscholar.org/188f2b8234c7acb473ae305f832bfbb968b1d8f1/5-Figure2-1.png"alt="Overview of MOTIP" /> <spanclass="math inline">\(Fig.2^{[1]}\)</span>这里有三个主要的组件：一个可学习的ID词典，表示不同的identities，一个DETR detector检测感兴趣的目标和一个IDdecoder预测当前帧中目标的ID，将object embeddings和它们对应的IDembeddins结合起来，形成历史轨迹，因此，ID field被视为一个in-contextprompt，ID decoder预测每个新检测的ID。</p>]]></content>
    
    
    <summary type="html">&lt;h3 id=&quot;multiple-object-tracking-as-id-prediction1&quot;&gt;Multiple Object
Tracking as ID Prediction&lt;sup&gt;[1]&lt;/sup&gt;&lt;/h3&gt;
&lt;blockquote&gt;
&lt;p&gt;作者是来自南大、上海AI Lab和中国移动的Ruopeng Gao、Yijun Zhang和Limin
Wang。论文引用[1]:Gao, Ruopeng et al. “Multiple Object Tracking as ID
Prediction.” ArXiv abs/2403.16848 (2024): n. pag.&lt;/p&gt;
&lt;/blockquote&gt;
&lt;h3 id=&quot;time&quot;&gt;Time&lt;/h3&gt;
&lt;ul&gt;
&lt;li&gt;2024.Mar&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id=&quot;key-words&quot;&gt;Key Words&lt;/h3&gt;
&lt;ul&gt;
&lt;li&gt;end-to-end in-context ID prediction&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id=&quot;总结&quot;&gt;总结&lt;/h3&gt;
&lt;ol type=&quot;1&quot;&gt;
&lt;li&gt;在多目标跟踪中，tracking-by-detection方法经历了很多测试，将这个过程分为检测和关联阶段，利用鲁棒的单帧检测器，通过人工设计的启发式算法和代理任务，将目标关联视为一个后处理步骤。然而，启发式方法的本质是阻止了对端到端训练数据的利用，导致当面临复杂或者新的场景的时候，增加的人为的修改。在本文中，将目标关联任务视为一个端到端的in-context
ID预测问题，提出了一个baseline称之为MOTIP。特别地，当考虑将对应的IDs作为in-context
prompts时，将target
embeddings嵌入到历史轨迹信息中，然后直接预测当前帧的目标的ID
labels。MOTIP能够直接从训练数据中学习tracking
capabilities，将其从繁重的人工算法中解放出来。&lt;/li&gt;
&lt;/ol&gt;</summary>
    
    
    
    <category term="Papers" scheme="https://young-eng.github.io/YoungBlogs/categories/Papers/"/>
    
    
    <category term="Object Tracking" scheme="https://young-eng.github.io/YoungBlogs/tags/Object-Tracking/"/>
    
  </entry>
  
  <entry>
    <title>SORT</title>
    <link href="https://young-eng.github.io/YoungBlogs/2024/12/17/SORT/"/>
    <id>https://young-eng.github.io/YoungBlogs/2024/12/17/SORT/</id>
    <published>2024-12-17T06:52:22.000Z</published>
    <updated>2024-12-17T06:52:22.841Z</updated>
    
    <content type="html"><![CDATA[]]></content>
    
    
      
      
    <summary type="html">
</summary>
      
    
    
    
    
  </entry>
  
  <entry>
    <title>SparseTrack</title>
    <link href="https://young-eng.github.io/YoungBlogs/2024/12/17/SparseTrack/"/>
    <id>https://young-eng.github.io/YoungBlogs/2024/12/17/SparseTrack/</id>
    <published>2024-12-17T06:51:58.000Z</published>
    <updated>2024-12-17T13:16:29.199Z</updated>
    
    <content type="html"><![CDATA[<h3id="sparsetrack-multi-object-tracking-by-performing-scene-decomposition-based-on-pseudo-depth1">SparseTrack:Multi-Object Tracking by Performing Scene Decomposition based onPseudo-Depth<sup>[1]</sup></h3><blockquote><p>作者是来自华科的Zelin Liu, Xinggang Wang等人。论文引用[1]:Liu, Zelinet al. “SparseTrack: Multi-Object Tracking by Performing SceneDecomposition based on Pseudo-Depth.” ArXiv abs/2306.05238 (2023): n.pag.</p></blockquote><h3 id="time">Time</h3><ul><li>2023.Nov</li></ul><h3 id="key-words">Key Words</h3><ul><li>sparse decomposition</li><li>a method for obtaining the relative depth of targets from 2D images:pseudo-depth</li><li>depth cascading matching(DCM)</li></ul><h3 id="总结">总结</h3><ol type="1"><li><p>探索鲁棒的、高效的关联方法一直是MOT中重要的问题，尽管现有的跟踪方法实现了很好的性能，阻塞和频繁的遮挡仍然是MOT中挑战性的问题。作者表明了：<strong>在密集场景中进行sparsedecomposition是增强关联遮挡目标的重要的步骤</strong>。为了这个目的，提出了一个pseudo-depth估计的方法，来得到2D图像中的targets的相对的深度。其次，设计了一个深度级联匹配算法，用得到的深度信息将densetarget 多个稀疏 的target subsets，在这些稀疏的targetsubsets中进行数据关联，以从近到远的方式。SparseTrack提供了一个新的视角，用于解决MOT中挑战性的拥挤场景。只用IoU匹配，SparseTrack实现了很好的性能。<span id="more"></span></p></li><li><p>MOT在很多场景中有应用。它旨在持续地在不同的视频帧中识别相同的目标，尽管之前的tracker在多个跟踪数据集上实现了很好的性能，密集场景和频繁的遮挡仍然是MOT中的重要挑战。当前的主流的跟踪方法是用tracking-by-detection(TBD)的范式，逐帧地进行数据关联。为了解决密集场景中的遮挡关联的障碍，一些方法例如bytetrack通过关联低置信度的detections，实现了有效的跟踪。尽管bytetrack展示了处理低置信度detections的高效，在大量低分数的遮挡或者频繁拥挤的场景中，它在locationassociation上的精度容易下降。其他的方法通过用了powerful时序建模和轨迹query机制来确保遮挡实例的跟踪性能。然而，这些方法是高计算成本的，特别是被大量的物体和频繁的遮挡所填充的场景。在这个工作中，<strong>作者证明了基于depthinformation的target setdecomposition是一个有效的路径，用于处理密集遮挡中的数据关联。作者展示了一个occlusionorder for the objects in a local region, occlusion order是和depthorder一致的，从近到远。因此，一组密集的occlusions可以通过基于深度信息的分割测量，被划分为几个不重叠的subsets</strong>。在相邻的targetsubsets之间，它们的x-y location可以是相似的，但是它们的x-y-depthlocations更容易区分。这个跟踪器在不同的depthlevel对稀疏的subsets进行数据关联。相比于直接关联整个的occluded objectset，<strong>在数据关联中，sparsedecomposition能够缓解位置相似但是深度不同的轨迹的冲突</strong>。特别地，优先对有更小的depths的targetsubsets进行关联，因为occlusion order和depthorder高度相关。因此，每个子集中的targets可以以fine-grained的方式被处理，受到较小的来自其他depthlevel的targets的影响。</p><p>为了实现以上的设计，提出了一个方法，用于得到来自2Dimage中的targets的relative depth: pseudo-depth，基于两个scene priors：(1)捕捉目标的相机is higher than the ground</p><ol start="2" type="1"><li>场景中的所有目标都是在平地上。换句话说，地面上没有明显的起伏。这种情况下，可以将targets的相对深度从三维空间投影到2D图像平面，<strong>得到target pseudo-depthvalues，即目标bbox的底部到图像底边的距离</strong>。<strong>这个pseudo-dpeth值用作reference，来测量targets之间的相对深度关系，用ground作为参考系统，而不是三维空间中的目标的groundtruth depth。另外，设计了一个depth cascadematching(DCM)算法，基于pseudo-depth信息，来执行层次化的关联。具体地，根据pseudo-depthvalue，将轨迹和检测划分成多个target subsets，DCM算法在这些稀疏的targetsubsets上，以pseudo-depthvalue从近到远的方式，执行IoU关联。通过将pseudo-dpeth方法和DCM集成到数据关联过程中，提出了一个新的tracker称之为SparseTrack，SparseTrack的本质是通过DCM、基于depth，对遮挡进行层次化的关联</strong>。</li></ol><p>SparseTrack在多个数据集上实现了很好的性能，证明了基于pseudo-depth的targetset decomposition的高效。 主要贡献如下：</p><ol type="1"><li>提出了一个从2D图像中得到目标的相对深度的方法：pseudo-depth，是基于场景中的两个先验，能够得到目标的pseudo-depthvalue，比较不同目标之间的相对深度关系</li><li>基于pseudo-depth方法提供的深度信息，设计了一个有效的、用于在密集场景关联遮挡的深度级联匹配方法，能够将密集的targetsets分解成多个稀疏的target subsets，实现scene decomposition</li></ol></li><li><p>MOT：当前的主流MOT关注于确保鲁棒的时序关联，导致多种tracking的发展，基于不同的时序建模方法，主流的tracking方法可以分为4类：通过位置信息的时序关联；通过外观特征的时序关联；通过图优化的时序关联；通过注意力机制的时序关联；早期的trackers，基于深度学习，执行连续帧之间的时序关联，基于恒速运动先验利用KF来对目标的帧间运动进行建模，确保了轨迹的稳定性。为了实现长期的关联，DeepSORT和MOTDT通过增加外观特征，补偿关联长期轨迹中位置信息的缺陷。尽管外观信息通过ReID模型产生的外观cuesu对于关联长期目标是有效的，目标遮挡和运动模糊对这些cues的可靠性提出了挑战，一些方法用自监督预训练增强的ReID模型来缓解遮挡目标的外观，但是有更高的计算成本。其他的tracker，通过用单一的框架，联合优化detection和reid，提高计算效率，这有点限制了检测性能和外观质量的上限。受Transformer启发，有的从注意力机制的角度执行轨迹时序的propagation，实现了复杂场景下的高性能。最近的一些工作将MOT视为一个图优化问题，用图神经网络或者图匹配，来实现鲁棒的数据关联。当场景中目标数量增加时，基于图的方法的计算开销会增加2倍。</p></li><li><p>处理目标遮挡的方法：tracker的性能一定程度上依赖于处理遮挡的能力，最近的工作尝试从不同的角度处理遮挡。例如MotionTrack学习轨迹的运动模式，将它和历史信息结合，来对遮挡的轨迹进行建模。MOTR是一个端到端的MOT框架，用detectionqueries和trajectoriesqueries处理新出现的目标和跟踪目标。由于queries和大量的时序训练之间的相对独立，MOTR在遮挡的时序建模表现很好。DPMOT提出subject-ordereddepth estimation方法，来自动的对2D 场景中的检测目标的deption位置进行排序，通过构建pseudo-3D卡尔曼滤波，DPMOT实现了鲁棒的遮挡目标的关联，BoTSORT结合相机运动补偿和IoU-ReID融合，能够结合运动和外观信息实现更精确的遮挡跟踪。OUTrack用一个无监督的reid模块和occlusion-awaremodule来预测目标遮挡出现的位置，为了补偿缺失的detections。ApLift引入了一个先进的近似求解器用于处理不相交的pathproblem，来管理扩展的和拥挤的轨迹序列。</p></li><li><p>目标集合分解：很多的trackers跟着tracking-by-detection范式，执行一定程度的targetset分解。例如DeepSORT通过多阶段的级联匹配，实现了逐步的检测关联。FairMOT基于外观和位置信息，分别处理detectionset，也可以被视为通过tracking clues的detectionset的分支。LMGP通过引入了一个pre-clustering的方法，有效地消除了single-cameratrackers的轨迹误差，将检测到的objects分组用于association。ByteTrack将detectionset基于置信度分数，分为高分和低分，用scores和occlusion之间的关联分别处理低分遮挡的目标。然而，在密集场景中，阻塞导致遮挡，导致低分的检测，虽然bytetrack将低分的occlusion从场景中解耦，低分的目标仍然是拥挤的。这种情况下，基于IoU的数量关联会产生更多的匹配误差，限制了跟踪的能力，为了这个目的，SparseTrack基于pseudo-depthlevels，划分detectionset和轨迹set，确保关联阶段中的目标不再拥挤。</p></li><li><p>深度信息在MOT中的应用：深度信息用在了很多与3D场景有关的应用中，例如单眼或者立体深度估计、3D检测和跟踪，最近流行的Nerf。标准的深度提供了物体的大量的外观和位置信息，导致不同的3Dtracking方法。例如TrackRCNN用外观和运动特征用于tracking，通过交替跟踪3D点云和2D图像中的物体，增强了精度和稳定性。FANTrack用了一个基于特征的关联网络FAN用于tracking，features是来自3DCNN from point cloud data和2D CNN from cameraimage。CenterPoint用keypoint检测器检测目标的中共新，回归出其他属性例如3Dsize、3D direction、speed等。然而，通过简单地greedy nearest pointmatching执行3D objecttracking。<strong>事实上，二维图像可以被视为在透视变换下三维场景的投影。根据相机模型，可以简单地推理2D图像和3D场景之间的关系。SparseTrack利用这个variablerelationship，来得到2D图像中目标的pseudo-depth，部分取代了标准深度在描述物体间位置关系的作用</strong>。</p></li><li><p>GeneralFramework：SparseTrack遵循tracking-by-detection范式，执行逐帧的数据关联。给定要给frame<spanclass="math inline">\(f_i\)</span>，首先被YOLOX处理，输出bbox和置信度分数。然后，基于每个检测到的bbox和输入的imagesize，用pseudo-depth方法得到2D图像中的目标的pseudo-depth值。pseudo-depth值反应了图像中目标之间的相对深度关系。在数据关联阶段，用pseudo-depth值和用DCM算法来完成decomposition和associationfor tracks and the detections。最后，SparseTrack输出tracking results(<span class="math inline">\(x_1,y_1,w,h,s\)</span>)</p></li><li><p>Pseudo-Depth：在通常的跟踪场景中，pseudo-depth方法是基于两个先验的假设：捕捉目标的相机isabove tehground和场景中的所有目标在同一个平面上。这些假设在一般的场景中很容易满足，特别地，pseudo-depth方法是一个获得pseudo-depthvalues的简单的几何方法；首先，得到位置点 <spanclass="math inline">\(P_0\)</span>，物体在3D space中接触地面的点，将<span class="math inline">\(p_0\)</span>投影到图像平面F上，得到对应的投影点 <spanclass="math inline">\(p_1\)</span>；第二，从<spanclass="math inline">\(p_1\)</span>点画个垂线到图像的底边 <spanclass="math inline">\(L_b\)</span>，得到垂线和底边 <spanclass="math inline">\(L_b\)</span>的交点 D。顶一个投影点 <spanclass="math inline">\(P_1\)</span>和交点D之间的欧氏距离作为物体的pseudo-depth value。设置图像的高度为H，pixelcoordinates of <span class="math inline">\(P_1\)</span>为 <spanclass="math inline">\((x_p,y_p)\)</span>，pseudo-depth计算如下：</p></li></ol><p><span class="math display">\[L_p = H - y_p\]</span></p><p><span class="math inline">\(L_P\)</span>是pseudo-depth值，尽管pseudo-depth值不能代表三维空间中物体的ground truthdepth，它能够充分地衡量不同物体之间的相对depth relationship，为在depthlevel上，密集场景的划分提供了基础。</p><ol start="9" type="1"><li>作者提出了一个简单但是powerful的数据关联方法，不同于其他的方法关注于提高外观特征和motioncues，来确保关联的可靠性，SparseTrack场景中的所有目标通过pseudo-depth方法和DCM划分为多个稀疏的targetsubsets，来有效地关联遮挡。将detection set划分为高分和低分的detectionsubsets based on confidence scores。然后，用DCM算法在高分detectionsubset上进行数据关联。对于出现在之前帧中的轨迹但是不匹配的，通过DCM将它们和低分的detectionsubset关联。对于未匹配的高分detections，将它们和之前帧中出现的轨迹关联。最后，没有匹配的高分detections初始化为新的轨迹，丢失的轨迹超过了一定的帧数就会被删除。因为置信度分数和目标的遮挡相关联，基于置信度分数，继承了decomposition方法，因为高分的检测通常意味着很低的遮挡，在关联高分目标的时候，将pseudo-depthlevel设为1或2，相反，在关联低分detection的时候，将pseudo-depthlevel设为4或8.帮助密集遮挡的细粒度关联</li></ol><p><imgsrc="https://figures.semanticscholar.org/8c82653331bf3b944d335e05042418c83e65bd96/1-Figure1-1.png"alt="密集场景中关联低分检测" /> <spanclass="math inline">\(Fig.2{[1]}\)</span>在bytetrack中，低分检测将和track set用IoU进行匹配，因为低分检测的位置的高度相似，容易出错。这篇文章尝试从低分检测分解的角度来解决这个问题，而不是将它们进行匹配。</p><p><imgsrc="https://figures.semanticscholar.org/8c82653331bf3b944d335e05042418c83e65bd96/2-Figure2-1.png"alt="local occlusions" /> <spanclass="math inline">\(Fig.2{[1]}\)</span>在局部区域的occlusions。随着depthvalues的增加，被遮挡目标的排序逐渐向背景偏移</p><p><imgsrc="https://figures.semanticscholar.org/8c82653331bf3b944d335e05042418c83e65bd96/2-Figure3-1.png"alt="DCM" /> <span class="math inline">\(Fig.1^{[1]}\)</span>DCM的层次化关联的例子，白色方形区域表示在水平和竖直方向上都没有对应的detection和轨迹subsets之间的数据关联。方形中的深色区域反应了轨迹和detections之间的相似度，更深的颜色表示更高的相似度。DCM根据depthorder 从近到远，将同一个depth level的targetset分解为多个subsets。对于每个depthlevel中的没有匹配的轨迹和检测，tracker会在下一个depthlevel中处理它们。</p><p><imgsrc="https://figures.semanticscholar.org/8c82653331bf3b944d335e05042418c83e65bd96/4-Figure4-1.png"alt="framework of SparseTrack" /> <spanclass="math inline">\(Fig.2{[1]}\)</span>SparseTrack的框架，图例信息位于右下角的框中，<spanclass="math inline">\(\tau\)</span>用来划分高分和低分，SparseTrack执行target setdecomposition，通过DCM和pseudo-depth来精确地关联低分occlusions。</p>]]></content>
    
    
    <summary type="html">&lt;h3
id=&quot;sparsetrack-multi-object-tracking-by-performing-scene-decomposition-based-on-pseudo-depth1&quot;&gt;SparseTrack:
Multi-Object Tracking by Performing Scene Decomposition based on
Pseudo-Depth&lt;sup&gt;[1]&lt;/sup&gt;&lt;/h3&gt;
&lt;blockquote&gt;
&lt;p&gt;作者是来自华科的Zelin Liu, Xinggang Wang等人。论文引用[1]:Liu, Zelin
et al. “SparseTrack: Multi-Object Tracking by Performing Scene
Decomposition based on Pseudo-Depth.” ArXiv abs/2306.05238 (2023): n.
pag.&lt;/p&gt;
&lt;/blockquote&gt;
&lt;h3 id=&quot;time&quot;&gt;Time&lt;/h3&gt;
&lt;ul&gt;
&lt;li&gt;2023.Nov&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id=&quot;key-words&quot;&gt;Key Words&lt;/h3&gt;
&lt;ul&gt;
&lt;li&gt;sparse decomposition&lt;/li&gt;
&lt;li&gt;a method for obtaining the relative depth of targets from 2D images:
pseudo-depth&lt;/li&gt;
&lt;li&gt;depth cascading matching(DCM)&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id=&quot;总结&quot;&gt;总结&lt;/h3&gt;
&lt;ol type=&quot;1&quot;&gt;
&lt;li&gt;&lt;p&gt;探索鲁棒的、高效的关联方法一直是MOT中重要的问题，尽管现有的跟踪方法实现了很好的性能，阻塞和频繁的遮挡仍然是MOT中挑战性的问题。作者表明了：&lt;strong&gt;在密集场景中进行sparse
decomposition是增强关联遮挡目标的重要的步骤&lt;/strong&gt;。为了这个目的，提出了一个pseudo-depth估计的方法，来得到2D图像中的targets的相对的深度。其次，设计了一个深度级联匹配算法，用得到的深度信息将dense
target 多个稀疏 的target subsets，在这些稀疏的target
subsets中进行数据关联，以从近到远的方式。SparseTrack提供了一个新的视角，用于解决MOT中挑战性的拥挤场景。只用IoU匹配，SparseTrack实现了很好的性能。</summary>
    
    
    
    <category term="Papers" scheme="https://young-eng.github.io/YoungBlogs/categories/Papers/"/>
    
    
    <category term="Object Tracking" scheme="https://young-eng.github.io/YoungBlogs/tags/Object-Tracking/"/>
    
  </entry>
  
  <entry>
    <title>Multi-Object Tracking Survey</title>
    <link href="https://young-eng.github.io/YoungBlogs/2024/12/05/Multi-Object-Tracking-Survey/"/>
    <id>https://young-eng.github.io/YoungBlogs/2024/12/05/Multi-Object-Tracking-Survey/</id>
    <published>2024-12-05T02:59:14.000Z</published>
    <updated>2024-12-17T02:15:33.443Z</updated>
    
    <content type="html"><![CDATA[<h3 id="multi-object-tracking-survey">Multi-Object Tracking Survey</h3><blockquote><p>来自论文:"Deep Learning in Video Multi-Object Tracking: A Survey",作者是来自意大利和西班牙等院校的GioeleCiaparrone等人。论文引用[1]:Ciaparrone, Gioele et al. “Deep learning invideo multi-object tracking: A survey.” Neurocomputing 381 (2019):61-88.</p></blockquote><h3 id="总结">总结</h3><ol type="1"><li><p>多目标跟踪的问题是指在一个序列中跟踪不同目标的轨迹，通常是一个视频。近些年随着深度学习的兴起，算法从深度模型的表征能力中收益。本文提供了提供了用深度学习模型来解决MOT问题的全面的综述。</p></li><li><p>MOT问题不同于目标检测(输出一系列的boundingbox)。MOT算法还将targetID和每个box(detection)进行关联，为了区分类内的目标。<strong>单目标跟踪中，目标的外观是已知的先验，在MOT中，检测这一步对于识别targets是必须的，追踪多个目标的主要困难在于多样的阻塞和目标之间的交互，有时它们还有相似的背景</strong>。因此，将单目标跟踪应用于多目标跟踪会导致目标漂移和多个ID切换，这样的模型通常在区分相似目标和类内目标的时候有困难。专门针对多目标跟踪的一系列的算法被调整出来，用于解决这个问题，还有数据benchmarks和比赛，来方便不同方法的对比。最近，很多的算法用深度学习来做，深度学习模型的长处在于学习丰富表征和提取特征。卷积神经网络长处在于空间模式提取，循环神经网络想LSTM用于处理时序数据。</p></li></ol><span id="more"></span><ol start="3" type="1"><li><p>MOT算法介绍：MOT采用的标准方式是<strong>tracking-by-detection</strong>：从视频帧中提取一系列的detections，然后用来指导追踪过程，通常将它们进行关联，为了给包含相同目标的boundingboxes分配相同的ID。因此，很多MOT算法将这个任务视为分配问题。现代的检测器保证了很好的检测质量，大多数的MOT方法聚焦于提升<strong>association</strong>；事实上，很多MOT数据集提供了标准的detections的集合，为了能在关联算法的质量上比较它们的性能，因为检测器的性能对追踪结果有很大的影响。MOT算法也可以分为batch和online方法。当决定特征帧中的目标的时候，Batchtracking算法允许用futureinformation。它们利用全局信息，导致更好的追踪质量。在线tracking算法，相反，只能用现在和过去的信息来对当前帧做预测。在一些场景下有这个要求例如自动驾驶。相比于batch方法，在线方法效果会差一些，因为不能利用futureinformation来修正过去的errors。事实上，在线方法在实际环境中跑得很慢，特别是当利用深度学习算法的时候。</p><p>尽管文献中有各种各样的方法，大多数的MOT算法有下列的几个步骤：</p><ul><li><strong>DetectionStage</strong>：目标检测算法分析每一帧，来识别目标类别usingbboxes，在MOT语境中称为<strong>Detections</strong>。</li><li><strong>Feature extraction/motionprediction</strong>：一个或多个特征提取算法分析detections/tracklets来提取外观，运动或者interactionfeatures。optionally, 一个运动预测器预测每个trackedtarget的下一个位置。</li><li><strong>Affinity Stage</strong>：features和motionpredictions用来计算detections/trackletspairs之间的相似度/距离分数。</li><li><strong>AssociationStage</strong>：相似度/距离用来关联属于同一目标的detections/tracklets，通过为识别相同目标的检测结果分配相同的ID。这些阶段可以按照上述顺序执行。有很多算法将这些步骤合并了，或者用不同的techniques多次执行。更多的是，一些方法没有直接将检测进行关联，而是用它们来refine轨迹预测，来管理新的tracks的初始化和终结，尽管如此，这种情况下，这些步骤仍然能够被识别。</li></ul></li><li><p><strong>Metrics</strong>：为了提供common的实验设定，算法能够被公正地测试和比较，建立了一组metrics，用的很广泛。</p><ul><li><p><strong>Classicalmetrics</strong>：强调了MOT算法犯得一些错误，为了展示这些问题，计算了下列的值：</p><ul><li><em>Mostly Tracked</em>trajectories(MT)：在至少80%的帧中，正确地追踪到的groundtruth的轨迹。</li><li><em>Fragments</em>：是一个轨迹假设，覆盖了至少groundtruth轨迹的80%。一个true trajectory至少有多个fragment。</li><li><em>Mostly Lost</em>：groundtruth 轨迹少于20%的帧被追踪到。</li><li><em>False Positives</em>：被追踪到的轨迹，没有groundtruth对应。</li><li><em>IDswithces</em>：目标被正确追踪的次数，但是目标关联的ID改变了。</li></ul></li><li><p><strong>CLEAR MOT metrics</strong>：CLEAR MOTmetrics是在2006/2007年举办的workshop上的CLEAR开发的。这个Metrics是MOTA和MOTP。他们作为其它构成它们的简单的metrics的summary。首先会解释简单的metrics，然后构建复杂的。因为关注的是2Dtracking，用的最多的metric，来决定object和prediction是否相关的是bboxes的IoU。groundtruth和hypotheses之间的mapping如下：**如果groundtruth object <span class="math inline">\(o_j\)</span>和 hypothesis <spanclass="math inline">\(h_j\)</span> 在 <spanclass="math inline">\(t-1\)</span> 帧 匹配，在<spanclass="math inline">\(t\)</span> 帧， IoU(<spanclass="math inline">\(o_i,h_j\)</span>) &gt;= 0.5, 则 <spanclass="math inline">\(o_i,h_j\)</span>在这帧是匹配的，即使有另外一个hypothesis <spanclass="math inline">\(h_k\)</span>， IoU(<spanclass="math inline">\(o_i,h_j\)</span>) &lt; IoU(<spanclass="math inline">\(o_i,h_k\)</span>)。匹配执行完之后，剩余的objects会和剩余的hypotheses进行匹配，仍然用0.5的IoU阈值。不能和一个hypothesis进行关联的groundtruth bboxes记为falsenegatives(FN)，不能和一个真实的bboes进行关联的hypothesess记为falsepositives(FP)。每次，一个ground truth objecttracking中断之后，之后又继续的，记为一个fragmentation。在追踪期间，每次一个追踪到的groundtruthobject ID不正确地改变，记为 ID switch。MOTA和MOTP分别定义为：</p><ul><li>FP：整个视频中的false positives的数量</li><li>FN：整个视频中的false negatives的数量</li><li>Fragm：fragmentation的数量</li><li>IDSW：ID switch的数量 <strong>MOTA的定义如下</strong>： <spanclass="math display">\[MOTA=1-\frac{(FN+FP+IDSW)}{GT}\quad\in(-\infty,1]\]</span>GT是ground truthboxes的数量。这个分数可以是负数，因为算法犯的错误可能大于groundtruthboxes的数量，通常，相反用MOTA，更common的是用MOTA，就是用百分比表示，<strong>MOTP计算如下</strong>：</li></ul><p><span class="math inline">\(c_t\)</span>表示帧t中的matches的数量，<span class="math inline">\(d_{t,i}\)</span>是hypothesis i和分配的ground truth object之间的 bboxoverlap，注意到这个metric考虑了很少的tracking，而是关注于detections的质量。</p><p><strong>IDscores</strong>：MOTA的主要问题考虑了tracker做的不正确的decision的次数，例如IDswitch，但是在一些场合，人们更感兴趣的是奖励那些能够长时间跟踪目标的跟踪器。因此，定义了一些新的metrics，来补充信息。不是通过逐帧的匹配groundtruth和detections。这个mapping是全局进行的。分配给一个给定ground truthtrajectory的轨迹预测 是 最大化 正确分为groundtruth的frames的数量。为了解决这个问题，构建了一个bipartitegraph，这个问题的最小的成本解决思路是将其视为problemsolution。对于bipartitegraph，一系列的vertices定义如下：第一组的vertices，<spanclass="math inline">\(V_T\)</span>，对于每个truetrajectory，有一个所谓的regular node，对于每个计算的轨迹有一个falsepositive node。第二个set，<spanclass="math inline">\(V_c\)</span>，对于每个计算的轨迹有一个regularnode，对于每个true one，有一个false negative。边缘的cost areset，为了计算false negative的数量和false negative frames，防止edge werechosen。在关联之后，有四个不同的pairs，如果来自 <spanclass="math inline">\(V_T\)</span> 的regular node和 <spanclass="math inline">\(V_C\)</span>的一个regular node相匹配(一个truetrajectory和一个计算的trajectory相匹配)，则计数一个 <spanclass="math inline">\(true positive ID\)</span>，来自 <spanclass="math inline">\(V_T\)</span>的每个false positive 和来自 <spanclass="math inline">\(V_C\)</span>的一个regularnode相匹配，计数为一个false positive ID。每个来自 <spanclass="math inline">\(V_T\)</span>的regular node和来自 <spanclass="math inline">\(V_C\)</span>的false negative计数为一个 falsenegative ID，最后，每个false positive和false negative匹配，计数为一个true negativeID。之后，三个分数都计算了，IDTP是真正被选为true positive IDmatches的边的权重之和(可以被视为整个视频中争取分配的detections的百分比)。IDFN是selectedfalse negative ID edges的权重之和，IDFP是selected false positive IDedges的权重之和。有了这三个basic measures，其它的三个计算如下：</p><ul><li><p>Identification precision: <spanclass="math inline">\(IDP=\frac{IDTP}{IDTP+IDFP}\)</span></p></li><li><p>Identification recall: <spanclass="math inline">\(IDR=\frac{IDTP}{IDTP+IDFN}\)</span></p></li><li><p>Identification F1: <spanclass="math inline">\(IDF1=2\frac{IDP*IDR}{IDP+IDR}\)</span></p></li></ul></li></ul><ol start="5" type="1"><li><p><strong>Benchmark datasets</strong>：</p><ul><li><strong>MOTChallenge</strong>：MOTChallenge是用的最多的MOTbenchmark。对于每个数据集，提供了groundtruthfor training split, detections for both training and testssplits，MOT数据集提供detections的原因是detection的质量对于tracker的性能有很大的影响，但是算法的检测部分通常独立于trackingpart，通常用的是现有的模型；提供publicdetections，每个模型都能用，方便追踪算法的比较，由于检测质量从性能计算中被剔除，trackers从一个共同的基础上开始。在testdataset上的算法的评估是通过把结果提交到test server。</li><li><strong>MOT15</strong>：是第一个MOTChallengedataset，包含了22个序列的videos，11 for training和11 fortesting。包含11283个不同分辨率的frames，有1221个不同的identities和101345个boxes。提供的detections是通过ACFdetector得到的。</li><li><strong>MOT16/17</strong>：2016年提出的新版本的数据集，共有14个videos，7for training, 7 for testing。用DPMv5得到的publicdetections，共计包括11235frames，有1342个identies和292733个boxes。MOT17数据集和MOT16的视频一样，但是有更精确的groundtruth，对于每个视频，有三组detections：一个是来自FasterRCNN，一个来自DPM，一个来自Scale-Dependent Pooling detector(SDP)。</li><li><strong>MOT19</strong>：用在了CVPR2019Tracking上的新版本的数据集。包含8个视频, 4 for training 4 fortesting。在最拥挤的视频中，平均每帧有245个行人，数据集有13410frames，有6869个tracks和共计2259143个boxes，比之前的数据集都多。</li><li><strong>KITTI</strong>：MOTChallegne主要关注于行人追踪，KITTI数据集追踪行人和车辆。这个数据集是通过开车绕着城市收集的，包含21个trainingvideos和29个test ones，共计19000frames。包含了用DPM和RegionLets检测器得到的detections，还有立体和laser 信息。</li></ul></li><li><p><strong>DL in detectionstep</strong>：尽管许多研究使用数据集中由不同检测器生成的检测结果作为其算法的输入，例如DPMfor MOT16。也有很多算法继承了定制的detectionstep，通过增强检测质量，提高了整体的追踪性能。很多的算法利用FasterRCNN及其变体作为customdetections。尽管很多算法用深度学习模型来提取rectangularbboxes，很少的工作用deep networks在检测阶段有不同的用途。</p><ul><li><p>SORT算法是第一个MOTpipelines用卷积神经网络用于行人检测。它们展示了通过FasterRCNN计算的detections比ACF检测器计算的detections，能够提高MOTA，它们用了一个简单的方法，用卡尔曼滤波器预测目标运动，然后用匈牙利算法关联detections，用IoUdistances来计算costmatrix，高质量的detections降低了复杂追踪算法需要。MOTA分数被falsepositives和false negatives严重影响。</p></li><li><p>有人用SSD检测器用来查找行为和车辆，用了基于CNN的相关滤波来使得SSD能够产生更多的精确的bboxes。CCF利用PCA压缩的CNN特征，来预测后面帧的目标的位置；预测的位置然后用来cropRoI aroundit。作为SSD的输入。这样，网络能够用更深的layers计算小的detections。提取更多有价值的语义信息，产生更多的精确的bboxes和lessfalse negatives。算法然后将这些detections和在fulll image上用NMSstep得到的ones进行结合，然后用匈牙利算法执行tracks和detections之间的association，with a matrix that took into account geometric(IoU) andappearance(Average Peak-to-Correlation Energy-APCE)cues。APCE也用作objectre-identification，来去掉occlusions。作者展示了<strong>用多尺度增强训练一个detector能够在tracking中有更好的结果</strong>。</p></li><li><p>有时候MOT中的CNN用来除了计算objectbboxes之外的用途。例如：CNN用来计算减小false positives</p></li></ul></li><li><p><strong>DL in feature extraction and motionprediction</strong>：特征提取阶段是使用深度学习模型的首选，因为它们有很强的表征能力，使得它们擅长提取有意义的高阶features。典型的方式是用CNN模型来提取特征。</p><ul><li>自编码器：有人提出了两层的自编码器网络，用来refine visualfeatures。在extraction 之后，用SVM计算affinity，关联任务formulated as aminimum spanning tree problem。featurerefinement极大地提升了模型性能。</li><li>CNN作为visual featureextractors：用的最广的特征提取是基于卷积网络的修改。有人将visualfeatures引入了经典算法，visual features结合用卡尔曼滤波提取的spatialones，然后计算affinity matrix； DeepSORT引入了CNN提取的visualinformation。CNN提供了128 features的vector作为output，这些vectors之间的余弦距离加入到affinityscorse。实验表明这个修改克服了SORT算法的缺点(ID switch比较大)；有人用了DeepSORT特征提取器，用一个区别相关滤波来测量特征之间的关系，之后，matchingscore和时空关系score相结合，最后的score用作一个likelihood in a GaussianMixture Probability Hypothesis Density filter。</li><li>Siamese networks：另一个idea是用结合不同images信息的lossfunctions来训练CNNs，为了学习能够区分不同目标的features。这些网络称之为孪生网络。有人提出了孪生网络，用一个contrastiveloss进行训练，网络将两个images、IoU score和arearatio作为输入，产生的contrastiveloss作为输出。在网络训练之后，计算对比损失的loss被去掉，最后一层作为featurevector for the input image。相似度分数通过结合featurevectors之间的欧氏距离、bbox之间的IoU score和arearatio被计算出来。通过用一个贪心算法解决关联的步骤。有人也提出了一个Siamesenetwork，将两个imagepatches，计算它们之间的相似度分数。测试时的得分是通过比较网络提取的两张图像的视觉特征，并结合时间约束信息来计算的。</li><li>SymTripletloss：在训练期间，用了3个共享权重的CNNs，损失函数结合了属于同一个Object的两张图片提取的信息，和不同目标的一张图片的信息。当positivepair的feature vectors之间的distance小的时候，Symtripletloss减小，当negative pairsfeatures接近的时候，增加。优化这个函数导致相同物体的图像的相似的特征，不同的物体不同的vectors，它们之间有很大的distance。首先，执行帧间的关联，这种情况下affinityscore是detections的featurevectors的欧氏距离和时序运动信息的结合。之后，tracklets进行连接，用一个HierarchicalAgglomerative Clustering 算法over 外观特征。</li></ul><p>也有人提出了SiameseCNN，接受连个image作为输入，输出两张图片都属于同一个人的概率，用这个训练网络，因此它能学到更多的表征特征来区分subjects。这之后，输出层去掉了，最后一个隐藏层提取的features和上下文信息一起作为GradientBoosting model的输入，为了得到detections之间的affinityscore。然后，用线性规划解决关联步骤。</p><ul><li><strong>CNNs用于motionprediction：相关滤波</strong>：有人研究相关滤波的employment，输出是一个追踪的物体的responsemap。这个map是下一帧物体的新的位置的估计。这个affinity可以进一步和光流affinity结合，用Lucas-Kanade算法计算。motion affinity用卡尔曼滤波计算，scale affinity通过一个设计bboxes的高和宽的ratio表示。<strong>两个detections之间的affinity是先前分数的线性组合</strong>。也有一个用SVM分类器去掉falsedetections的步骤，missing detections handling用作之前步骤计算的responsemap。如果一个目标mistakelylost，然后重新识别，这个步骤能够修正mistake，重新连接brokentracklet。也有人将相关滤波用来预测序列帧中的目标的位置，滤波器将CNN提取的外观特征作为输入，之前用PCA降过维，产生的下一帧中的目标的预测的位置作为输出，预测的位置之后用来计算相似度分数，结合预测和检测之间的IoU，和responsemap之间的APCE 分数。之后构建cost matrix，计算every pair of detectionsbetween frames的said score，分配的问题用匈牙利算法解决。</li></ul><p>还有人用了强化学习的方法，来训练一些agents，帮助特征提取，算法完全基于Motionfeatures。不需要任何视觉信息，用卡尔曼滤波器学习motionmodel。它的行为有一个agent管理，用一个agentfor each trackedobject。这个agent学习决定卡尔曼滤波器应该采取哪个行为between a set ofactions，包括忽略预测，忽略newmeasure。这个作者声称他们的算法可以在即使非视觉场景下解决它们的算法，和性能深受视觉特征影响的经典算法相比。</p></li></ol></li><li><p><strong>DL in affinitycomputation</strong>：很多工作通过用一些distance measure over featuresextracted by aCNN，计算tracklets和detections之间的affinity，也有一些算法用深度学习模型来直接输出affinityscore，不需要显示地计算features之间的distance metric。</p><ul><li><strong>CNNs for affinitycomputation</strong>：有人用4个不同的CNNs来计算图中点之间的affinityscore，关联任务视为最小化成本的对切口问：它可以被视为graph clustering问题，每个输出的cluster表示单个trackedobject。与边关联的代价考虑了两个检测之间的相似性。这相似性是personre-id置信度，深度一致匹配和时空关联匹配的结合。为了计算person-reidaffinity，用了多种的架构。Siamese CNNs也是一种用于affinity计算的常用的方式，</li></ul></li><li><p><strong>DL in Association/Trackingstep</strong>：有一些工作用深度学习模型，来提高通常由经典算法例如匈牙利算法执行的关联过程。有人用一个双向GRURNN来决定在哪split tracklets，算法在三个方面进行：一个trackletgenerationstep，包含一个NMS步骤，来去掉冗余的检测，用外观和运动affinity的匈牙利算法来形成高置信度的tracklets；然后，是一个trackletcleaving步骤；因为occlusions，一个tracklet可能包含一个ID switcherror。这个步骤旨在分离IDswitch发生的点，为了得到包含相同identity的两个分离的tracklets，最后用了一个tracklet重连步骤，用一个customized关联算法，用来Siamese双向GRU提取的特征。GRU为每一帧输出一组feature vectors。计算这些featurevectors之间的距离。</p><p>也有一些工作用深度强化学习agents在跟踪阶段做决定。用多个deep RLagents来manage 多个trackedtargents，决定tracks什么时候开始和结束。这个agent是用3层的MLP建模的。有人用了3个不同的CNN。第一个称之为PafNet，用来从trackedobjects中区分背景。第二个称之为PartNet，用来区分不同的目标。第三个CNN，有一个卷积和FC层构成，用来决定是否更新trackingmodel。整个的算法流程是这样的：对于之前帧的中每个追踪到的目标，在当前帧中用PafNet和PartNet计算scoremaps。然后，用核相关滤波器tracker，预测目标的新的位置。更多的是，在特定帧之后，执行一个检测验证的步骤；检测器输出的结果通过解决一个graphmulticut 问题，分配给trackedtargets。没有和一定数量的帧的detection关联的targets会被终结。然后，第三个CNN用来检查关联的detectionbox是否比预测的更好，如果是，KCF模型参数会更新，反应目标特性的变化。这样的CNN用PafNet提取的maps，用强化学习进行训练。没有关联的detections用SVM分类器和匈牙利算法用来恢复targetocclusion；最后，剩余的没有关联的detections用来初始化新的targets。</p></li><li><p>分析：用publicdetections在降低FNs方面有很有限的提升，最有效的方式是构建和训练一个custom检测器，降低了FNs的数量是privatedetectors有更好的tracking效果的主要原因，能够识别之前uncoveredtargets。SORT算法对丢失的detections很敏感，当相关的检测丢失的时候，不能检测一个target。为了避免这个问题，很多新的算法用了新的策略来解决这个问题。事实上，执行插值的方法能够在occlusion期间恢复missingboxes，对于检测没有被single detectioncover的目标来说这是不够的。深度学习算法在检测和特征提取上扮演了重要的角色，用深度网络来学习affinity函数没有那么普遍，对于MOT来说还没有被证实是一个重要的算法。很多算法依赖于在深度和非深度特征上的手工distancemetrics的结合。然而，这些工作展示了用affinity网络能够产生top-performing的算法，从用SiameseCNNs到循环神经网络。特别地，有人提出了自适应的Siamese网络，能够产生可靠的相似度测量，帮助在occlusions之后的行人重识别，使得算法达到了最高的MOTA。bodypart information的集成对于StackNetPose CNN也是重要的。作为一个attention机制，允许网络关注输入图像的相关部分，能够产生更精确的similaritymeasures。这个算法用privatedetections在MOT16上达到了最高的性能。很少的工作用深度学习模型来指导association过程，这个是未来研究的一个方向。</p></li><li><p>基于SOT的MOT方法必须小心，不要保持由不可避免的大量的falsepositive detections造成的虚假的轨迹。当前的方法仍然倾向于用detectionoverlap(detection cover了多少最近的帧)来理解一个轨迹在long run中是tru orfalsepositive，但是更好的解决方法应该是用来避免detections中多余的reliance。<strong>很多方法将关联视为一个图优化问题</strong>，batch方法从中收益，因为它们能够执行全局优化；例如，最小成本liftedmulticut problem在MOT16上取得了很好的表现，然而heterogeneous associationgraph fusion和correlation clustering用在了top MOT17方法中。最终，bboxes的精度影响了最终的算法性能。MOT15撒谎给你的top rankedtracker用了一个deep RL agent在之前的算法上进行了一个bbox回归的步骤。开发一个有效的bbox回归器用在MOT上也是一个有趣的研究方向。</p></li><li><p><strong>总结</strong>：MOTpipeline的几个主要的步骤如下：<strong>Detection, feature extraction,affinity computation,association</strong>。在每个步骤上用深度学习都被探索过了，然而大多数的方法聚焦于前两个，一些用深度学习来学习affinityfunctions也展示了，但是只有少数的方法直接用深度学习来指导关联过程。在MOT挑战上的结果表明，尽管有很多不同的方法，现有方法也有一些共同点：</p><ul><li><strong>检测的质量很重要</strong>：falsenegative的数量主导着MOTA的分数，用高质量的detections仍然是减小falsenegatives的最有效的方式。因此，好好在检测阶段用上深度学习能够提高追踪算法的性能。</li><li><strong>CNNs在特征提取上很重要</strong>：外观特征对于一个好的tracker是重要的，CNNs在提取这个上很有效。更多的是，strongtrackers倾向于和motionfeatures联合起来用，可以通过LSTMs，卡尔曼滤波器或者贝叶斯滤波器进行计算。</li><li><strong>SOT trackers和全局图优化</strong>：SOTtrackers适配到MOT任务，用深度学习，可以得到很好的online trackers；batch方法受益于深度学习模型在全局图优化算法上。 一些promising的研究方向：</li><li><strong>研究更多的策略来减小检测误差</strong>：尽管现在的检测器实现了很好的性能，但是仍然倾向于产生一些falsenegatives和false positives in complexscenarios，一些算法提供了解决方式通过将它们和从其它sources提取到的信息结合起来，来降低exclusivereliance on detections。</li><li><strong>用DL来追踪不同的targets</strong>：大多数的基于DL的MOT算法聚焦于行人追踪，一些不同的targets提出了不同的挑战</li><li><strong>研究当前算法的稳健性</strong>：在不同的场景下对当前的算法提出了挑战</li><li><strong>将DL用于指导关联</strong>：将DL用于指导association算法，直接执行tracking仍然是研究得比较少的问题；这个方向需要更多的研究，来理解DL算法是否有用。</li><li><strong>将SOT tracker和privatedetections结合</strong>：一个可能的方式减小lost tracks的数量，降低falsenegatives，将SOT tracker和private detections结合特别是batchsetting，有可能恢复之前丢失的detections</li><li><strong>研究bbox回归</strong>：用bbox回归展示了一个promising的步骤，但是还没有被仔细的explored，有很大的提升空间，用过去和将来的信息来指导回归</li><li><strong>研究post-tracking处理</strong>：在batchcontexts中，有可能在tracker的输出上用一些correction算法来提高它的性能。</li></ul></li></ol><h3id="multiple-object-tracking-in-recent-times-a-literature-review2">MultipleObject Tracking in Recent Times: A Literature Review<sup>[2]</sup></h3><blockquote><p>作者是来自Islamic University of Technology的MkBashar等人，论文引用[2]:Bashar, Mk et al. “Multiple Object Tracking inRecent Times: A Literature Review.” ArXiv abs/2209.04796 (2022): n.pag.</p></blockquote><h3 id="time">Time</h3><ul><li>2022.Sep</li></ul><h3 id="总结-1">总结：</h3><ol type="1"><li><p>MOT是重要的视觉任务，像是拥挤场景中的occlusion，相似背景，小目标检测，IDswitching等，为了解决这些挑战，研究人员用注意力机制，图卷积神经网络的tracklets的interrelation，用siamese网络的在不同帧的目标相似性，它们也尝试基于CNN网络简单的IOU匹配，用LSTM的motionprediction，为了把这些分散的技术弄到一个框架下，研究了过去3年超过100篇论文，尝试提取研究人员用来解决MOT问题的techniques。</p></li><li><p><strong>为了完成追踪的任务，首先需要定位帧中的目标，然后，每个目标分配一个ID，连续帧的每个相同目标会有轨迹，这里的目标可以是行人，车辆等。如果我们想追踪帧里的多个目标，就称之为MOT，可以追踪同类的所有目标或者上述类别的所有目标。如果我们直追踪单个目标，则称之为SOT，MOT比SOT更具有挑战性。因此研究人员提出了很多基于深度学习的架构来解决MOT相关的问题</strong>。之前也有一些MOT的review的paper，但是它们只包含基于深度学习的数据关联的方法，只分析problemstatement，对paper的分类没有做好，实际中的应用也是缺失的。这篇MOT的组织如下：</p><ul><li>MOT的主要的挑战</li><li>列出MOT的经常用的路径</li><li>写下MOT benchmark数据集的总结</li><li>MOT metrics的总结</li><li>一些应用</li></ul></li><li><p>MOT有很多挑战：</p><ul><li><strong>Occlusion</strong>：当我们想要看的东西被部分或者全部遮挡时，occlusion发生了，大多数的MOT方法只基于cameras，没有sensor的数据，这是为什么对于一个tracker来说，当目标模糊了之后，tracker很难追踪它的位置。另外，在拥挤场景中，occlusion更严重，一段时间以来，用bboxes来定位目标在MOT中很流行。但是在拥挤场景中，occlusions很难处理因为bboxes是重叠的，这个问题通过联合tracking和segmentation来解决。Inliterature，可以用外观信息和图信息来找到<strong>globalattributes来解决occlusion</strong>。然而，频繁的occlusion在MOT问题上又很低的精度。研究人员尝试去解决这个问题</li><li><strong>轻量化架构的挑战</strong>：尽管最近的解决方案依赖于heavy-weight架构，这很消耗资源。</li><li><strong>一些常见的挑战</strong>：MOT会受到目标检测的影响，如果目标没有正确检测到，整个的追踪努力就会白费。由于相机和目标的运动，运动模糊使MOT更具有挑战性。主要的一个挑战就是detection和tracklet之间的关联。不正确或者不精确的objectdetection是低精度的结果。还有一些挑战，例如相似的外观特征会混淆模型，tracks的初始化和结束也很重要。还有小目标的追踪。</li></ul></li><li><p>MOT的方法：<strong>tracking主要有两个步骤：目标检测和目标关联</strong>。一些工作聚焦于目标检测，一些聚焦于数据关联。对于这两步，有很多不同的方法，这些方法不管是检测阶段还是关联阶段，都是不可differentiable的，有时候能看到方法的有重叠，大多数的papers，用了MOTcomponent的多种的组合。</p><ul><li><p><strong>Transformer</strong>：Transformer的注意力机制帮助记住上下文forlong term。基于query keyfashion，transformer用来预测输出。视觉研究人员聚焦于利用它来做上下文的记忆。在大多数情况下，MOT去基于之前的信息预测目标在下一帧中的位置，这里认为transformer是最好的，因为transformer能够处理时序信息。TransTrack用Transformer从<strong>objectquery和trackquery</strong>中来预测两组bboxes。通过简单的IoU匹配，找到最后的目标的trackingbox，和<em>tracking by detection</em>范式一样。</p></li><li><p><strong>GraphModel</strong>：图卷积网络是一种特殊的卷积网络，卷积网络是图的形式而不是线性的。从连续帧中检测到的目标视为一个node，两个node之间的连接视为一个edge。通过匈牙利算法实现数据关联。</p></li><li><p><strong>检测和目标关联</strong>：检测是通过深度学习模型来做的，但是主要的挑战是关联目标，为了追踪感兴趣目标的轨迹，有很多不同的方法。有人采用了自下而上和自上而下的方法。在自下而上的方法中，现决定点的轨迹。但是在自上而下的方法中，先决定box的轨迹。通过结合两者，得到完整的目标的轨迹。为了解决关联的问题，有人简单地检测目标，然后用匈牙利算法进行关联。<strong>TrackRCNN</strong>是RCNN的扩展，是一个3D的卷积网络，能够同时进行检测和追踪。在2020年，<strong>有人将目标检测和reid用两个分支来做，两个分支在架构上相似，都用center来提取特征，分别进行检测和re-id。他们声称在两个任务上focusedequally，这就是为什么它们称之为FairMOT</strong>。在2021年，有两篇文章用LSTM来提高数据关联。有人提出了DetectionRefinement forTracking(DRT)，通过半监督学习来产生heatmap来更精确地定位物体，实现检测。这个架构有两个分支，第二个分支能够恢复遮挡的物体，同时，这篇文章也通过LSTM解决了数据关联的问题。也有人提出了CorrTracker，是一个关联网络，能够通过关联传播信息。通过自监督学习来实现检测。有人也基于FairMOT引入了D2LA网络，来保持精度和复杂度的平衡。为了避免遮挡，它们采用了称之为stripattentionmodule的measure；另一方面，有人评估每个检测到的目标，将每个目标mapping到对应的pose，因此它们能够在occlusion之后识别object。有人提出了它们自己的数据集，用称之为HeadHunter的架构fordetection还有HeadHunter-T fortracking。HeadHunter有两个阶段，在第一个阶段，用FPN和Resnet-50来提取特征。在第二阶段，用FasterRCNN和RPN来产生objectproposals。Jialian等人提出了两个modules：基于association module的costvolume和motion-guided feature warper(MFW)module，来分别提取目标位置的偏置信息和将信息在帧与帧之间传播，将整个过程称为TraDeS。Changzhi等人提出了ParalleMOT，有两个不同的分支用于detection和re-id。</p><p>有人做目标追踪的同时做objectforecasting。用FairMOT检测bboxes，然后stack预测网络，做了一个JointLearningArchitecture。有人提取每一帧的新的特征，来得到全局信息，积累partialfeatures用于occlusionhandling。他们将两种类型的features融合，来检测行人。</p><p>Hong等人用Non-Maskable Suppression在trackingphase，来降低重要的bboxes被去掉的概率。Jian等人用NMS来去掉检测器中冗余的bboxes，他们通过比较features和重新识别bboxeswith the help of IoU，来重新检测轨迹的位置。最终的输出是jointre-detection和re-id tracker。</p></li><li><p><strong>注意力机制</strong>：为了重新识别occluded目标，注意力是需要的，注意力意味着仅考虑消除背景的objects ofinterest，因此features能够被记住很长时间，即使在occlusion之后。Yaoye等人引入了stripattention module来重新识别被遮挡的行人，这个module实际上是一个poolinglayer，包括max和meanpooling，能够从行人中提取更丰富的信息，所以当被blocked的时候，模型能够记住。Tianyi提出了spatial-attention机制，通过执行Spaital Transformation Network(STN) in an appearancemodel，来强制模型仅关注于前景，在另一方面，Lei等人首次提出了PrototypicalCross-Attention Module(PCAM)来从过去的帧中提取相关的特征。他们用Prototypical Cross-AttentionNetwork(PCAN)在整个网络中传递前景和背景的对比特征。</p></li></ul><p>Huiyuan等人提出自注意力机制来检测车辆。Jiaxu等人用了交叉和自注意力，自注意力用于提取robustfeatures。然后数据给到cross-attention用于instance 关联。</p><ul><li><p><strong>MotionModel</strong>：motion是目标不可避免的一个特性。因此这个特征可以用在MOT中，无论是检测还是关联。目标的<strong>motion可以通过物体在两帧之间的位置差来进行计算</strong>。基于这个measure，有不同的方法。Hasith等人用motion来计算dissimilaritycost，通过实际位置和预测位置的差来计算Motion。为了预测occludeobject的位置，Bisheng等人用基于LSTM的motion model。Wenyuan等人将DeepAffinity Network和motionmodel结合，通过去掉目标不可能的位置来优化数据关联。</p><p>Qian等人通过用Multi-frame differencing和Low-rank matrixcompletion，从连续的satellite frames中measuedistance，来计算motion。形成了一个motionmodel的baseline，来检测和降低虚警的数量。Shoudong用motionmodel通过Motion-Aware Tracker用于运动估计和关联。Zhibo提出了compensationtracker可以得到丢失的目标。</p></li><li><p><strong>SiameseNetwork</strong>：<strong>两帧之间的相似性信息在目标追踪中有很大的帮助。这个Siamese网络能够学习相似性，对输入做差分。这个网络有两个平行的subnetworks，共享权重和参数空间，最后，两个twin网络的参数are tiedup，然后在一个特定的损失函数上进行训练，来测量它们之间的语义相似</strong>。Bing通过引入motionmodel，提升了MOT网络。他们将Siamese networktrackign嵌入到FasterRCNN中，实现了快速的tracking。Cong等人提出了一个CleavingNetwork，用Siamese Bi-directional GRU(SiaBiGRU) in post-processing thetrajectories，来消除损坏的tracklets。然后他们建立了re-connection网络，来连接tracklets，形成轨迹。<strong>在一个典型的MOT网络中，有预测和检测模块，预测模块来预测下一帧中的物体的外观，检测模块检测物体。两个模块的结果用于匹配特征和更新目标的轨迹</strong>。Xinwen等人提出了SiameseRPN结构作为预测器，他们也提出了一个自适应的阈值方法用于数据关联。Philippe等人将高效的Transformer引入到一个SiameseTracking network。他们用transformer layer替换卷积层。</p></li><li><p><strong>Tracklet关联</strong>：一组连续的感兴趣目标的帧称为tracklet。在检测和追踪目标的时候，tracklets首先用不同的算法被识别，它们关联到一块构建一个轨迹。Tracklet关联是一个有挑战性的问题。</p><p>Jinlong等人提出了Tracklet-PlaneMatching，从检测的目标中构建短的tracklets，然后再tracklet-plane中对齐，根据每个trackelt的开始和结束的时间，每个tracklet分配了一个超平面。然后形成了大的轨迹。这个过程能够处理非相邻的和重叠的tracklets。Duy等人通过3D的几何算法构建轨迹，它们是从多相机来做的，通过formulating空间和时序信息，来全局优化关联。</p><p>Cong提出了PositionProjection网络，将轨迹从局部转换到全局。Daniel通过将new-comingobject分配给之前发现的occluded object来实现re-id occludedobjects。</p></li></ul></li></ol><p><imgsrc="https://figures.semanticscholar.org/36973996f54a0dd1baf9e4e49a10db7b4d3a4fb9/4-Figure2-1.png"alt="Workflow of MOT" /> <spanclass="math inline">\(Fig.1^{[1]}\)</span>MOT算法的工作流，给定一个适配的rawframe，用一个目标检测器来得到objects的bboes；然后，对于每个检测到的objects，计算不同的features，通常是visual和motion；在这之后，affinity这个步骤计算两个objects属于同一个target的概率；最后，一个关联步骤给每个object配一个ID。</p><p><imgsrc="https://figures.semanticscholar.org/1b91e3aea4b247b51f40b061de48532de4bc09ba/3-Figure2-1.png"alt="MOT approaches categorization" /> <spanclass="math inline">\(Fig.2^{[2]}\)</span> 最近的MOT方法的组织</p><h3 id="multiple-object-tracking-a-literature-review3">Multiple ObjectTracking: A Literature Review<sup>[3]</sup></h3><blockquote><p>作者是来自腾讯、自动化所等单位的Mk Bashar等人，论文引用[3]:Luo,Wenhan et al. “Multiple object tracking: A literature review.” Artif.Intell. 293 (2014): 103448.</p></blockquote><h3 id="总结-2">总结</h3><ol type="1"><li><p>尽管有很多不同的方法来解决这个问题，它仍然是具有挑战性的，因为有外观改变、严重的遮挡等因素。MOT这个任务是在给定输入视频的条件下，主要划分为定位多个物体、保持身份ID、产生各自的轨迹。作为计算机视觉的一个中级任务，多目标追踪是姿态估计、行为识别/分析等的基础。它有多个应用。MOT可以被视为一个<strong>多变量估计</strong>问题。给定一张图像，用 <span class="math inline">\(s^i_t\)</span>表示在第 t 帧的第 i个物体。</p></li><li><p>接着常用的tracking-by-detection或者Detection baseTracking(DBT)范式，用 <span class="math inline">\(o^i_t\)</span>表示第t帧的第i个观测。MOT的目标是找到所有目标的最优的序列状态，给定的所有观测中，通过从序列状态的条件分布，执行MAP(Maximuma posteriori,最大后验)估计： <spanclass="math display">\[\widehat{\mathbf{S}}_{1:t}=\underset{\mathbf{S}_{1:t}}{\operatorname*{\arg\max}}P\left(\mathbf{S}_{1:t}|\mathbf{O}_{1:t}\right).(1)\]</span></p><p>之前的不同的MOT算法，可以被认为是设计不同的<strong>方法</strong>来解决MAP问题，或是概率推理的角度，或是确定性优化的角度。基于概率推理的方法，通常用等式(1)进行推理，用如下的两步进行迭代：</p><p><span class="math display">\[\begin{aligned}&amp; \mathbf{Predict:}P(\mathbf{S}_{t}|\mathbf{O}_{1:t-1})=\intP(\mathbf{S}_{t}|\mathbf{S}_{t-1})P(\mathbf{S}_{t-1}|\mathbf{O}_{1:t-1})d\mathbf{S}_{t-1},\\&amp; \mathbf{Update:}P(\mathbf{S}_{t}|\mathbf{O}_{1:t})\proptoP(\mathbf{O}_{t}|\mathbf{S}_{t})P(\mathbf{S}_{t}|\mathbf{O}_{1:t-1}).\end{aligned}\]</span></p><p>这里的 <spanclass="math inline">\(P\left(\mathbf{S}_{t}|\mathbf{S}_{t-1}\right)\)</span>和 <spanclass="math inline">\(P\left(\mathbf{O}_{t}|\mathbf{S}_{t}\right)\)</span>分别是动态模型和观测模型，这个基于确定性优化的方法直接最大化概率函数，<spanclass="math display">\[\widehat{\mathbf{S}}_{1:t}=\arg\max_{\mathbf{S}_{1:t}}P\left(\mathbf{S}_{1:t}|\mathbf{O}_{1:t}\right)=\arg\max_{\mathbf{S}_{1:t}}L\left(\mathbf{O}_{1:t}|\mathbf{S}_{1:t}\right)=\arg\max_{\mathbf{S}_{1:t}}\prod_{n}P\left(\hat{\mathbf{O}}_{1:t}^{n}|\mathbf{S}_{1:t}\right),(2)\]</span> 或者相反，最小化能量函数：<spanclass="math inline">\(E\left(\mathbf{S}_{1:t}|\mathbf{O}_{1:t}\right):\)</span></p><p><spanclass="math display">\[\widehat{\mathbf{S}}_{1:t}=\arg\max_{\mathbf{S}_{1:t}}P\left(\mathbf{S}_{1:t}|\mathbf{O}_{1:t}\right)=\arg\max_{\mathbf{S}_{1:t}}\exp\left(-E\left(\mathbf{S}_{1:t}|\mathbf{O}_{1:t}\right)\right)/Z=\arg\min_{\mathbf{S}_{1:t}}E\left(\mathbf{S}_{1:t}|\mathbf{O}_{1:t}\right),\]</span></p><p><span class="math inline">\(Z\)</span> 是一个归一化因子，确保 <spanclass="math inline">\(P\left(\mathbf{S}_{1:t}|\mathbf{O}_{1:t}\right)\)</span>是一个概率分布。</p></li><li><p>很难通过一个通用的标准，将一个MOT方法分类到一个类别中。因此，对MOT进行分类有多个标准。这里有三个标准：1)初始化方法；2)处理模式；3)输出的类型。</p><ul><li><strong>初始化</strong>：大多数的MOT可以分为两类：依赖于目标是如何初始化的：<strong>Detection-basedTracking(DBT)</strong> 和 <strong>Detection-FreeTracking(DFT)</strong>。<ul><li><strong>Detection-BasedTracking</strong>：首先检测到目标，然后连接成轨迹。这个方法通常也称为“tracking-by-detection"。**给定一个序列，在每一帧上进行目标检测或者运动检测，来得到objecthypotheses，然后进行tracking，将detectionhypotheses连接成轨迹。这里有两个问题：1）目标检测器是提前训练了，大多数的DBT方法聚焦于特定类型的目标，例如行人。2)DBT的性能依赖于采用的目标检测器的性能。</li><li><strong>Detection-FreeTracking</strong>：DFT方法需要人为的在第一帧中初始化固定数量的目标，然后在后面的帧中定位这些目标。DBT更受欢迎，因为这些新的目标发现了，消失的目标终止了。DFT不能处理目标消失的情况。然而，它不许哟啊训练好的目标检测器。</li></ul></li><li><strong>处理模式</strong>：MOT方法可以进一步分为<strong>在线</strong>和<strong>离线</strong>。区别在于当处理当前帧的时候，来自futureframes的观测是否被利用。在线方法，也成为因果,tracking，仅依赖于过去的信息；离线方法，batchtracking，能利用所有的信息。<ul><li><strong>online tracking</strong>：在线方法中，图像序列是step-wisehandled manner。</li><li><strong>offlinetracking</strong>：离线方法中，用一个batch的frames来处理数据。从所有帧中得到的观测，一起用来估计最后的输出。注意到，由于计算和存储的限制，通常不能一次处理所有帧；另外一个方式是将数据分成更短的videoclips，对每个batch进行推理。</li></ul></li><li><strong>输出类型</strong>：将MOT方法分为确定性的和概率性的标准，依赖于输出的随机性。两类方法的不同主要来自于优化方法。<ul><li><strong>stochastic tracking</strong>：stochastictracking的输出结果因时间不同而不同。<strong>例如，在DFT的中，如果用粒子滤波进行推理，bboxes的结果是不同的；不同的结果来自于处理过程中的粒子产生的随机性。即使在DBT的中，一些研究也用SOT来对检测框进行修正，这种类型的方法在不同的运行时间导致不同的追踪结果</strong>。</li><li><strong>DeterministicTracking</strong>：当多次运行的时候，确定性跟踪的输出是不变的，<strong>例如，在tracking-by-detection中，用匈牙利算法进行数据关联会产生确定性的追踪结果，确定性的跟踪通常和用于推导最终结果的确定性优化想关联</strong>。</li></ul></li><li><strong>讨论</strong>：DBT和DFT之间的不同之处是在于是否采用了检测模型。区分在线和离线追踪的关键是处理观测的方式。</li></ul></li><li><p>MOT的组件：在这个部分，MOT的目标是发现每一帧中的多个目标，通过连续帧恢复id信息。开发MOT方法的时候，两个主要的问题：一个是如何测量帧中的目标的相似性；另一个问题是如何基于跨帧对象之间的相似性度量来恢复身份信息。第一个问题涉及外观、运动、交互、exlusion和occlusion的建模；第二个涉及推理的问题。</p><ul><li><strong>外观模型</strong>：外观在MOT中是一个重要的用于匹配计算的线索。然而，不同于SOT(主要关注于构建复杂的外观模型，来区分背景和目标)，<strong>大多数的MOT方法，不考虑外观模型作为核心的组件</strong>。外观模型通常包含两个组件：<font color=red>视觉表征(visualrepresentation)</font>和<font color=red>统计测量(statisticalmeasuring)</font>。视觉表征用一些特征描述目标的视觉特性；或是singlecue或是multiplecues。统计测量，是不同观测之间的相似度计算。两个观测i和j之间的相似度如下：</li></ul><p><span class="math inline">\(O_i\)</span> 和 <spanclass="math inline">\(O_j\)</span> 是不同观测的视觉表征，<spanclass="math inline">\(F(.,.)\)</span> 是一个函数，计算它们之间的相似度。-<strong>视觉表征</strong>：根据不同类型的特征来描述一个目标，将特征分为以下几类：-<strong>局部特征</strong>：KLT是一个搜索好的局部特征和tracking的粒子。用在了SOT和MOT中。如果将图像像素视为finestlocalrange的话，光流也可以视为局部特征。一些MOT的思路是在数据关联之前用光流将detection连接成shorttracklets。因为它和运动相关联，因此用来编码运动信息。光流的一个应用是发现拥挤场景下的crowdmotion patterns，这种情况下一般的特征不可靠。 - <strong>RegionFeatures</strong>：相比于local features，region features是从widerrange中得到的(例如bbox)：有三种类型：1)zero-order类型;2)first-order类型;3)up-to-second-order类型。这里的order是指计算表征的时候的orderofdiscrepancy。例如：zero-order一位置像素值没有进行比较，one-order意味着计算了一次像素之间的差异。<font color=red>zero-order</font>：这是用的最多的MOT的表征。颜色直方图和rawpixeltemplate是两种典型的例子。<font color=red>first-order</font>：基于梯度的表征例如HOG和level-setformulation用的比较多。<font color=red>up-to-second-order</font>：Regioncovariance matrix，计算了up-to-second order discrepancy。 -<strong>others</strong>：除了local和regionfeatures，也有一些其他类型的表征，例如depth，用来修正检测假设。ProbabilisticOccupancy Map(POM)用来估计一个目标有多大的可能出现在一个gridcell中。另一个例子是gaitfeature，对每个人是唯一的。DCNN扮演者类似bag-of-words中的密码本的角色。ColorNames描述器用在了外观表征上。来自CNN的特征用在了视觉表征上。还有用点云的特征，用于特征融合。-<strong>Discussion</strong>：通常，颜色直方图是研究得很多的相似度测量，但是它忽略了目标区域的spatiallayout。局部特征是高效的，但是对遮挡、out-of-planerotation是敏感的，基于梯度的特征例如HOG，能够描述目标的外形，对于一些变换也很鲁棒，但是它们不能处理遮挡和形变。regioncovariancematrix特征更加鲁棒，因为它们考虑了更多的信息，但是需要更多的计算。depthfeatures使得匹配计算更加精确，但是它们需要多视角或者额外的算法来得到。 -<strong>统计测量</strong>：这个步骤和上面紧密相连。基于视觉表征，统计测量计算两个观测之间的相似度。一些方法只依赖于一种类型的cue，其他的依赖于多个cues。- <strong>single cue</strong>：用singlecue进行外观建模要么将距离转换成相似度，要么直接计算affinity。例如NormalizedCross Correlation(NCC)经常用于计算基于raw pixeltemplate的表征的两个counterparts之间的affinity。说到颜色直方图，Bhattacharyya距离B(.,.)用于计算连个直方图之间的距离，这个距离转换成相似度S，或者将距离拟合到高斯分布。非相似度转换到likelihood，也用在了covariancematrix表征上。来自神经网络的深度特征之间的余弦相似度也被使用过。除了这些典型的模型，bag-of-words模型用在了基于pointfeatures representation上。 - <strong>multiplecues</strong>：不同类型的cues能够互补，使得外观模型更加鲁棒。然而，如何融合这些cues的信息很重要，基于此，根据5中不同类型的融合策略，总结了基于外观模型的multi-cue：<strong>Boosting,Concatenating, Summation, Product, Cascading</strong>。 -<strong>Boosting</strong>：Boosting通过基于boosting的算法，从featurepool中选择一定的features。例如，从颜色直方图中，HOG和covariance matrixdescriptor, AdaBoost,RealBoost和一个HybridBoost算法分别用来选择最具有表征的features将同一目标的轨迹和其他目标的区分开。-<strong>Concatenation</strong>：不同类型的特征通过concatenated用于计算，color,HOG,光流等进行concatenation，用于外观建模。- <strong>Summation</strong>：将不同特征的affinity values进行加权计算。- <strong>Product</strong>：将不同特征的affinityvalues进行乘积计算，得到integratedaffinity。注意到，当用这个策略的时候，通常进行独立假设。 -<strong>Cascading</strong>：用不同类型的视觉表征，用cascade的方式，要么narrow the searchspace，要么对外观以coarse-to-fine的方式进行建模。</p><ul><li><strong>运动模型</strong>：运动模型得到目标的动态行为，在futureframes中估计目标的潜在位置，因此降低了搜索空间，在大多数情况下，目标假设movesmoothly，因此在图像空间中。讨论线性运动模型和非线性运动模型。<ul><li><strong>线性运动模型</strong>：这是用的最多的模型。<strong>模型假设速度恒定</strong>。基于这个假设，有3种不同的方式构建模型：<ul><li><p>velocitysmoothness：目标在连续帧中的速度变化是smoothly。</p></li><li><p>Position smoothness：<strong>直接force观测的位置和估计的位置discrepancy。将估计的位置拟合到以观测的位置为中心的高斯分布中</strong>。在估计的时候，forwardmotion和backward motion都考虑了，公式如下：</p><p>F和B意味着forward 和backbward方向，观测的位置和估计的位置之间的位移<span class="math inline">\(\Delta p\)</span>拟合到zero-center的高斯分布中。</p></li><li><p>Accelerationsmoothness：除了考虑位置和速度smoothness，加速度也考虑了。在时刻k的一个状态的运动的概率分布如下：</p></li></ul></li><li><strong>非线性运动模型</strong>：线性运动模型通过用来解释目标的dynamics。然而，也有一些线性模型无法处理的情况。非线性模型用来产生不同tracklets之间的更精确的motionaffinity。例如，有人用了非线性运动模型来处理targets may movefreely的情况。给定属于同一个目标的两个tracklets <spanclass="math inline">\(T_1\)</span> 和 <spanclass="math inline">\(T_2\)</span>，线性运动模型会产生比较低的概率来连接它们。alternatively，用一个非线性运动模型，tracklet<span class="math inline">\(T_1\)</span> 的尾巴和tracklet <spanclass="math inline">\(T_2\)</span>的头之间的gap可以合理地被tracklet<span class="math inline">\(T_0\)</span>解释，如图所示，<spanclass="math inline">\(T_0\)</span> 匹配 <spanclass="math inline">\(T_1\)</span>的尾巴和<spanclass="math inline">\(T_2\)</span>的头。这个连接 <spanclass="math inline">\(T_1\)</span> 和 <spanclass="math inline">\(T_2\)</span> 之间的真实Path是通过基于 <spanclass="math inline">\(T_0\)</span> 估计得到的。<spanclass="math inline">\(T_1\)</span> 和 <spanclass="math inline">\(T_2\)</span> 之间的affinity的计算和之前的相似。</li></ul></li><li><strong>interaction model</strong>：交互模型，也称之为mutual motionmodel，捕获一个目标对另一个目标的影响。在拥挤的场合，一个目标可能经历一些来自其他目标的"force"。例如，当行人在街上走的时候，他会调整他的速度、方向和目的地，为了避免和其他相碰。另一个例子是：当一群人在街上走的时候，每个人都同时跟着他人，实际上，有两个典型的交互模型：<strong>socialforce model</strong> 和 <strong>crowd motion pattern models</strong>。<ul><li><p><strong>Social Force Models</strong>：socialforce模型也称之为groupmodels。在这些模型当中，每个物体都被认为和其他物体和环境因素有依赖。这种类型的信息可以缓解拥挤场景下的性能下降。在socialforce模型中，目标被视为agents，基于其他物体和环境，决定它们的速度、加速度和目的地。在socialforce 模型中，目标行为基于<em>individual force</em> 和<em>groupforce</em>进行建模。<strong>Individualforce</strong>：对于多个目标中的每个个体，两种类型的forces被考虑：fidelity(不会改变目的地)；constancy(不会突然改变动量/速度/方向)。<strong>Groupforce</strong>：三种类型的forces被考虑：attraction(个体一起移动，group靠的近)；repulsion(个体会一起移动，当group和其他保持距离的时候)；coherence(个体和group移动的时候保持相似的速度)。目前主要的对物体和social force进行建模的方法是对一个<strong>energyobjective</strong>最小化，包含反应个体force和group force的项。</p></li><li><p><strong>Crowd Motion Pattern Models</strong>：受crowd simulationliterature的启发，引入了motionpatterns来缓解拥挤场景下追踪个体目标的困难，通常情况下，这个类型的模型用在了over-crowded场景，这种情况下，目标相当小，cues例如外观和个体运动是模糊的。这种情况下，来自crowd的motion是一个比较可靠的cuefor this problem。</p><p>粗略地说，这里有两种类型的motion patterns，<em>structured</em> 和<em>unstructured</em> 两种，structured motion patterns展示了collective时空结构，unstructured motionpatterns展示了多样的motion的模态，通常情况下，motionpatterns通过多种方式进行学习(包括ND tensor voting,隐马尔科夫模型等)，用一些先验知识来帮助目标跟踪。</p></li></ul></li><li><strong>Exclusionmodel</strong>：Exclusion是constraint，来避免physicalcollisions。他来自于这样一个事实：两个不同的物体不能占据真实世界中的相同的物理空间。给定多个detectionresponses和多个轨迹假设，通常有两个constraints：一个称之为<em>detection-levelexclusion</em>，例如同一帧中的两个不同的detectionresponses不能给到同一个物体；第二个称之为<em>trajectory-levelexclusion</em>，例如两个轨迹不能无限接近。<ul><li><em>detection-level exclusionmodeling</em>：不同的方法用来对detection-levelexclusion进行建模，这里有soft和hard models。<strong>Softmodeling</strong>：通过最小化costterm惩罚violation的情况，例如如果两个同时发生的detectionresponses被分配给相同的轨迹，它们之间的距离也足够远，则定义一个penalty。为了建模exclusion，构建一个exclusiongraph，来得到constraint，给定所有的detection，定义一个graph，其中nodes表示detection，每个node之连接到和同时存在的node。在构建完这个graph之后，最大化labelassignment，使连接的nodes有不同的labels。<strong>Hardmodeling</strong>，通过用explicit constraint来对detection-levelexclusion进行hard modeling。例如，为了建模detection-levelexclusion，如果两个tracklets在它们的timespan上有重叠，它们不能分配到同一个cluster，属于同一个trajectory。用non-negativediscretization来将检测设置为非重叠的组，服从互斥约束。</li><li><em>trajectory-level exclusion modeling</em>：trajectory-levelexclusion通过惩罚两个相近的、有不同的trajectorylabels的detection进行建模，这会抑制其中一个轨迹label。例如，惩罚项和两个detectionresponses之间的距离成反比。如果两个detectionresponses太近了，它会导致相当大的cost；也有方法，它的penalty和两个轨迹的时空overlap成正比。两个轨迹越近，惩罚越大。</li></ul></li><li><strong>occlusionhandling</strong>：遮挡或许是MOT中最重要的挑战，它是ID切换的主要原因<ul><li><em>part-to-whole</em>：这个策略使建立在当遮挡发生的时候，物体的一部分还可见的时候，这个假设在大多数情况下成立。基于这个假设，采用这个策略的方法利用可见的部分推测整个物体的状态。流行的方式是将整个物体划分为多个parts，基于individualparts计算affinity。如果遮挡发生了，遮挡部分的affinities会比较低。Tracker会意识到这个，然后采用没有遮挡部分用于估计。基于这些individualparts，遮挡部分的观测被忽略了。例如，part-wise外观模型被构建，重建损失用来决定哪些parts被遮挡还是没有。整个物体的外观模型通过仅更新未这遮挡的部分进行更新。part-to-whole策略也用于基于featurepoint clustering的追踪。假设有相似motion的featurepoints属于同一个object。只要一个object的一些parts是可见的，clustering offeature point trajectories会work。</li><li><em>hypothesize-and-test</em>：这个策略通过假设proposals和testingproposals according to observations athand，回避了遮挡的挑战。这个策略有两个步骤：hypothesize和test。<strong>Hypothesize</strong>：张等人基于occludablepair of observations，产生occlusionhypotheses，它们相近且有相似的scale。假设 <spanclass="math inline">\(o_i\)</span>被 <spanclass="math inline">\(o_j\)</span>遮挡，一个correspondingocclusion假设是，<span class="math inline">\(p_j\)</span> 和 <spanclass="math inline">\(t_j\)</span> 是 <spanclass="math inline">\(o_j\)</span>的位置和时间戳。 <spanclass="math inline">\(s_i\)</span>和 <spanclass="math inline">\(f_i\)</span> 是 <spanclass="math inline">\(o_i\)</span>的尺寸和外观特征。这个方法将遮挡视为distraction，在其他的工作中，occlusionpatterns被用来辅助检测。不同的detectionhypotheses通过结合不同level和patterns ofocclusion来生成。<strong>Test</strong>：假设的观测和原始的一起给到cost-flowframework作为输入，执行MAP来得到最优的方案。一个multi-person检测器基于detectionhypotheses进行训练，这个检测器降低了检测的困难。</li><li><em>buffer-and-recover</em>：这个策略会缓存observations，当遮挡发生的时候，记住遮挡发生之前的状态。当遮挡结束的时候，基于遮挡之前的缓存的observation和状态，恢复objectstates。有人提出了当遮挡发生的时候，keep trajectory alive for up to 15frames。通过遮挡推断出休眠轨迹的位置。以防物体再次出现，再次触发track，保持identity。只要得到了足够的observations，产生hypotheses来解释observations</li><li><em>others</em>：上述的策略可能不能覆盖所有的策略。例如，有人将targets表示为图像空间中的高斯分布，显式地对所有目标之间的pair-wiseocclusion ratios进行建模。</li></ul></li><li><strong>推理</strong>：<ul><li>概率推理：<strong>基于概率推理的方法将目标的状态表示为不确定的分布。跟踪算法的目标是估计目标状态的概率分布，通过基于已有的观测的多种概率推理方式</strong>。这种类型的方法需要theexisting，例如过去和现在的观测。很自然地将马尔可夫假设用在目标状态序列上。这个假设包括两个方面。首先，当前的<strong>目标状态仅依赖于之前的状态</strong>；如果一阶马尔可夫特性应用了的话，仅依赖于最近的状态；另外，目标的观测<strong>只和对应于这个观测的状态</strong>有关。换句话说：观测是条件独立的。这两个方面分别和&lt;fontcolor=red dynamic model</font>和&lt;font color=red observationmodel</font>有关。<strong>dynamicmodel对应于跟踪策略，观测model提供关于目标状态的观测measurements</strong>。<strong>预测阶段是基于过去所有的观测对当前状态的估计，更具体地，当前状态的后验概率分布是通过dynamicmodel在最后一个目标空间里进行集成，进行估计的。更新阶段是基于观测模型得到的测量，来更新状态的后验分布</strong>。根据公式，目标的状态可以通过iteratively执行预测和更新阶段来估计。然而，在实际中，不简化假设的时候，目标的状态分布无法表示。因此计算状态分布的积分没有解析解。另外，对于多目标，状态的集合的维度是很大的，使得integration比较困难，需要对近似解进行推导。</li></ul>有很多的概率推理模型用于多目标跟踪，例如KF,EKF和粒子滤波。 -卡尔曼滤波器：在线性系统和高斯分布的目标状态下，KF被证明是最优的估计器；-扩展卡尔曼滤波器：未了包含非线性情况，扩展卡尔曼滤波器是一个可能的方式，通过泰勒展开近似一个非线性系统。-粒子滤波：基于蒙特卡洛采样的模型在跟踪中也很流行，这个策略通过一组加权的particles，对潜在的分布进行建模<ul><li>deterministic优化：<strong>不同于概率推理模型，基于确定性优化的方法旨在找到MOT的最大后验方法</strong>，为了这个目的，推理数据关联、目标状态的任务被视为一个优化问题。这个框架下的方法更适合离线跟踪，因为所有帧的观测都能得到。给定所有帧的观测，这些方法能够将属于通过一个目标的观测进行全局关联成轨迹。这个关键的问题是找到最优的关联。有以下方法：<ul><li><strong>Bipartite graphmatching</strong>：通过将MOT问题建模为bipartite graphmatching，两个不相交的graphnodes可以是在线跟踪中的现有的轨迹和新的检测，或者离线跟踪中的两组tracklets。nodes之间的权重被建模为轨迹和检测之间的affinity。然后用贪心的bipartite分配算法或者匈牙利算法来决定两组nodes之间的匹配。</li><li><strong>动态规划</strong>：扩展动态规划、线性规划等用来解决detections或tracklets之间的关联问题。</li><li><strong>Min-cost max-flow network flow</strong>：networkflow是一个directedgraph，每个边都有一个确定的容量。对于MOT，图中的nodes是detectionresponses或者tracklets，flow被建模为一个indicator，来连接两个nodes。为了满足flowbalance要求，一个源节点，sink节点(汇聚节点)对应于轨迹的开始和结束。在一个graph中，一个轨迹对应于一个flowgraph，从源节点到汇聚节点的总的flow等于轨迹的数量；transition的成本是关联假设的负log-likelihood。</li><li><strong>conditional random field</strong>：顶一个graphG=(V,E)，V是node的集合，E是边的集合。low-leveltracklets作为graph的输入，图中的每个node表示观测或者pairs oftracklets，预测label，表示观测属于哪个track或者是否连接tracklets。</li><li><strong>MWIS</strong>：最大权重独立set是heaviest subset ofnon-adjacentnodes.如上所述的CRF模型，属性图中的节点表示连续帧中的轨迹对，节点的权重表示轨迹对的关联度，如果两个轨迹共享相同的检测，则边缘连接。给定此图，将数据关联建模为MWIS问题</li></ul></li></ul></li></ul></li></ol><p><imgsrc="https://figures.semanticscholar.org/3dffacda086689c1bcb01a8dad4557a4e92b8205/10-Figure5-1.png"alt="MOT Components" /> $Fig.3^{[3]} Components of MOT</p><p><imgsrc="https://figures.semanticscholar.org/3dffacda086689c1bcb01a8dad4557a4e92b8205/11-Figure6-1.png"alt="Examplar features" /> <spanclass="math inline">\(Fig.4^{[3]}\)</span> (a)Image showing opticalflow;(b)image showing covariance matrix;(c)image showing pointfeature;(d)image showing gradient based features;(e)image showingdepth;(f)image showing color feature.</p>]]></content>
    
    
    <summary type="html">&lt;h3 id=&quot;multi-object-tracking-survey&quot;&gt;Multi-Object Tracking Survey&lt;/h3&gt;
&lt;blockquote&gt;
&lt;p&gt;来自论文:&quot;Deep Learning in Video Multi-Object Tracking: A Survey&quot;,
作者是来自意大利和西班牙等院校的Gioele
Ciaparrone等人。论文引用[1]:Ciaparrone, Gioele et al. “Deep learning in
video multi-object tracking: A survey.” Neurocomputing 381 (2019):
61-88.&lt;/p&gt;
&lt;/blockquote&gt;
&lt;h3 id=&quot;总结&quot;&gt;总结&lt;/h3&gt;
&lt;ol type=&quot;1&quot;&gt;
&lt;li&gt;&lt;p&gt;多目标跟踪的问题是指在一个序列中跟踪不同目标的轨迹，通常是一个视频。近些年随着深度学习的兴起，算法从深度模型的表征能力中收益。本文提供了提供了用深度学习模型来解决MOT问题的全面的综述。&lt;/p&gt;&lt;/li&gt;
&lt;li&gt;&lt;p&gt;MOT问题不同于目标检测(输出一系列的bounding
box)。MOT算法还将target
ID和每个box(detection)进行关联，为了区分类内的目标。&lt;strong&gt;单目标跟踪中，目标的外观是已知的先验，在MOT中，检测这一步对于识别targets是必须的，追踪多个目标的主要困难在于多样的阻塞和目标之间的交互，有时它们还有相似的背景&lt;/strong&gt;。因此，将单目标跟踪应用于多目标跟踪会导致目标漂移和多个ID切换，这样的模型通常在区分相似目标和类内目标的时候有困难。专门针对多目标跟踪的一系列的算法被调整出来，用于解决这个问题，还有数据benchmarks和比赛，来方便不同方法的对比。
最近，很多的算法用深度学习来做，深度学习模型的长处在于学习丰富表征和提取特征。卷积神经网络长处在于空间模式提取，循环神经网络想LSTM用于处理时序数据。&lt;/p&gt;&lt;/li&gt;
&lt;/ol&gt;</summary>
    
    
    
    <category term="Papers" scheme="https://young-eng.github.io/YoungBlogs/categories/Papers/"/>
    
    
    <category term="Multi-Object Tracking" scheme="https://young-eng.github.io/YoungBlogs/tags/Multi-Object-Tracking/"/>
    
  </entry>
  
  <entry>
    <title>ALIGN</title>
    <link href="https://young-eng.github.io/YoungBlogs/2024/12/01/ALIGN/"/>
    <id>https://young-eng.github.io/YoungBlogs/2024/12/01/ALIGN/</id>
    <published>2024-12-01T06:27:30.000Z</published>
    <updated>2024-12-20T06:19:07.578Z</updated>
    
    <content type="html"><![CDATA[<h3id="scaling-up-visual-and-vision-language-representation-learning-with-noisy-text-supervision1">ScalingUp Visual and Vision-Language Representation Learning With Noisy TextSupervision<sup>[1]</sup></h3><blockquote><p>作者是来自Google的Chao Jia, Yinfei Yang等人，论文引用[1]:Jia, Chao etal. “Scaling Up Visual and Vision-Language Representation Learning WithNoisy Text Supervision.” International Conference on Machine Learning(2021).</p></blockquote><h3 id="time">Time</h3><ul><li>2021.Jun</li></ul><h3 id="key-words">Key Words</h3><h3 id="总结">总结</h3><ol type="1"><li>预训练表征对于很多NLP和感知任务很重要，当NLP中的表征学习不需要人类标注的时候，迁移到rawtext上训练的时候，视觉和视觉语言表征仍然依然严重依赖于精心准备的训练集，这成本很高而且需要专家知识。对于视觉应用，通过很多有显示label的数据集例如ImageNet或者OpenImages来学习表征。<strong>对于视觉语言，流行的数据集例如ConceptualCaptions, MSCOCO或者CLIP，都涉及non-trivial数据收集的过程。这个费劲的精心挑选的过程限制了数据集的规模，阻碍了训练模型的scaling</strong>。在这篇文章中，<strong>作者利用一个超过十亿的图像文本对的有噪声的数据集，不需要费劲地过滤或者后处理步骤。一个简单地双编码器结构用对比损失，学习对齐图像文本对的视觉语言表征</strong>。展示了语料库的规模可以弥补其噪声，即使一个简单的学习策略，就能实现SOTA的表征。作者的视觉表征当迁移到例如ImageNet这样的分类任务时，实现了强大的性能。对齐的视觉语言表征enablezero-shot的图像分类和在数据集Flickr30K上达到SOTA，即使和更复杂的交叉注意力的模型相比，这些表征使能用复杂的文本和图像文本queries实现跨模态的搜索。</li></ol><span id="more"></span><ol start="2" type="1"><li><p>在现有文献中，视觉和视觉语言表征学习用不同的数据源研究了很多。在视觉域，在大规模监督数据上例如ImageNet进行预训练，证明了通过迁移学习在下游任务上提高性能是重要的。这类预训练数据集的构建需要大量的数据收集、采样和人类标注工作，很难scale。预训练在视觉语言模型中已经是事实上的路径。然而，视觉语言预训练数据集例如ConceptualCaptions等需要很多的人类标准、语义解析、清洗和平衡的工作。因此，这些数据集的规模只有10M左右。这至少比视觉领域中对应的数据集小一个数量级。在视觉域中，并且比来自互联网的用于NLP预训练的大型文本语料库要小得多。</p><p>在这个工作中，作者利用超过十亿的、有噪声的图像文本对来扩充视觉和视觉语言表征学习。<strong>跟着ConceptualCaptions数据集描述的步骤来构建一个有噪声的数据集，不是用复杂的过滤和后处理步骤来清洗数据集，只用了简单的基于频率的清洗。最后的数据集是有噪声的，但是比ConceptualCaptions数据集大两个量级。作者展示了在百万兆级的数据集上预训练的视觉和视觉语言表征在多个任务上实现了很强的性能</strong>。</p><p>为了训练模型，用一个简单的dual-encoder架构，在一个共享的隐嵌入空间，用一个目标函数对齐视觉和语言表征。类似的目标函数已经用在了学习视觉-语义嵌入(visual-semanticembeddings,VSE)中，作者称自己的模型为<strong>ALIGN</strong>：<strong>A</strong><strong>L</strong>arge-scale <strong>I</strong>ma<strong>G</strong>e and<strong>N</strong>oisy-textembedding。<strong>图像和文本编码器通过对比损失（以归一化的 softmax形式表示）进行学习，该损失函数将匹配的图像-文本对的嵌入推近，同时将不匹配的图像-文本对的嵌入推远。这是自监督和监督表征学习中最有效的损失函数之一。考虑到成对的texts作为图像的fine-grainedlabels，作者的图像文本对比损失类似于传统的基于label的分类目标函数，主要的不同在于testencoder产生label的权重</strong>。</p><p>对齐的图像和文本表征适用于跨模态的匹配任务，在对应的benchmarks上实现了SOTA。这样的跨模态匹配当把classnames输入到textencoder中的时候，自然地使得zero-shot图像分类可行。</p></li><li><p>高质量的用于分类或者检索的视觉表征通常在大规模的标签的数据集上进行预训练，最近，自监督和半监督学习作为另外的范式被研究了很多，然而，通过这些方法训练的模型目前限制了迁移到下游任务上的能力。利用图像和自然语言captions是另一个学习视觉表征的方向。通过从图像中预测captions，Joulin等人的工作展示了很好的视觉表征。这些工作然而受限于小的数据集，导致模型不能产生能够用于像跨模态检索这样的视觉语言表征。在视觉语言表征学习域中，视觉语义嵌入和改进版本(利用目标检测器，密集特征图或者多层注意力)被提出了。最近更多的、有跨模态的注意力层的模型出现了，在图像文本匹配任务上展示了很好的性能。然而，它们的速度要慢几个数量级，因此对于现实世界中的图像-文本检索系统来说是不切实际的，相比之下，作者的模型继承了简单地VSE形式，仍然在图像文本匹配benchmarks上超过了之前所有的交叉注意力的模型。</p><p>和作者的工作相近的是CLIP，用一个类似的对比学习，通过自然语言监督提出了视觉表征学习。除了用不同的视觉和语言编码器架构，主要的不同在于训练数据：<strong>ALIGN遵循原始 alt-text 数据中图像-文本对的自然分布，而 CLIP则通过首先从英语维基百科构建一个高频视觉概念的允许列表来收集数据集。作者展示了视觉和视觉语言表征可以通过不需要专家知识来组织的数据集进行学习</strong>。</p></li><li><p>大规模的有噪声的图像-文本数据集：作者工作的焦点是扩展视觉和视觉语言表征学习。对于这个目的，作者使用比现有数据集大得多的数据集。特别地，遵循构建conceptualcaptions 数据集的方法，得到原始英语alt-text数据的版本。ConceptualCaptions数据集通过大量的过滤和后处理进行清洗。这里，出于scaling的目的，通过放宽原始工作中大部分的清洗的步骤，以质量换规模。只应用最小的基于频率的滤波。结果是一个更大的但是更有噪声的数据集。</p></li><li><p><strong>基于图像的过滤</strong>：跟着Sharma等人的工作，去掉了porngraphic图像，只保留shorterdimension大于200像素的、aspectratio小于3的图像。和超过1000个alt-text关联的图像被丢弃。为了确保没有在测试图像上进行训练，作者还删除了所有下游评估数据集中重复或接近重复的测试图像。</p></li><li><p><strong>基于文本的过滤</strong>：排除了超过10张图像共享的alt-texts，这些alt-texts通常和图像的内容不相关。也丢弃了包含任何稀有token的alt-texts，还有那些太短或者太长的。这个去掉了有噪声的文本或者太一般很难有用的文本。</p></li><li><p><strong>预训练和任务迁移</strong>：在有噪声的图像文本对上进行预训练。用一个dual-encoder架构预训练ALIGN。这个模型包含一对图像和文本编码器，带有余弦相似度的结合的函数。用带有globalpooling的EfficientNet作为image encoder和带有CLS token嵌入的BERT作为textembedding encoder。一个带有线性激活的全连接层加在了BERTencoder的顶部，来匹配来自imagetower的维度。图像和文本编码器都是从零开始训练。图像和文本编码器通过normalized softmaxloss进行优化。在训练阶段，将匹配的图像文本对作为positive，所有其它的在训练batch中形成的随机图像文本对视为negative。我们最小化两个loss：一个用于image-to-text分类：</p><p>另一个用于text-to-image分类：</p><p>这里，<span class="math inline">\(x_i\)</span> 和 <spanclass="math inline">\(y_i\)</span>是第i个pair的图像和第j个pair文本的normalized embedding。N是batchsize，<span class="math inline">\(\theta\)</span> 是temperature来scalelogits。为了让in-batch negatives更有效，从所有计算的分数中concatembeddings，来形成更大的batch。这个温度变量对于很重要，因为图像和文本嵌入都是L2正则化。不同于手动地扫描最优温度值，和其它参数一起学习更有效。</p></li><li><p>作者提出了一个简单地方法，利用大规模的、有噪声的图像文本对数据，来扩展视觉和视觉语言表征学习。作者的方法避免了在数据组织和标注上的大量工作，只需要小部分的基于频率的学习。在这个数据集上，用对比损失训练一个简单地dual-encoder的模型。这个模型称之为ALIGN，能够实现跨模态检索。在只有视觉的下游任务上，ALIGN也是comparable或者超过了用大规模标签数据训练的SOTA模型。</p></li></ol><p><imgsrc="https://figures.semanticscholar.org/141a5033d9994242b18bb3b217e79582f1ee9306/2-Figure1-1.png"alt="方法总结" /> <span class="math inline">\(Fig.1^{[1]}\)</span>ALIGN，视觉和语言表征，通过有噪声的imagealt-text数据进行学习。这个表征可以用于只有视觉或者视觉语言任务迁移。没有任何微调，ALIGN赋能zero-shot的视觉分类和跨模态的搜索，包括image-to-text搜索。</p>]]></content>
    
    
    <summary type="html">&lt;h3
id=&quot;scaling-up-visual-and-vision-language-representation-learning-with-noisy-text-supervision1&quot;&gt;Scaling
Up Visual and Vision-Language Representation Learning With Noisy Text
Supervision&lt;sup&gt;[1]&lt;/sup&gt;&lt;/h3&gt;
&lt;blockquote&gt;
&lt;p&gt;作者是来自Google的Chao Jia, Yinfei Yang等人，论文引用[1]:Jia, Chao et
al. “Scaling Up Visual and Vision-Language Representation Learning With
Noisy Text Supervision.” International Conference on Machine Learning
(2021).&lt;/p&gt;
&lt;/blockquote&gt;
&lt;h3 id=&quot;time&quot;&gt;Time&lt;/h3&gt;
&lt;ul&gt;
&lt;li&gt;2021.Jun&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id=&quot;key-words&quot;&gt;Key Words&lt;/h3&gt;
&lt;h3 id=&quot;总结&quot;&gt;总结&lt;/h3&gt;
&lt;ol type=&quot;1&quot;&gt;
&lt;li&gt;预训练表征对于很多NLP和感知任务很重要，当NLP中的表征学习不需要人类标注的时候，迁移到raw
text上训练的时候，视觉和视觉语言表征仍然依然严重依赖于精心准备的训练集，这成本很高而且需要专家知识。对于视觉应用，通过很多有显示label的数据集例如ImageNet或者OpenImages来学习表征。&lt;strong&gt;对于视觉语言，流行的数据集例如Conceptual
Captions, MSCOCO或者CLIP，都涉及non-trivial
数据收集的过程。这个费劲的精心挑选的过程限制了数据集的规模，阻碍了训练模型的scaling&lt;/strong&gt;。在这篇文章中，&lt;strong&gt;作者利用一个超过十亿的图像文本对的有噪声的数据集，不需要费劲地过滤或者后处理步骤。一个简单地双编码器结构用对比损失，学习对齐图像文本对的视觉语言表征&lt;/strong&gt;。
展示了语料库的规模可以弥补其噪声，即使一个简单的学习策略，就能实现SOTA的表征。作者的视觉表征当迁移到例如ImageNet这样的分类任务时，实现了强大的性能。对齐的视觉语言表征enable
zero-shot的图像分类和在数据集Flickr30K上达到SOTA，即使和更复杂的交叉注意力的模型相比，这些表征使能用复杂的文本和图像文本queries实现跨模态的搜索。&lt;/li&gt;
&lt;/ol&gt;</summary>
    
    
    
    <category term="Papers" scheme="https://young-eng.github.io/YoungBlogs/categories/Papers/"/>
    
    
    <category term="multi-modal" scheme="https://young-eng.github.io/YoungBlogs/tags/multi-modal/"/>
    
  </entry>
  
  <entry>
    <title>DetCLIPv3</title>
    <link href="https://young-eng.github.io/YoungBlogs/2024/11/28/DetCLIPv3/"/>
    <id>https://young-eng.github.io/YoungBlogs/2024/11/28/DetCLIPv3/</id>
    <published>2024-11-28T14:31:57.000Z</published>
    <updated>2024-11-28T14:31:57.410Z</updated>
    
    <content type="html"><![CDATA[]]></content>
    
    
      
      
    <summary type="html">
</summary>
      
    
    
    
    
  </entry>
  
  <entry>
    <title>DetCLIP</title>
    <link href="https://young-eng.github.io/YoungBlogs/2024/11/28/DetCLIP/"/>
    <id>https://young-eng.github.io/YoungBlogs/2024/11/28/DetCLIP/</id>
    <published>2024-11-28T14:31:53.000Z</published>
    <updated>2024-11-28T14:31:53.548Z</updated>
    
    <content type="html"><![CDATA[]]></content>
    
    
      
      
    <summary type="html">
</summary>
      
    
    
    
    
  </entry>
  
  <entry>
    <title>F-VLM</title>
    <link href="https://young-eng.github.io/YoungBlogs/2024/11/28/F-VLM/"/>
    <id>https://young-eng.github.io/YoungBlogs/2024/11/28/F-VLM/</id>
    <published>2024-11-28T14:31:28.000Z</published>
    <updated>2024-11-28T14:31:28.653Z</updated>
    
    <content type="html"><![CDATA[]]></content>
    
    
      
      
    <summary type="html">
</summary>
      
    
    
    
    
  </entry>
  
  <entry>
    <title>openMixer</title>
    <link href="https://young-eng.github.io/YoungBlogs/2024/11/28/openMixer/"/>
    <id>https://young-eng.github.io/YoungBlogs/2024/11/28/openMixer/</id>
    <published>2024-11-28T14:25:59.000Z</published>
    <updated>2024-11-30T02:02:01.895Z</updated>
    
    <content type="html"><![CDATA[<h3id="exploiting-vlm-localizability-and-semantics-for-open-vocabulary-action-detection1">ExploitingVLM Localizability and Semantics for Open Vocabulary ActionDetection<sup>[1]</sup></h3><blockquote><p>作者是来自密西根州立大学，NEC和罗格斯大学的Wentao Bao, Kai Li, YuxiaoChen等人。论文引用[1]:</p></blockquote><h3 id="time">Time</h3><ul><li>2024.Nov</li></ul><h3 id="key-words">Key Words</h3><ul><li>vision-language models within the family of query-based DETR</li><li>spatial and temporal OpenMixer Blocks</li></ul><h3 id="总结">总结</h3><ol type="1"><li>ActionDetection旨在检测视频里的空间和时间上的动作，现有的方式聚焦于闭集检测，actiondetector是训练和测试都是在一个固定的行为类比的集合上。然而，这个限制的设定在一个开放的世界中不可行，测试的视频超过了训练的行为类别，本文中，解决这个挑战性的开集行为检测问题。旨在检测测试视频中的任意行为，然而在一个固定的行为类别上训练模型。为了实现这样一个开集的能力，提出了<strong>OpenMixer</strong>的方法，利用内在的语义和大的视觉语言模型的定位能力within the family of query-based detectionmodels(DETR)。特别地，OpenMixer是通过时空上的OpenMixer Blocks(S-OMB andT-OMB)，一个动态的融合模块。这三个模块一起用来预训练VLMs的强大的表征能力和来自DETR设计的端到端的学习。更多的，在多个设定下，建立了OVADbenchmarks，实验结果表明，OpenMixer在检测见过和没见过的行为上，都超过了基线方法。</li></ol><span id="more"></span><ol start="2" type="1"><li><p>行为检测旨在在时空上定位视频里的actors。在视频监控等应用上很重要，引起了很多的关注。现有的AD方法主要是在闭集上，模型训练和测试视频都是在同一个固定的行为类别上。然而，最近取得了很大的进步，从同一个行为类别中进行训练和测试的假设限制了它们应用到实际真实世界中的能力,where测试视频包含了预定义训练类别之外的行为。例如，视频监控可能能够检测大家，但是其它的危险行为例如射击和追逐不能检测到，如果这个系统没有用标注的视频进行训练，另外，在开放世界中检测行为促进了整个的视频理解，开启了高阶视频理解的大门，像是reasoning，forecasting等，这通常要求检测视频中的多个行为。这个促使我们研究开集行为检测，针对检测视频中的任意行为，包括训练集中见过的行为和训练集中没有见过的行为，然而，OVAD时挑战的，因为它要求逐帧的理解行为动态，然而，运动动态建模已经被传统的闭集行为检测研究的很多了，利用训练的全监督，在开集设定中仍然是有挑战的，因为对于没有见过的类别，这里没有监督信息。</p><p>最近，预训练VLMs有很强大的泛化能力，很多的开集方法用于图像识别，目标检测和图像分割，然而，这些方法用于图像，没有考虑视频帧的时序动态，另外，图像VLMs例如CLIPare struggling 来捕捉文本中的actionverbs和视频中的人类行为，这不可避免地要求学习时序动态或者进行微调，来识别下游任务中的行为，冒着对于没有见过类别的很差的泛化能力。</p><p>有很少的重要的工作利用VLMs来做开集视频理解，包括行为识别和时序动作定位，然而，对于region-level的行为检测byVLM，在video-level 预训练 和region-level adaptation之间有representationgap，类似于基于图像的开集检测的representationgap问题。对于OVAD任务，这个representationgap来自于整体的预训练中的video-action 对齐和下游region-level子任务，例如region-action alignment和action-relevant personlocalization。representationgap造成的原因来自预训练视频VLM内在的不同的adaptiongoals，例如将VLMs的语义和定位从视频迁移到region，用于两个子任务。</p><p>重新考虑VLM的Transformer的设计，发现，用VLM的语义特征和低估了的定位能力对于OVAD是重要的，首先，将video-level语义迁移到eachregion，提出了<strong>学习a set of region-wisequeries来编码视频的时序动态，通过用预训练的视频级的特征，作为自适应的语义条件</strong>。更新的queries和视频级的特征进一步动态的进行融合，和用于识别的文本语义进行对齐；第二，为了利用视频VLMs的定位能力，用于region-wise定位，学习一系列的queries，从VLM视觉attentionreveal的先前的位置，解码person boxes。</p><p>特别地，开发了一个query-based开集行为检测器，OpemMixer，来检测开集中的任意行为，它适合DETR，基本的思路是通过学习两组queries和对应的解码modules，来解耦行为识别和定位。OpenMixer包含一个空间的OpenMixerBlock(S-OMB)用于定位，一个时间上的OpenMixerBlock(T-OMB)用于捕捉region-level时序动作，和一个动态的融合对齐(DFA)，用于开集行为识别。这个S-OMB通过text-patch交叉注意力，继承了VLMs的定位，T-OMB利用了VLMs的视觉语义特征，来捕捉时序动态，DFA动态地融合预训练的语义，将其融合成可学习的region-levelqueries，用于泛化的识别，最后，模型能够利用VLMs的语义和定位能力，从DETR的pipeline，实现端到端的检测。</p><p>主要的贡献如下：</p><ul><li>构建了开集行为检测的任务，是有价值的</li><li>开发了OpenMixer模型，能够利用预训练视频语言模型的语义和定位能力，面向OVAD任务。</li><li>在多个视频行为检测数据集上，验证了提出的模块的有效性</li></ul></li><li><p>时空行为检测最近的工作都是采用两个backbone的设计，分别提取关键帧和整个视频的特征，用于actor定位和actor-context关系建模。虽然它们通过利用图像和视频backbone，实现了很好的性能，模型的参数比较大，很难优化，最近的DETR的进步，通过单个的backbone就能够是西安端到端的actiondetection，展示了很好的性能。基本的想法是用单个videotransformer来得到所有的视频frames的特征，然后引入可学习的queries，和videofeatures进行mix，用于特别地，WOO用SparseRCNN用于定位，TubeR用经典的DETR来学习actiontubes，STMixer用AdaMixer的设计，实现SOTA的性能。在对actors和actorcontext的interaction进行建模的时候，query-base的设计有益的，同时将整个架构简化为了单阶段的设计。然而，这些方法，没有一个能处理开放世界的没有见过的行为，因此，引入了一个开集行为检测的方法，基于query-based的设计，来检测任意的行为。</p><p>开集视觉理解。<strong>由于VLM强大的对齐能力，没有见过的类别的视觉数据能够通过视觉特征和类别名的文本特征进行对齐，来识别</strong>，这促使了一系列的开集的目标检测、行为识别和时序动作定位的工作。对于定位，最近的基于图像的开集目标检测器OV-DETR和CORA和作者的方法有相同之处，都是讲VLMs的语义注入到可学习的queries。然而，OV-DETR的queryconditions是特定类别的，以至于它们不能适应测试时的样本，CORA两阶段的训练限制了在视频领域的灵活性。对于视频理解，最近的工作和STAN，都是建立在基于图像的CLIP模型，然而，相比于OVTAL任务，提出的OVAD任务更具挑战性，因为它需要在时间和空间上进行识别，iCLIP旨在zero-shot行为检测，没有考虑测试时候的行为。另外，它跳过了利用现有的检测器来定位行为，只学习识别没有见过得到行为，缺乏定位action-relevantpersons的能力，注意到一个同期的工作forOVAD，但这是一个两阶段的设计，依赖于额外的大规模的region-text预训练数据，不许哟啊利用基于视频的VLMs的内在的检测知识。据作者所知，OpenMixer是第一个query-base的OVAD模型，能够和任意的视频VLMs进行结合，不需要region-level预训练。</p></li><li><p>相比于闭集的视频行为检测，开集行为检测旨在识别和在时空上定位视频中的人物的行为，包括训练阶段中见过的和没有见过的行为类别。具体地，一个OVAD模型从<span class="math inline">\(N_train\)</span> 个训练样本 <spanclass="math inline">\({(X,Y)_i | i = 1,...,N_{train}}\)</span>中学习，X表示训练视频，Y表示关键帧的bbox标注，包含bbox coordinatesb和行为类别y，在训练的时候，行为y来自固定的行为类别 <spanclass="math inline">\(C_b\)</span>，在测试的时候，学习的行为检测器能够从开集$C_B $ 检测给定视频中的任意行为类别, <spanclass="math inline">\(C_N\)</span>包含任意的新的行为类别。</p></li><li><p>OpenMixer是在query-base的actionDETR系列中开发的，<strong>DETR类型的模型讲行为检测任务识别一个集合预测问题，从视频中学习一组稀疏的queryfeatures，讲ground truth boxes和actionclasses进行匹配。对于OVAD任务，从开集中预测的行为类别包含基本的和新的行为</strong>。OpenMixer如图所示，给定一个videoX和text prompted action class list作为输入，利用预训练videoVLM的视觉和文本编码器，来得到所有的视频和行为text。<spanclass="math inline">\(\mathbf{V},\mathbf{f}_{v},\mathbf{S}=\Psi_{\mathrm{VE}}(\mathbf{X})\)</span>和 <span class="math inline">\(\mathbf{f}_{t} =\Psi_{\mathrm{TE}}(\mathbf{y})\)</span>，这里V, <spanclass="math inline">\(f_v\)</span>和S是4D的patch-level的视频特征，video-levelfeature和video attention。<spanclass="math inline">\(f_t\)</span>是类别y的textfeature，构建级联的M个openMixer Blocks(OMBJ)，来学习一组N个空间queries<span class="math inline">\(Q_s\)</span>，和N个时序的queries <spanclass="math inline">\(Q_t\)</span> from <span class="math inline">\(V,S, f_v, f_t\)</span> for person detection和行为类别。OMB将来自VLM的所有features作为输入，<spanclass="math inline">\(Q_s\)</span> 和 <spanclass="math inline">\(Q_t\)</span> 来预测person boxes, personscores和action scores.</p><p>对于第M个OMB，如图中所示，它包含了一个时序opemMixerblock(T-OMB)，一个S-OMB和一个动态的融合对齐(DFA)，S-OMB包含先验的位置采样，query-query(Q-Q)mixing by self-attention和 query-video(Q-V) mixing byAdaMixer，T-OMB包含Q-Q mixing，query conditioning和Q-Vmixing。DFA模块更新 <span class="math inline">\(Q_s\)</span>, <spanclass="math inline">\(Q_t\)</span>，第(m-1)个OMB的personboxes，预测的person scores和action scores。这三个模块用于OVAD任务 withconsideration of VLM semantics和localizability。</p></li><li><p><strong>Spatial OMB的LocalizabilityPrior</strong>：一个主要的挑战用于one-stage query-based detectors的是lowconvergence oflocalization，造成的主要原因是缺乏目标位置的先验知识，对于行为检测，近期的两阶段行为检测通过离线的persondetector和RoIAlign cropping解决定位问题，但是featurecropping缺乏时空上下文，当引入VLM的时候，会有representationgap，对于最近的query-based action detectors，personlocation的先验知识是缺失的，因此，当谈到VLMs的OVAD任务时，一个自然的问题是：能够从预训练的VLMs中以一个低的代价，得到actors的先验位置，受这些考虑的启发，依赖于预训练的VLM的视觉attention。</p><ul><li><p><strong>来自VLMattention的位置先验</strong>：视觉注意力maps是通过class activation map表征的，用来解释recognitionmodels，在ViT和VLM的时代，最近的工作提出用ViT最后一层的多头自注意力或者gradient-weighted accumulative product overmulti-layer self-attention，然而，MHSA不是visually faithful，因为videotokens的高度冗余，在video VLM上，gradient-based方法的缺点是在videoVLM上有很大的计算成本，对于不同的VLMs有不同的执行。另外，由于缺乏token-level的video-text关联，它们的attentionmap和action不是紧密相关。因此，一个高效的、结构未知的CAM对于大的videoVLM更合适，使得我们用patch-text关联作为VLMattention来编码未知先验，特别地，4D video feature。features都是L2归一化，首先通过最大相似度得到pre-matched textfeature，因为在测试的时候没法得到类别标签，因此，<spanclass="math inline">\(f_t\)</span>和V的内积决定了patch-text 关联。<spanclass="math inline">\(\mathbf{S}=\mathbf{V}\otimes\mathbf{f}_{t}\)</span>。自注意力层中的q-vattention展示了一个相反的heatmap，前景region和low attentionvalue相关联。CAM是由reversed patch-text 相似度决定。<spanclass="math inline">\(\hat{S} = 1 -S\)</span>，通过reshaping和spatialinterpolation over <spanclass="math inline">\(\hat{S}\)</span>，得到attentionmap用于先验未知采样。将 <span class="math inline">\(\hat{S}\)</span>作为VLM indicated的person location的先验分布，因此，top-N个位置被采样作为初始的box center， <spanclass="math inline">\(\{(u,v)_{i}|i=1,\ldots,N\}{\sim}\hat{\mathbf{S}}(u,v,k)\)</span>，(u,v)是关键帧k的2D 坐标，N是queries的数量。</p></li><li><p><strong>SpatialOMB</strong>：有了采样的先验位置，S-OMB包含Q-Q和Q-V mixingmodule，将video patch features V和第(m-1)阶段的box prediction <spanclass="math inline">\(\hat{b}_{m-1}\)</span>，来更新spatial queries<span class="math inline">\(\hat{Q}_s\)</span>，更新的spatial queries<span class="math inline">\(\hat{Q}_s\)</span> 用来预测person scores<span class="math inline">\(\hat{o}_m\)</span> 和person box offsets<span class="math inline">\(\Delta \hat{b}_m\)</span> by MLP。stagem的预测的boxes通过 <span class="math inline">\(\hat{b}_m = \hat{b}_{m-1}+ \Delta \hat{b}_m\)</span>，初始的box queries <spanclass="math inline">\(\hat{b}_0\)</span> 包含采样的先验位置和videospatial range。</p></li><li><p>后面的设计的technical intuition是鼓励proposed spatialOMB，来学习box 偏置 <span class="math inline">\(\Deltab\)</span>，从继承自预训练的VLM的先验位置开始，相比于之前的方法，用固定的non-informativeframecenters作为先验位置，作者的VLM基于attention的先验位置能够自适应的test-timevideo content和vocabulary，不仅提高了seen action的定位，而且泛化到unseen。</p></li><li><p><strong>Adaptive Semantics for TemporalOMB</strong>：对于query-based OVAD模型，temporal queries 期望用来bediscriminative 对于base和novel的行为。这个对于query-video(Q-V) mixingmodule，需要很强的content 解码的能力。pioneering workDETR用交叉注意力，同时采样MLP-Mixer，然而，没有VLM语义，这些方法不可避免地在seenclassdata上过拟合，不能检测到unseen。最近的工作能够解决VLM语义的重要性forquery features，但是它们缺乏对测试视觉内容的适应性，因为class-wisesemantic condition和region prompting。这个趋势我们提出了TemporalOMB，利用预训练的VLMs适应的semantics。</p></li><li><p><strong>Temporal OMB</strong>：时序queries <spanclass="math inline">\(Q_t\)</span> 和 当前阶段m的预测的boxes <spanclass="math inline">\(\hat{b}_m\)</span>，通过和video features V 和<spanclass="math inline">\(f_v\)</span>的interacting，来更新queries。为了实现用适应的语义的动机，提出了queryupdate： <span class="math display">\[\hat{\mathbf{Q}}_t =\Psi_{\text{qv}} \big( \Psi_{\text{qq}}(\mathbf{Q}_t, \mathbf{b}) \oplus\mathbf{f}_v, \mathbf{V}, \mathbf{b} \big),\]</span></p></li></ul><p>Q-Q mixing和Q-V mixing modules byself-attention和AdaMixer，这里，<span class="math inline">\(f_v\)</span>是预训练VLM video feature的适应的semantic condtition，broadcastly addedto the output of Q-Q mixing。</p><p>注意到语义条件的自适应性来自于测试时的video feature <spanclass="math inline">\(f_v\)</span>。Alternatively，当语义条件 <spanclass="math inline">\(f_v\)</span> 变到了 <spanclass="math inline">\(f_t\)</span> over Cclasses的时候，等同于way，然而，empirically展示了，这导致inferiorperformance，特别是对于seen动作检测。这个inferiority归因于对于测试时的视频内容缺少adaptability。Besides，作为另一个替代，后条件是将condition<span class="math inline">\(f_v\)</span> 放在 Q-V mixing之后，模块学习residual of <spanclass="math inline">\(f_v\)</span>，empirically发现，pre-condition优于post-condition，因为更好的queryfeatures用来学习重要的Q-V mixing module。</p><ul><li><p><strong>动态融合对齐</strong>：为了识别seen和unseenactions，模型需要学习有区别的region-wise 视觉特征，来对齐seenactions，同时保持预训练VLMs的泛化的知识，来对齐unseenactions。处理两个目标是有挑战的。最近的方法是通过prompttuning，adapters和gradient preserving来用modeladaptation。然而，这些方法要么难以泛化到新的类别，要么通过大的VLM反向传播时产生很大的成本，特别是对于长视频。因此，对于开集行为识别用了动态融合对齐，设计的时候是轻量的，对于seen和unseen的行为都效果很好。DFA为了学习每个阶段的行为分类而制定，</p></li><li><p><strong>动态特征融合</strong>：这个步骤旨在融合video-levelfeature <span class="math inline">\(f_v\)</span> into each of thequeries <span class="math inline">\(\hat{Q}_t\)</span>。特别地，首先重复<span class="math inline">\(f_v\)</span> N次，然后，<spanclass="math inline">\(F_v\)</span> 和 <spanclass="math inline">\(\hat{Q}_t\)</span> 之间的融合是 通过这个 <spanclass="math inline">\(\tilde{\mathbf{F}}_v = \lambda \odot \mathbf{F}_v+ (1 - \lambda) \odot \hat{\mathbf{Q}}\)</span>,实现，query-specificlearnable <span class="math inline">\(\lambda\)</span>背后的intuition是它允许 dynamic contributions of the video-levelknowledge from <span class="math inline">\(f_v\)</span> to the differentlearnable queries in the set-matching training。</p></li><li><p><strong>Query-TextAlignment</strong>：为了做分类和开集行为，对于行为类别，用GPT-4来产生多个视觉描述的行为promptsfor each category，用VLM text encoder，汇聚的 C个类别的文本特征用 <spanclass="math inline">\(F_t\)</span> 表示，最后，用visual-text cosine相似度的softmax来表示多类别的分类概率。<spanclass="math inline">\(P(\hat{y}|\mathbf{\hat{Q}}) = \text{softmax}\left( \frac{\tilde{\mathbf{F}}_v \otimes \mathbf{F}_t^\top}{\tau}\right)\)</span>, <span class="math inline">\(\tau\)</span>是VLM的temperature。 在测试的时候，开集行为识别for all queries是通过找到最大的visual-text 余弦相似度。<spanclass="math inline">\(\hat{y} = \arg\max_{y \in \mathcal{C}} \left(\tilde{\mathbf{F}}_v \otimes \mathbf{F}_t^\top \right)\)</span></p></li></ul><p>注意到，不用spatialqueries作为DFA的输入，这使得T-OMB和S-OMB在训练的时候解耦，以至于personlocalization是类别未知的，这对于开集任务是重要的。</p><ul><li><strong>训练和推理</strong>：在训练的时候，对于行为定位，跟着DETR采用regularset matching loss。<span class="math inline">\(\mathcal{L}_{\text{set}}= \mathcal{L}_{\text{bce}} + \mathcal{L}_{L_1} +\mathcal{L}_{\text{giou}}\)</span>，$<spanclass="math inline">\(\mathcal{L}_{\text{bce}}\)</span> 是binary交叉熵损失 for person score prediction，<spanclass="math inline">\(\mathcal{L}_{\text{L1}}\)</span> 和 <spanclass="math inline">\(\mathcal{L}_{\text{giou}}\)</span>是预测值和真值之间的coordinate distance和GIoUdistance。然后，用匈牙利匹配来找到预测和ground truthboxes之间的最优bipartite matching。对于行为识别，用多类别的交叉熵损失<spanclass="math inline">\(\mathcal{L}_{\text{act}}\)</span>，因此训练的总损失是$<em>{} = w_1 $</em>{} + w_2 <spanclass="math inline">\(\mathcal{L}_{\text{act}}\)</span>，超参数 <spanclass="math inline">\(w_1\)</span> 和 <spanclass="math inline">\(w_2\)</span>是平衡损失项的权重。在推理的时候，有阈值的personscores决定了保留的person boxes，行为的分数给boxes分配行为类别。</li></ul></li></ol><p><imgsrc="https://figures.semanticscholar.org/7cd4f79243c11ea79e52a2488c9202a342967447/3-Figure1-1.png"alt="Framework" /> <span class="math inline">\(Fig.1^{[1]}\)</span>Framework 和opemMixer Block，给定一个vieo和开集行为，用promptedclasses和预训练的video VLM来得到所有的VLM features，有了openMixerblocks的级联和时空queries，模型预测action scores，persionboxes和相关联的person scores。</p><p><imgsrc="https://figures.semanticscholar.org/7cd4f79243c11ea79e52a2488c9202a342967447/4-Figure2-1.png"alt="S-OMB和T-OMB" /> <span class="math inline">\(Fig.2^{[1]}\)</span>Spatial 和Temporal OMB, DFA，Q-Q和Q-V mixingmodules旨在融合queries和across query-visual 特征。</p>]]></content>
    
    
    <summary type="html">&lt;h3
id=&quot;exploiting-vlm-localizability-and-semantics-for-open-vocabulary-action-detection1&quot;&gt;Exploiting
VLM Localizability and Semantics for Open Vocabulary Action
Detection&lt;sup&gt;[1]&lt;/sup&gt;&lt;/h3&gt;
&lt;blockquote&gt;
&lt;p&gt;作者是来自密西根州立大学，NEC和罗格斯大学的Wentao Bao, Kai Li, Yuxiao
Chen等人。论文引用[1]:&lt;/p&gt;
&lt;/blockquote&gt;
&lt;h3 id=&quot;time&quot;&gt;Time&lt;/h3&gt;
&lt;ul&gt;
&lt;li&gt;2024.Nov&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id=&quot;key-words&quot;&gt;Key Words&lt;/h3&gt;
&lt;ul&gt;
&lt;li&gt;vision-language models within the family of query-based DETR&lt;/li&gt;
&lt;li&gt;spatial and temporal OpenMixer Blocks&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id=&quot;总结&quot;&gt;总结&lt;/h3&gt;
&lt;ol type=&quot;1&quot;&gt;
&lt;li&gt;Action
Detection旨在检测视频里的空间和时间上的动作，现有的方式聚焦于闭集检测，action
detector是训练和测试都是在一个固定的行为类比的集合上。然而，这个限制的设定在一个开放的世界中不可行，测试的视频超过了训练的行为类别，本文中，解决这个挑战性的开集行为检测问题。旨在检测测试视频中的任意行为，然而在一个固定的行为类别上训练模型。为了实现这样一个开集的能力，提出了&lt;strong&gt;OpenMixer&lt;/strong&gt;的方法，利用内在的语义和大的视觉语言模型的定位能力
within the family of query-based detection
models(DETR)。特别地，OpenMixer是通过时空上的OpenMixer Blocks(S-OMB and
T-OMB)，一个动态的融合模块。这三个模块一起用来预训练VLMs的强大的表征能力和来自DETR设计的端到端的学习。更多的，在多个设定下，建立了OVAD
benchmarks，实验结果表明，OpenMixer在检测见过和没见过的行为上，都超过了基线方法。&lt;/li&gt;
&lt;/ol&gt;</summary>
    
    
    
    <category term="Papers" scheme="https://young-eng.github.io/YoungBlogs/categories/Papers/"/>
    
    
    <category term="Open-set Object Detection" scheme="https://young-eng.github.io/YoungBlogs/tags/Open-set-Object-Detection/"/>
    
  </entry>
  
  <entry>
    <title>OV-STAD</title>
    <link href="https://young-eng.github.io/YoungBlogs/2024/11/28/OV-STAD/"/>
    <id>https://young-eng.github.io/YoungBlogs/2024/11/28/OV-STAD/</id>
    <published>2024-11-28T14:25:19.000Z</published>
    <updated>2024-12-01T06:17:39.597Z</updated>
    
    <content type="html"><![CDATA[<h3id="open-vocabulary-saptio-temporal-action-detection1">Open-VocabularySaptio-Temporal Action Detection<sup>[1]</sup></h3><blockquote><p>作者是来自南大，南航和上海AI Lab的Tao Wu, ShuqiuGe等人，论文引用[1]:Wu, Tao et al. “Open-Vocabulary Spatio-TemporalAction Detection.” ArXiv abs/2405.10832 (2024): n. pag.</p></blockquote><h3 id="time">Time</h3><ul><li>2024.May</li></ul><h3 id="key-words">Key Words</h3><ul><li>video-language model</li></ul><h3 id="总结">总结</h3><ol type="1"><li>时空行为检测是一个fine-grained视频理解任务，当前的方法需要所有box和label的监督信息。然而，在真实世界中，很可能出现在训练集中没有出现过的行为类别，因为行为类别空间是大的，很难枚举。对于新的类别，模型训练和数据标注的成本很高，需要仔细的box标注，和从头训练网络。<strong>在本文中，通过进行开集STAD，提出了一个新的挑战性的设定，来更好的模拟开放世界中的行为检测。开集时空行为检测需要在有限行为类别和box监督中训练模型</strong>，期望在新的类别上产生良好的泛化表现。对于开集时空行为检测，基于已有的STAD数据集，建立了两个benchmarks，提出了一个基于VLM的简单有效的方法，为了更好的将VLM适配fine-grained行为检测任务，将其仔细地在localized video region-textpairs上进行微调。这定制化的fine-tuning使得VLM有更好的模型理解，导致videoregions和texts有更精确地的对齐。在对齐之前的局部特征和全局视频特征融合isadopted ，通过提供全局上下文，来进一步提高行为检测能力。</li></ol><span id="more"></span><ol start="2" type="1"><li><p>时空行为检测旨在定位视频帧中的行为实例的boxes和识别它们的类别。它有广泛的应用，当前主流的STAD模型包含两阶段的方法，将humandetection和行为分类进行解耦，还有query-based一阶段的方法。尽管有很大的进步，当前的方法要求所有行为类别的bboxes和行为标签，用于监督训练，训练的模型在训练期间只能处理seenactionlabels。一些视觉语言模型VLMs例如CLIP，ALIGN，通过在大规模的图像文本对上的对比预训练，有zero-shot的视觉识别能力。最近，一些目标检测方法利用了VLMs用于开集目标检测，用了预训练VLMs提供的视觉和文本表征，在有限的baseclasses上进行训练，用于目标检测任务。训练的模型期望在新的目标类别上实现很好的泛化性能，新的目标类别是在训练过程中没有见过的。至于videodomain，视频文本数据预训练模型例如VideoCLIP和ViCLIP使得开发用于视频识别和检测的开集方法成为可能。有很多其它的方法聚焦于开集行为识别或者时序行为检测，但是据作者所知，morefine-grained 时空行为检测任务在开集情况下还没有被研究。</p><p><strong>然而，作者相信，开集时空行为检测有很大的研究价值，行为的类别是巨大的，当构建一个时空行为检测数据集的时候，很难枚举所有的行为类别，时空行为检测的数据的标注的成本也很高，因为它要求细粒度的标注，就是，用bboxes来精确地标记每一帧的所有存在的行为实例，同时标上类别标签。在真实的应用中，很可能遇到数据集里没有的行为类别，每次遇到新的行为类别的时候，需要消耗很多的资源来搜集和标注新类别的数据和重新训练模型，在开集设定下，时空行为检测模型在有限的行为类别上进行训练，用提供的boxes和标签作为监督信息，训练的模型期望在没有见过的行为类别上有很好的泛化性，能够有效地减少数据标注的成本，提高模型训练的效率</strong>。</p><p>OV-STAD在至少3个方面具有挑战性：首先，对于模型训练和评估，这里没有现有的benchmark；第二，在上游预训练和目标行为检测任务上有gap；上游预训练将文本和全局视频特征进行对齐；然而目标任务需要行为类别和localvideopatches进行对齐；在预训练的时候，很大比例的文本仅参与了scene，object，和外观描述，但是这和行为和活动无关。因此，预训练模型在理解运动概念上存在不足。当前时空行为检测数据集比较小。在训练期间，模型会倾向于过拟合。</p><p>在本文中，基于UCF101-24和JHMDB21和AVA，通过适当地将行为类别划分为novel和baseclasses，来构建两个benchmarks。在两个benchmarks测试一些video-languagemodels。直接将这些模型用在OV-STAD任务上表现不好，<strong>虽然它们在zero-shot行为识别上能够得到比较好的结果。将这个归因于上游预训练和目标任务之间的gap</strong>。为了缩小这个gap，提出从时空groundigndatasets中选择video region-text对。这些事human-centered，文本包含高密度的人类行为和活动描述。这个模型能够学习丰富的运动信息的概念；除此之外，提出了在对齐actionprompts之前，融合全局video-level feature和local patch-levelfeature，保留从大规模video-text预训练的知识，来减小过拟合，为行为识别提供全局上下文。在两个benchmarks上进行了大量实验，展示了作者方法的有效性。作者的方法在novelactionclasses上实现了很好的效果。总的来说，作者的贡献有三块：对于STAD任务，提出了新的开集设定，建立了讲个模型训练和评估的benchmarks；提出了一个基于视觉语言模型的简单有效的方法，利用了videoregion-text 对齐和global-local fusion来缩小上游预训练和目标行为检测任务的gap；在两个benchmarks上做了大量实验，展示了设计的有效性，作者的方法在novelclasses上取得了很好的效果。</p></li><li><p><strong>开集目标检测</strong>：开集目标检测旨在检测训练过程中没有标注的新的类别的目标。F-VLM用冻结的预训练的VLMencoders用于特征提取，训练一个额外的检测头，一些工作将regionalembeddings和CLIP的表征空间通过知识蒸馏进行对齐，其它的用VLMs为imageregions，产生伪目标标签或者文本描述。产生的数据用于训练检测器，来学习多种目标类别的概念，GLIP和GroundingDINO识别目标检测和visual grounding tasks之间的关联，利用visual groundingdatasets for visual concept learning。</p><ul><li><strong>开集行为识别</strong>：早期的OVAR方法，通过增加额外的可学习的adapters和时序建模Layers，将图像文本对预训练的VLMs用于行为识别。也有方法利用大语言模型来为视频制作多样的自然语言描述。也有方法通过替换captions中的动词，构建hardnegatives，来识别行为。VideoCLIP利用instructional videodatasets，InternVid利用LLMs来构建大规模的video-textdatasets，预训练一个类似CLIP的模型，用于videofield，来弥合视频和图像之间的差距。</li><li><strong>时空行为检测</strong>：时空行为检测器可以被划分为两阶段和单阶段。两阶段的方法将STAD任务结构成定位和分类两步；一些工作利用额外的检测器来检测视频keyframes中的bboxes。通过RoI操作，提取每个box的特征用于行为识别，其它的工作将proposalgenerationnetwork和分类network联合起来训练，实现端到端的检测过程。一阶段的方法同时执行定位和分类。MOC在特征图上的每个点，回归bboxes和预测行为类别。受DETR启发，TubeR采用了一系列的tubeletqueries，来直接产生actiontubes，STMixer构建了多尺度的时空特征，从中灵活地采样和解码特征。</li></ul></li><li><p>方法：采用在大规模的的video-text数据集上预训练的VLM用于visual andtextual feature extraction，利用它理解多样visualconcepts的能力。采用两阶段的检测pipeline，首先检测bboxes，然后提取每个box的特征，和actionpromptembeddings进行对齐，用于行为分类。<strong>然而，局部视觉特征在运动表征上是不足的</strong>，和文本描述没有很好地对齐。<strong>提出通过全局和局部videofeature fusion，来增强video region features，还有video region-textalignment</strong>。</p><ul><li><strong>Revisiting Video-text contrastivePretraining</strong>：用在InternVid-10M-FLT上预训练的ViCLIP模型用于视频和文本的特征提取。InternVid-1j0M-FLT包含10M个video-text对。ViCLIP通过video-text对齐，来学习video-textrepresentations。特别地，ViCLIP采用一个视频encoder和一个textencoder，来得到全局视频特征和文本特征。为了提高video feature <spanclass="math inline">\(\mathcal{f}_G\)</span> 和对应的text feature <spanclass="math inline">\(\mathcal{f}_T\)</span>之间的相似度，同时减小和不匹配的textfeatures的相似度，ViCLIP计算和最小化基于 <spanclass="math inline">\(\mathcal{f}_T\)</span> <spanclass="math inline">\(\mathcal{f}_G\)</span> 的InfoNCE loss：</li></ul><p><spanclass="math display">\[\mathcal{L}=-\sum_{i=1}^N\log\frac{\exp\left(\sin\left(f_i^\mathbf{G},f_i^\mathbf{T}\right)/\tau\right)}{\sum_{j=1}^N\exp\left(\sin\left(f_i^\mathbf{G},f_j^\mathbf{T}\right)/\tau\right)},\]</span></p><p>N表示batch中的video-text对，sim(')计算两个特征之间的余弦相似度，<spanclass="math inline">\(\tau\)</span> 是learnable temperature。</p><ul><li><strong>两阶段OV-STAd pipeline</strong>：首先用离线的action-agnostic检测器来产生keyframes上的proposals，然后识别每个proposal上的类别。通过对齐proposals和行为类别prompts，在baseclasses上微调模型。特别地，video clips centered at thekeyframes将作为video encoder的输入，提取全局视频特征 <spanclass="math inline">\(\mathcal{f_G}\)</span> 和视频特征图 <spanclass="math inline">\(\mathcal{f}^V\)</span>。局部region features <spanclass="math inline">\(\mathcal{f}^R\)</span> 通过对 <spanclass="math inline">\(\mathcal{f}^V\)</span> with box proposals <spanclass="math inline">\(B\)</span> detected on the keyframeRoIAlign得到。文本encoder用来提取prompt embeddings <spanclass="math inline">\(\mathcal{f}^P\)</span>。通过对海量的video-text数据进行预训练，保持video-text 对齐，也为local features提供 globalcontext。提出了通过简单地elelment-wise addition，将global video features<span class="math inline">\(\mathcal{f}^G\)</span> 和 局部regionfeatures <span class="math inline">\(\mathcal{f}^R\)</span>进行融合。</li></ul><p><span class="math display">\[f^{\mathbf R&#39;}=\beta\cdot f^{\mathbfR}+(1-\beta)\cdot f^{\mathbf G},\]</span></p><p><span class="math inline">\(\beta\)</span>是预设的超参数，用来控制local 和global 信息的比例。计算融合videofeatures <span class="math inline">\(\mathcal{f}^{R&#39;}\)</span>和action prompt embedding <spanclass="math inline">\(\mathcal{f}^P\)</span> 之间的余弦相似度作为行为分类的分数 <span class="math inline">\(S\)</span>，given by:</p><p><spanclass="math display">\[S_{ij}=\operatorname{sigmoid}(\operatorname{sim}(f_i^{\mathbf{R}&#39;},f_j^{\mathbf{P}})/\tau),\]</span></p><p><span class="math inline">\(Sij\)</span> 是第i个humanbox在第j个行为类别上的分数。对于训练，提供了base actionclasses的prompts，基于output scores S和ground truth Y，计算 focalloss：</p><p><span class="math display">\[\mathcal{L} = - \sum_{i=1}^{N}\sum_{j=1}^{C_b} \left\{ \begin{array}{ll}(1 - S_{ij})^{\gamma} \log S_{ij} &amp; \text{if } j = Y_i \\S_{ij}^{\gamma} \log (1 - S_{ij}) &amp; \text{otherwise}\end{array} \right.\]</span></p><p>N表示batch中的proposals的数量，<spanclass="math inline">\(C_b\)</span> 表示baseclasses的数量。对于推理，action prompts 应该用action prompts ofinterest进行替代，这包括base classes 和novel classes.</p></li><li><p><strong>Video Region-TextAlignment</strong>：<strong>尽管VLM在大量的video-text数据上进行contrastivepretraining之后，有能力将视频和它们对应的文本描述对应起来。argue that在上游预训练和target action detectiontask上仍然有gap：首先，在预训练的时候，全局视频features和text features是对齐的，然而，target action detection任务需要对齐的local video patches(bboxes) with action categoryprompts</strong>。第二点，很大比例的视频或者文本描述只涉及scenes,objects,appearances，和actions没有关系。研究表明这些预训练模型对于理解多样的运动概念不够。</p><p>为了更好地将VLM适配fine-grained actindetection任务，提出进一步在videoregion-text数据上训练模型，为了更好地videoregion和对应的文本描述的对齐。从时空video grounding datasets包括HC-STVG等中收集video region-text pairs。STVG任务需要通过给定的textwith a spatio-temporal tube(sequence of bboxes)描述的targetobjects进行定位。HC-STVG2.0中的文本描述是关于人类行为和活动的，但是描述通常包含多个描述一系列连续行为的句子。可能引入了一些噪声来对齐box。为了减少region-text对齐中的噪声，将长句子弄成短句子，每个仅包含一个行为。同时将tubes分割成sub-tubes，将它们和短句子按照时间顺序进行一对一的匹配。</p><p>在将video regions和文本描述对齐的过程中，模型exposed to variousaction descriptions，能够学习区分多样的运动概念。</p><p>video-text预训练的VLM用于video and text featureextraction。文本encoder被冻结，只训练video encoder in thisstage，旨在获得更好的video region representations。给定video featuremaps <span class="math inline">\(\mathcal{f}^V\)</span> 和bboxes <spanclass="math inline">\(B\)</span>，region <spanclass="math inline">\(B\)</span> 的features <spanclass="math inline">\(\mathcal{f}^R\)</span> 通过RoIAlign在 <spanclass="math inline">\(\mathcal{f}^V\)</span> 操作得到。全局和局部的videofeature fusion进行融合，融合的特征 <spanclass="math inline">\(\mathcal{f}^{R&#39;}\)</span> 用于和textembeddins进行对齐。用Focal loss来增强hard samples的学习： <spanclass="math display">\[P_{ij} = \text{sigmoid}\big(\text{sim}(f_i\mathbf{R}&#39;, f_j^\mathbf{T}) / \tau\big)\]</span> <spanclass="math display">\[\mathcal{L} = - \sum_{i=1}^N \sum_{j=1}^N\begin{cases}(1 - P_{ij})^\gamma \log P_{ij}, &amp; \text{if } i = j \\P_{ij}^\gamma \log (1 - P_{ij}), &amp; \text{otherwise}\end{cases}\]</span></p><p>N表示batch中的video region-text对的数量，sim(')计算两个特征之间的余弦相似度，<span class="math inline">\(\tau\)</span>是可学习的temperature parameter。<spanclass="math inline">\(\gamma\)</span> 是focal loss的超参数。</p></li><li><p>数据集：</p><ul><li>UCF-JHMDB：这是基于UCF101-24和JHMDB21的，UCF101-24作为baseclasses，模型在这个数据上进行训练。JHMDB21作为novelclasses，在两个数据上都会进行评估。</li><li>OV-AVA。将60个action classes分为45 base和 15 novelclasses.这个划分是基于三个原则：首先：base classes的数量和novelclasses数量的比例是3：1；第二，base classes和novelclasses实例的分布应该类似，follow长尾分布；第三，在监督设定下的方法应该在baseclasses和novel classes上实现类似的性能。</li></ul></li></ol><p><imgsrc="https://figures.semanticscholar.org/f0f3f170bbc7619f12a3c43da2664ca3c92d1d42/2-Figure1-1.png"alt="Overview" /> <span class="math inline">\(Fig.1^{[1]}\)</span>用一个在大量数据上预训练的video-language model作为video和textencoders。模型进一步在video region-text pairs上进行训练，然后在baseclasses上进行微调。训练的模型期望在novel actionclasses上有很好的泛化性能。</p><p><imgsrc="https://figures.semanticscholar.org/f0f3f170bbc7619f12a3c43da2664ca3c92d1d42/4-Figure2-1.png"alt="Pipeline" /> <span class="math inline">\(Fig.2^{[1]}\)</span>OV-STAD的pipeline，一个在 大规模video-text数据集上预训练的video-languagemodel用于image和textfeautre的提取，利用它的zero-shot的视觉识别能力。进一步在videoregion-text pairs上训练模型，来得到videoregion和text之间更好地对齐。OV-STAD用了两阶段的pipeline，首先用一个检测器，产生proposals，然后通过将regionfeatures和action prompt embeddings进行对齐，识别行为类别。</p>]]></content>
    
    
    <summary type="html">&lt;h3
id=&quot;open-vocabulary-saptio-temporal-action-detection1&quot;&gt;Open-Vocabulary
Saptio-Temporal Action Detection&lt;sup&gt;[1]&lt;/sup&gt;&lt;/h3&gt;
&lt;blockquote&gt;
&lt;p&gt;作者是来自南大，南航和上海AI Lab的Tao Wu, Shuqiu
Ge等人，论文引用[1]:Wu, Tao et al. “Open-Vocabulary Spatio-Temporal
Action Detection.” ArXiv abs/2405.10832 (2024): n. pag.&lt;/p&gt;
&lt;/blockquote&gt;
&lt;h3 id=&quot;time&quot;&gt;Time&lt;/h3&gt;
&lt;ul&gt;
&lt;li&gt;2024.May&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id=&quot;key-words&quot;&gt;Key Words&lt;/h3&gt;
&lt;ul&gt;
&lt;li&gt;video-language model&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id=&quot;总结&quot;&gt;总结&lt;/h3&gt;
&lt;ol type=&quot;1&quot;&gt;
&lt;li&gt;时空行为检测是一个fine-grained
视频理解任务，当前的方法需要所有box和label的监督信息。然而，在真实世界中，很可能出现在训练集中没有出现过的行为类别，因为行为类别空间是大的，很难枚举。对于新的类别，模型训练和数据标注的成本很高，需要仔细的box标注，和从头训练网络。&lt;strong&gt;在本文中，通过进行开集STAD，提出了一个新的挑战性的设定，来更好的模拟开放世界中的行为检测。开集时空行为检测需要在有限行为类别和box
监督中训练模型&lt;/strong&gt;，期望在新的类别上产生良好的泛化表现。对于开集时空行为检测，基于已有的STAD数据集，建立了两个benchmarks，提出了一个基于VLM的简单有效的方法，为了更好的将VLM适配fine-grained
行为检测任务，将其仔细地在localized video region-text
pairs上进行微调。这定制化的fine-tuning使得VLM有更好的模型理解，导致video
regions和texts有更精确地的对齐。在对齐之前的局部特征和全局视频特征融合is
adopted ，通过提供全局上下文，来进一步提高行为检测能力。&lt;/li&gt;
&lt;/ol&gt;</summary>
    
    
    
    <category term="Papers" scheme="https://young-eng.github.io/YoungBlogs/categories/Papers/"/>
    
    
    <category term="open-set object detection" scheme="https://young-eng.github.io/YoungBlogs/tags/open-set-object-detection/"/>
    
  </entry>
  
  <entry>
    <title>openmax</title>
    <link href="https://young-eng.github.io/YoungBlogs/2024/11/28/openmax/"/>
    <id>https://young-eng.github.io/YoungBlogs/2024/11/28/openmax/</id>
    <published>2024-11-28T09:17:02.000Z</published>
    <updated>2024-11-28T09:17:02.186Z</updated>
    
    <content type="html"><![CDATA[]]></content>
    
    
      
      
    <summary type="html">
</summary>
      
    
    
    
    
  </entry>
  
  <entry>
    <title>WSL-installation</title>
    <link href="https://young-eng.github.io/YoungBlogs/2024/11/25/WSL-installation/"/>
    <id>https://young-eng.github.io/YoungBlogs/2024/11/25/WSL-installation/</id>
    <published>2024-11-25T10:07:56.000Z</published>
    <updated>2024-11-26T04:42:18.517Z</updated>
    
    <content type="html"><![CDATA[<h3 id="wsl-安装">WSL 安装</h3><p>一些命令： <figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">wsl --shutdown ##关闭所有正在运行的WSL</span><br><span class="line">wsl --terminate &lt;DistributionName&gt; ##关闭指定的WSL</span><br><span class="line">wsl --list --all ##查看所有WSL的详细信息</span><br><span class="line">wsl -d &lt;DistriubutionName&gt; ##启动迁移后的发行版，确认它能正常运行</span><br><span class="line">wsl --unregister &lt;DistributionName&gt; ##注销指定的WSL</span><br></pre></td></tr></table></figure></p><span id="more"></span><h3 id="安装方式">安装方式</h3><ol type="1"><li><p>Microsoft Store 安装：直接去MicrosoftStore搜索想要安装的WSL版本即可。</p></li><li><p>命令行安装： <figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">wsl --set-default-version 2</span><br><span class="line">wsl --update</span><br><span class="line">wsl --list --online # 列出所有可安装的linux版本</span><br><span class="line">wsl --install -d Ubuntu-20.04 # 安装Ubuntu-20.04</span><br><span class="line"></span><br><span class="line">sudo apt update</span><br><span class="line">sudo apt upgrade</span><br><span class="line">sudo apt install gedit # gnome桌面下的编辑器</span><br><span class="line"></span><br></pre></td></tr></table></figure></p></li><li><p>将WSL迁移到其他盘：有两种命令行操作的方式：</p><ul><li>wsl export 和 wsl import <figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">wsl --export &lt;DistributionName&gt; &lt;FileName&gt;</span><br><span class="line">wsl --import &lt;DistributionName&gt; &lt;InstallLocation&gt; &lt;FileName&gt;</span><br></pre></td></tr></table></figure>其中，<code>&lt;DistributionName&gt;</code>是WSL的名称，<code>&lt;FileName&gt;</code>是导出的文件名，<code>&lt;InstallLocation&gt;</code> 是安装位置。</li><li>wsl manage<code>wsl --manage Ubuntu-24.04 --move D:\WSL\Ubuntu</code></li></ul></li></ol><p>参考链接： <figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">https://www.sysgeek.cn/move-wsl-distros-windows/</span><br><span class="line"></span><br><span class="line">https://learn.microsoft.com/zh-cn/windows/wsl/basic-commands</span><br><span class="line"></span><br><span class="line">https://blog.jikun.dev/other/wsl</span><br><span class="line"></span><br><span class="line">https://learn.microsoft.com/zh-cn/windows/wsl/basic-commands</span><br><span class="line"></span><br></pre></td></tr></table></figure></p>]]></content>
    
    
    <summary type="html">&lt;h3 id=&quot;wsl-安装&quot;&gt;WSL 安装&lt;/h3&gt;
&lt;p&gt;一些命令： &lt;figure class=&quot;highlight plaintext&quot;&gt;&lt;table&gt;&lt;tr&gt;&lt;td class=&quot;gutter&quot;&gt;&lt;pre&gt;&lt;span class=&quot;line&quot;&gt;1&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;2&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;3&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;4&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;5&lt;/span&gt;&lt;br&gt;&lt;/pre&gt;&lt;/td&gt;&lt;td class=&quot;code&quot;&gt;&lt;pre&gt;&lt;span class=&quot;line&quot;&gt;wsl --shutdown ##关闭所有正在运行的WSL&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;wsl --terminate &amp;lt;DistributionName&amp;gt; ##关闭指定的WSL&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;wsl --list --all ##查看所有WSL的详细信息&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;wsl -d &amp;lt;DistriubutionName&amp;gt; ##启动迁移后的发行版，确认它能正常运行&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;wsl --unregister &amp;lt;DistributionName&amp;gt; ##注销指定的WSL&lt;/span&gt;&lt;br&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;&lt;/figure&gt;&lt;/p&gt;</summary>
    
    
    
    <category term="methods" scheme="https://young-eng.github.io/YoungBlogs/categories/methods/"/>
    
    
  </entry>
  
  <entry>
    <title>YOLO-World</title>
    <link href="https://young-eng.github.io/YoungBlogs/2024/11/24/YOLO-World/"/>
    <id>https://young-eng.github.io/YoungBlogs/2024/11/24/YOLO-World/</id>
    <published>2024-11-24T06:07:47.000Z</published>
    <updated>2024-11-29T06:19:29.928Z</updated>
    
    <content type="html"><![CDATA[<h3id="yolo-world-real-time-open-vocabulary-object-detection1">YOLO-World:Real-Time Open-Vocabulary Object Detection<sup>[1]</sup></h3><blockquote><p>作者是来自华科和腾讯的Tianheng Cheng, LinSong等人，论文引用[1]:Cheng, Tianheng et al. “YOLO-World: Real-TimeOpen-Vocabulary Object Detection.” 2024 IEEE/CVF Conference on ComputerVision and Pattern Recognition (CVPR) (2024): 16901-16911.</p></blockquote><h3 id="time">Time</h3><ul><li>2024.Feb</li></ul><h3 id="key-words">Key Words</h3><h3 id="总结">总结</h3><ol type="1"><li>YOLO系列的检测器已经是高效和实际的工具了，然而，它们依赖于预先定义和训练的目标类别限制了它们在开集上的应用。为了解决这个问题，引入了YOLO-World。一个新的方式，通过VLM增强了YOLO开集检测的能力，特别地，提出了一个新的重新参数化的视觉语言PathAggregationNetwork(RepVL-PAN)，和region-text对比损失，来促进视觉和语言信息的interaction，方法能够以zero-shot的方式，检测非常广范围的目标。在LVIS数据集上，YOLO-World实现了35.4AP，实现了很好的速度和精度平衡。</li></ol><span id="more"></span><ol start="2" type="1"><li><p>尽管目标检测的方法很成功，但是它们仍然限制在一个固定的vocabulary，例如COCO数据集中的80类，一旦目标类别定义和标注了，训练检测器就智能检测这些类别，限制了在openscenarios上的能力。最近的工作探索了视觉语言模型来解决开集检测的问题，通过从languageencoders蒸馏vocabularyknowledge，BERT。然而，这些蒸馏的方法有限，因为训练数据少，多样性也有限，OV-COCO数据集包含48个basecategories。一些方法将目标检测训练reformulate为region-level视觉语言训练，训练开集目标检测器at scale。然而，这些方法仍然struggle fordetection in real-worldscenarios，主要是两个方面：计算成本高；在边缘设备上部署复杂。之前的工作展示了预训练大的检测器的良好性能，然而，预训练小的检测器使他们具有检测识别的能力没有被explored。本文中，提出了YOLO-world，旨在高效的开集目标检测。探索大规模的预训练的策略，来提高YOLO检测器，相比于之前的方法，提出的YOLO-world很高效，容易在下游应用上部署。<strong>特别地，YOLO-World是follows标准的YOLO架构，利用预训练的CLIPtext encoder来编码输入的文本，进一步提出了Re-parameterizableVision-Language Path AggregationNetwork(RepVL-PAN)来连接文本特征和图像特征 for better visual-semanticrepresentation。在推理期间，文本编码器可以被去掉，textembeddings能够被reparameterized into weights of RepVL-PAN for efficientdeployment</strong>。进一步研究了开集预训练策略forYOLO，通过在大规模数据集上的region-text对比学习，联合了检测数据，grounding data和image-text data intoregion-text pairs。预训练的YOLO-world有着大量的region-textpairs，展示了很强的开集检测的能力，训练更多的数据导致开集能力的提高。</p><p>另外，探索了<strong>prompt-then-detect</strong>范式，来进一步提高开集目标检测的能力，传统的检测器聚焦于固定集合的检测，然而，之前的开集检测器用textencoder编码prompts。注意到，这些方法倾向于用heavy backbone来employdetectors，来增加开集的capacity，相比之下，prompt-then-detect范式首先编码用户的prompts，来构建离线的vocabulary，这个vocabulary因不同的需要而不同。然后，高效的检测器能够推理离线的vocabulary,不需要重新编码prompts，为了实际的应用，一旦训练了检测器，可以pre-encodeprompts或者类别，来构建一个离线的vocabulary，然后将它们无缝的集成到检测器中。</p><p>主要贡献如下：引入了YOLO-World，一个先进的开集检测器，提出了Re-parameterizableVision-Language Path AggregationNetwork(RepVL-PAN)来连接视觉和语言特征，还有开集的region-text对比预训练策略。</p></li><li><p>之前的目标检测聚焦于固定集合的检测，在过去的很多年，传统目标检测的方法可以简单地分为3类，region-based，pixel-base和query-based，region-based有FasterRCNN，采用了两阶段的框架，pixel-based倾向于一阶段的检测器，进行分类和回归overpredefined anchors orpixels。DETR首先探索了用Transformer来做目标检测，在推理速度方面，YOLO系列用传统的卷积实现了很好的效果。一些工作提出了很多的架构或者设计，包括Pathaggregation networks，cross-stage partialnetworks和re-parameterization，进一步提高了速度和精度。相比于之前的YOLO系列，YOLO-World旨在检测固定集合之外的目标</p></li><li><p>开集目标检测是现代目标检测器的一个新的趋势，旨在检测预定义类别之外的目标，一些工作跟着标准的OVD，通过训练检测器onthe baseclasses，然后在新的类别上评估。然而，这个开集能够评估检测器的能力，来检测和识别新的目标。它仍然限制在开集，缺乏泛化到其它domains的能力，因为在有限的数据集上进行的训练。受视觉语言预训练的启发，最近的工作将开集目标检测formualte为image-textmatching，利用大规模的image-textdat来增加训练的vocabulary。OWL-ViTs用检测数据和groundingdatasets在简单的ViT上进行微调，构建了一个简单的开集检测器，GLIP展示了基于phrasegrounding的用于开集检测的预训练框架，用zero-shot的设定进行评估。GroundingDINO引入了grounded pre-training into detection transformers withcross-modalityfusions。一些方法统一了检测数据集和image-text数据集通过region-textmatching和用大规模image-text对进行预训练的检测器，实现了很好的性能和泛化性。然而，这些方法用了heavydetectors，像ATSS或者DINO，用Swin-L作为backbone，导致很高的计算需求和部署挑战，相比之下，YOLO-World，旨在高效的开集检测，不同于ZSD-YOLO，通过语言模型的对齐，用YOLO实现的开集检测，YOLO-World引入了一个新的YOLO框架，用了高效的预训练策略，增强了开集检测的能力。</p></li><li><p><strong>Region-TextPairs</strong>：传统的目标检测方法，包括YOLO系列，用实例的标注进行训练，包含了bbox和类别标签，本文中，将实例标注reformulate为region-textpairs <span class="math inline">\(\Omega = {B_i,t_i}^N_{i=1}\)</span>,<span class="math inline">\(t_i\)</span>是区域 <spanclass="math inline">\(B_i\)</span> 对应的文本，特别的，文本 <spanclass="math inline">\(t_i\)</span>可以是类别名字，名词短语或者目标描述。另外，YOLO-World采用 image I和textT作为输入，输出预测的boxes <spanclass="math inline">\({\hat{B_k}}\)</span> 和对应的Object embeddings<span class="math inline">\(e_k, (e_k \in\mathbb{R}^D)\)</span>。</p></li><li><p>模型架构，YOLO-world包含一个YOLO检测器，一个textencoder和re-parameterizable Vision-Language Path AggregationNetwork(RepVL-PAN)，给定输入的文本，YOLO-world中的textencoder将本文编码为text embeddings，检测器中的imageencoder从输入图像中提取多尺度特征，然后通过图像特征和textembeddings之间的跨模态融合，利用RepVL-PAN来增强text和image的represnetation。</p><ul><li><strong>YOLO-Detector</strong>：YOLO-World主要是基于YOLOv8，包含一个Darknetbackbone作为Image encoder,一个path aggregation network用于多尺度特征金字塔，和一个head用于bbox回归和object embeddings。</li><li><strong>TextEncoder</strong>：给定文本T，采用CLIP预训练的transformer的textencoder来提取对应的文本embeddings <span class="math inline">\(W =TextEncoderJ(T) \in \mathbb{R}^{C \timesD}\)</span>，C是名词的数量，D是embedding的维度。CLIP文本编码器相比于text-only language encoders，提供了很好的visual-semantic能力用于连接视觉目标和texts。当输入的文本是一个caption或者referringexpression，采用简单的n-gram算法来提取名词短语，然后将它们给到textencoder。</li><li><strong>Text Contrastive Head</strong>：采用解耦头，连个 <spanclass="math inline">\(3 \times 3\)</span> 的卷积，来回归bbox和objectembeddings，K表示目标的数量，提出了一个text contrastivehead，来得到object-text相似度，L2-norm是L2正则化，<spanclass="math inline">\(w_j \in W\)</span> 是第j个textembeddings，另外，增加了仿射变换，用可学习的scaling factor <spanclass="math inline">\(\alpha\)</span> 和shifting factor <spanclass="math inline">\(\beta\)</span>，L2-Norm和仿射变换对于为你的难过region-text训练都很重要</li><li><strong>Training with OnlineVocabulary</strong>：在训练期间，构建一个在线的vocabulary <spanclass="math inline">\(T\)</span> for each mosaic sample containing 4images。特别地，采样所有positive nouns，涉及到mosaicimages，从对应的数据集中，随机采样一些negative nouns，vocabulary foreach mosaic sample包含最多M个nouns，M默认设为80.</li><li><strong>Inference with OfflineVocabulary</strong>：在推理阶段，提出了prompt-then-detect策略，用离线的vocabularyfor further efficiency。如图3所示，用户可以定义一系列的customprompts，包括captions或者类别，然后用textencoder编码这些prompts，得到离线的vocabularyembeddings，离线的vocabulary 避免了每个Input的计算，提供了灵活性。</li></ul></li><li><p><strong>RepVL-PAN</strong>：图4展示了提出的RepVL-PAN的结构，followsthe top-down和bottom-up paths，来构建feature pyramids with multi-scaleimage features。另外，提出了Text-guided CSPLayer和Image-poolingattention，来进一步增强图像特征和文本特征之间的interaction，能够提高visual-semanticrepresentation for open-vocabulary 的能力。在推理期间，离线的vocabularyembeddings能够被re-parameterized into 卷积的参数或者线性层。</p><ul><li><strong>Text-guided CSPlayer</strong>：cross-stage partiallayers在top-down或者bottom-up融合之后被使用了。通过将textguidance引入multi-scale image features，扩展CSPLayer，构建Text-guideCSPLayer，特别地，给定text embedings W 和image features <spanclass="math inline">\(X_l \in \mathbb{R}^{H \times W \timesD}\)</span>，在最后的dark bottleneckblock之后采用max-sigmoid，来汇聚text features into image features：</li></ul><p><spanclass="math display">\[X_l&#39;=X_l\cdot\delta(\max_{j\in\{1..C\}}(X_lW_j^\top))^\top,\]</span></p><p>更新的 <span class="math inline">\(X_l&#39;\)</span> 和cross-stagefeatures进行concatenate，作为输出， <spanclass="math inline">\(\delta\)</span> 表示sigmoid 函数。</p><ul><li><strong>Image-Pooling Attention</strong>：为了用image-aware信息，增强text embeddings，通过Image-pooling attention来汇聚imagefeatures，更新text embeddings。不是直接在imagefeatures上用cross-attention，在多尺度特征上利用max pooling，得到 <spanclass="math inline">\(3 \times 3\)</span> regions，最后得到总共 27 patchtokens， <span class="math inline">\(\hat{X} \in \mathbb{R}^{27 \timesD}\)</span>，text embeddings这样进行更新：</li></ul><p><spanclass="math display">\[W&#39;=W+\text{MultiHead-Attention}(W,\tilde{X},\tilde{X})\]</span></p></li><li><p><strong>预训练策略</strong>：这里，提出了将YOLO-world在大规模检测，grounding和image-textdatasets上进行预训练的方法。</p><ul><li><strong>Learning from Region-Text ContrastiveLoss</strong>：给定mosaic sample I和textT，YOLO-world输出K个目标预测，along with annotations，利用task-alignedlabel assignment，给每个positive prediction分配一个textindex，作为分类的label，基于这个vocabulary，构建region-text contrastiveloss with region-text pairs，通过object-text相似度和object-textassignments之间的交叉熵损失。另外，采用IoU loss和分布的focal loss用于bbox回归，总的training loss定义为：<spanclass="math inline">\(\mathcal{L}(I) = \mathcal{L}_{\mathrm{con}} +\lambda_{I} \cdot (\mathcal{L}_{\mathrm{iou}} +\mathcal{L}_{\mathrm{dfl}})\)</span>，<spanclass="math inline">\(\lambda_{I}\)</span> 是 indicatorfactor，当输入的图像I来自detection 或者groundingdata的时候，设为1，来自image-text数据的时候，设为0，考虑到image-text数据集有噪声的boxes，仅计算有精确的bboxes的samples的回归损失。</li><li><strong>pseudo labeling with Image-Textdata</strong>：不直接用image-textpairs用于预训练，提出了一个自动标注的方法，来产生region-textpairs，特别地，labeling方法包含3个步骤：(1):提取名字短语：首先用n-gram算法从text中提取nounphrases，(2): psedulabeling:采用预训练的开集检测器，例如GLIP，为每个image，给定的名词短语，来产生pseudoboxes，因此，提供了粗糙的region-textpairs。(3)过滤：用预训练的CLIP来评估image-text对和region-text对的相关性，然后过滤相关性低的pseudo标注和images。进一步利用如NMS的方式，过滤大量的bbox。</li></ul></li><li><p>实验：YOLO-World是基于MMYOLOtoolbox开发的，根据不同的latency的要求，提供了3中YOLO-world的变体，采用开源的CLIPtext encoder with pre-trained weights来编码inputtext，除了特别说明，不需要额外的加速，测量所有模型的推理速度。在预训练阶段，采用AdamW优化器，初始学习率是0.002，weightdecay是0.05.text encoder在预训练期间被冻结。</p></li><li><p>结论：提出了YOLO-world，将YOLOs变成视觉语言架构的YOLO，用于开集预训练和检测，提出了RepVL-PAN，连接视觉和语言信息，能够被re-parameterizedfor efficient deployment。进一步提出了有效的预训练策略，用detection,grounding和image-text数据赋予YOLO-world很强的能力，用于开集检测。</p></li></ol><p><imgsrc="https://figures.semanticscholar.org/37c112454a236ab91c9c6b5cc165a6c3251e9206/3-Figure3-1.png"alt="Framework" /> <span class="math inline">\(Fig.1^{[1]}\)</span>相比于传统的YOLO检测器，YOLO-World作为一个开集检测器，用text作为输入，textencoder首先将input text编码成input text embeddings。然后，Imageencoder将输入图像编码成multi-scale imagefeatures，提出的RepVL-PAN利用多尺度跨模态融合 for both image 和 textfeatures。最后，YOLO-World预测bboxes和objectembeddings，用于匹配出现在input text中的categories或者nouns。</p><p><imgsrc="https://figures.semanticscholar.org/37c112454a236ab91c9c6b5cc165a6c3251e9206/5-Figure4-1.png"alt="RepVL-PAN" /> <span class="math inline">\(Fig.2^{[1]}\)</span>提出的RepVL-PAN 采用Text-guidedCSPLayer，将语言信息注入到图像特征中，Image pooling attention用于增强image-aware text embeddings。</p><p><imgsrc="https://figures.semanticscholar.org/37c112454a236ab91c9c6b5cc165a6c3251e9206/14-Figure8-1.png"alt="Labeling Pipeline" /> <spanclass="math inline">\(Fig.3^{[1]}\)</span> Labeling Pipeline forImage-Text Data，首先利用简单的n-gram来从captions中提取objectnouns，利用预训练的开集检测器来产生pseudo boxes given objectnouns，形成粗糙的region-textproposals，然后用预训练的CLIP来rescore或者relabel boxes along withfiltering。</p>]]></content>
    
    
    <summary type="html">&lt;h3
id=&quot;yolo-world-real-time-open-vocabulary-object-detection1&quot;&gt;YOLO-World:
Real-Time Open-Vocabulary Object Detection&lt;sup&gt;[1]&lt;/sup&gt;&lt;/h3&gt;
&lt;blockquote&gt;
&lt;p&gt;作者是来自华科和腾讯的Tianheng Cheng, Lin
Song等人，论文引用[1]:Cheng, Tianheng et al. “YOLO-World: Real-Time
Open-Vocabulary Object Detection.” 2024 IEEE/CVF Conference on Computer
Vision and Pattern Recognition (CVPR) (2024): 16901-16911.&lt;/p&gt;
&lt;/blockquote&gt;
&lt;h3 id=&quot;time&quot;&gt;Time&lt;/h3&gt;
&lt;ul&gt;
&lt;li&gt;2024.Feb&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id=&quot;key-words&quot;&gt;Key Words&lt;/h3&gt;
&lt;h3 id=&quot;总结&quot;&gt;总结&lt;/h3&gt;
&lt;ol type=&quot;1&quot;&gt;
&lt;li&gt;YOLO系列的检测器已经是高效和实际的工具了，然而，它们依赖于预先定义和训练的目标类别限制了它们在开集上的应用。为了解决这个问题，引入了YOLO-World。一个新的方式，通过VLM增强了YOLO开集检测的能力，特别地，提出了一个新的重新参数化的视觉语言Path
Aggregation
Network(RepVL-PAN)，和region-text对比损失，来促进视觉和语言信息的interaction，方法能够以zero-shot的方式，检测非常广范围的目标。在LVIS数据集上，YOLO-World实现了35.4AP，实现了很好的速度和精度平衡。&lt;/li&gt;
&lt;/ol&gt;</summary>
    
    
    
    <category term="Papers" scheme="https://young-eng.github.io/YoungBlogs/categories/Papers/"/>
    
    
  </entry>
  
  <entry>
    <title>OWL-ViT</title>
    <link href="https://young-eng.github.io/YoungBlogs/2024/11/24/OWL-ViT/"/>
    <id>https://young-eng.github.io/YoungBlogs/2024/11/24/OWL-ViT/</id>
    <published>2024-11-24T06:07:12.000Z</published>
    <updated>2024-11-28T09:17:59.129Z</updated>
    
    <content type="html"><![CDATA[<h3id="simple-open-vocabulary-object-detection-with-vision-transformers1">SimpleOpen-Vocabulary Object Detection with VisionTransformers<sup>[1]</sup></h3><blockquote><p>作者是来自Google的Matthias Minderer, AlexeyGritsenko等人，论文引用[1]:Minderer, Matthias, et al. "Simpleopen-vocabulary object detection." European Conference on ComputerVision. Cham: Springer Nature Switzerland, 2022.</p></blockquote><h3 id="time">Time</h3><ul><li>2022.Jun</li></ul><h3 id="key-words">Key Words</h3><ul><li>open-vocabulary detection</li></ul><h3 id="总结">总结</h3><ol type="1"><li>将简单的架构和大规模预训练结合起来，会导致图像分类上取得很大的提高。对于目标检测，预训练和scaling方法还没有很好地建立，特别是长尾和开集，这些训练数据都很少。本文中，提出了一个很强的方法，将image-text模型迁移到开集目标检测。用了一个标准的ViT架构和一些小的修改，contrastiveimage-text pre-training和端到端的detectionfine-tunign。作者分析这个设置的scaling特性，展示了增加image-level的预训练和模型大小，能够对下游任务有持续的提高，作者提供了在零样本文本条件和单样本图像条件下的目标检测的适配策略和正则化方法。</li></ol><span id="more"></span><ol start="2" type="1"><li><p>目标检测是一个基础任务。直到最近，检测模型通常被限制在一个小的，固定集合的语义类别，因为要得到有定位标注的大量或者openlabel spaces训练数据是有成本的且花时间的。**这个随着languageencoders和contrastive image-text training的发展而改变。这些模型从looselyaligned图像文本对中学习图像和文本共享的representation，这个在web上有大量的数据。通过利用大量的图像文本对，contrastivetraining在零样本的分类性能和其它language-basedtasks上取得了很大的提高。</p></li><li><p>很多最近的工作旨在将模型的语言能力迁移到目标检测上，这些方法，例如distillationagainst embeddings of imagescrops，用图像级labels的弱监督或者self-training。这里，作者提供了一个简单的架构和端到端的训练放啊，得到了很强的open-vocabularydetection，不需要这些方法，即使在训练过程中这些类别不可见。</p></li><li><p>首先用ViT架构，展示了很强的扩展性，在大的图像文本对上进行contrastively预训练。为了将模型的能力迁移到检测上，做了一些改进。去掉了最后的tokenpooling layer，相反，在每个transformer outputtoken上加了一个轻量的分类和回归头，<strong>开集分类是通过从textmodel中得到的class-name embeddings 替换固定的classification layerweights实现</strong>，在标准的检测数据集上用bipartite matchingloss微调预训练模型，图像和文本模型都能够端到端的微淘。</p></li><li><p>作者分析这个方法的scaling特性，发现，增加的模型尺寸和预训练时间，能够持续提高检测的性能，这很重要，因为相比于检测数据，图像文本对是大量的，且能够furtherscaling。作者模型的关键特征是它的简洁和模块化。因为模型的图像和文本内容没有融合，模型对于queryrepresentation的来源是未知的。因此，可以用模型，不要修改，作为一个one-shot检测学习器，简单地通过querying it with image-derivedembeddings。one-shot 目标检测是基于query image patch showing theobject，检测新目标的有挑战性的问题。基于图像的one-shot能力是基于文本的检测的扩展，因为它能够检测很难通过文本描述的物体，例如特征的构件。尽管用通用的架构，在unseen的COCO数据集上取得了SOTA。作者的贡献如下：</p><ul><li>一个简单的和很强的方法，用于将图像级的预训练迁移到开集目标检测</li><li>one-shot image-conditional的SOTA方法</li></ul></li><li><p>作者相信模型能够作为一个很强的基线方法，能够在很多框架中执行，称这个方法为<em>VisionTransformer for Open-World Localization(OWL-ViT)</em>。</p></li><li><p><strong>Contrastive Vision-LanguagePre-Training</strong>：将图像和文本嵌入到一个共享空间用于实现zero-shot泛化，这个idea用了很长时间。得益于对比损失和更好的架构，最近的模型能够从web-drivedimage和text pairs中学习一致的视觉和语言representations，不需要explicitannotation。这增加了可用的训练数据，在benchmarks上有了很大的提高。最近的很多图像文本对模型和作者的方法兼容，模型和数据集类似于LiT和ALIGN。</p></li><li><p><strong>Closed-Vocabulary ObjectDetection</strong>：目标检测模型传统上用于闭集，有一阶段和两阶段的检测器。最近的DETR展示了，目标检测能够作为一个集合预测问题，用bipartitematching进行训练，实现了很好的效果。这些架构不需要region prposalgeneration或者NMS。后续的工作提出了一些更高效的DETR的变体，包括不需要Decoder的架构，作者的工作简化了DETR，不需要decoder，通过从每个imagetoken中直接预测object instance，简化了模型。</p></li><li><p><strong>Long-Tailed and Open-Vocabulary ObjectDetection</strong>：为了超越闭集，固定的分类层被languageembeddings代替，来创建一个开集检测器，开集目标检测器近期有很大的进步，通过结合图像文本训练的模型和经典的目标检测器，这个任务主要的挑战是**如何将image-text的backbone的image-level的representation迁移到检测，尽管对于rareclasses，localizedannotations很少。利用好图像文本的预训练是很重要的，因为它不需要大量的人类标注，就能够scaling。有人提出了很多的方法，通过CLIP或者ALIGN用在croppedimages from class-agnostic region prposalnetwork上，ViLD对得到的embeddings进行蒸馏。RPN限制了在新的目标上的泛化能力，ViLD两步的蒸馏训练过程加剧了这个问题。RegionCLIP也用了多阶段训练，在captioningdata上产生pseudo-labels，紧接着region-text对比预训练，然后迁移到检测。相比之下，作者的方法在图像和文本模型上进行端到端的微调，简化了训练，提高了对于unseen类别的泛化性。MDETR和GLIP对整张图像用了一个single textquery，将检测formualte为一个phrase grounding问题，这些限制了目标的类别。作者的架构更加灵活，不需要image-text融合，能够处理多个独立的text或者image-derivedqueries，OVR-CNN和作者的方法最相似，微调image-text模型 to detection on alimitedvocabulary，依赖于图像文本的预训练。然而，作者的方法和其它的模型不同，用了ViT而不是ResNet，一个DETR类型的模型，而不是FasterRCNN，和LiT一样，用了image-text pre-training，而不是PixelBERT和visualgroundingloss。Detic通过只在有image-level的标注上，训练分类头，提高长尾检测的性能。注意到，这里定义的开集检测，目标类别可能在detectiontraining和testing的可能重叠。检测训练过程中，没有见过的任何定位实例的样本时，称之为zero-shot。</p></li><li><p><strong>Image-ConditionedDetection</strong>：和开集检测相关的是image-conditioneddetection，是检测目标，匹配single query image which shows an object ofthe category in question。这个任务也称之为 <em>one-shot objectdetection</em>。因为这个query image是single trainingexample。image-based querying允许open worlddetection，当object的name是未知的时候，例如对于uniqueobjects或者特殊的parts。作者的模型能够做到这个任务，不需要修改，通过简单地用image-derived而不是text-derivedembeddinngs作为queries。近期的在这个问题上的工作主要聚焦于架构的创新，例如用了query和targetimage之间的复杂的交叉注意力的形式，作者的方法依赖于简单地，但是比较大的模型和大量的image-text预训练。</p></li><li><p>作者的目标是构建一个简单可扩展的开集检测器，聚焦于标准Transformer的模型，因为它们的可扩展性，提出了一个两阶段的方法：</p><ul><li>contrastively pre-train image and text encoders on large-scaleimage-text data</li><li>增加了分类头，在中等的检测数据上进行微调模型能够以不同的方式进行query，来执行open-vocabulary或者few-shotdetection</li></ul></li><li><p>架构：模型采用标准的ViT作为图像的encoder和一个类似的Transformer架构作为textencoder，为了使image encoder适应detection，去掉了tokenpooling和最后的projection layer，相反，线性地映射每个output tokenrepresentation，来得到per-object image embeddings forclassification。预测的objects的最大的数量等于imageencoder的token的数量。实际中这不是瓶颈，因为模型的sequencelength至少是576.比现在的数据集的最大数量的instances都大。将tokenrepresentations通过一个MLP，得到boxcoordinates，设置类似于DETR，但是去掉了decoder，简化了模型。</p></li><li><p>开集目标检测：对于检测到的objects的open-vocabulary分类，跟着之前的工作，用在分类头的最后一层，用textembeddings，而不是learned class embeddings。textembeddings称之为queries，将categorynames或者其它的文本目标描述通过一个textencoder，来得到。这个模型的任务变成了<strong>预测每个object，一个bbox和一个eachquery applies to theobject的概率。对于每个image，queries是不一样的。每个image有它自己的discriminativelabel space，是通过一系列的text strings定义的。当完整的object categorynames用作query set for each image，这个方法就归类于经典的闭集目标检测。相比于其它的方法，没有把一个image的所有queries结合到single tokensequence中，相反，</strong>每个query 包含一个separate tokensequence，which 代表啊一个单独的目标描述，单独地被textencoder进行处理，另外，这个架构没有包含image和text的融合，尽管融合似乎是有益的，它降低了推理的效率，因为编码一个query需要一个forwardpass through the entire image model，对于每个image/querycombination都要重复。在这个设定中，计算queryembeddings独立于image，使得每张图片能够用大量的queries，many more thanis possible with early fusion**。</p></li><li><p><strong>One-or few-shot Transfer</strong>：这个设置不要求queryembeddings是文本来源，因为image和text没有进行融合，可以将image-derivedembeddings而不是text-derived embeddings作为queries to the classificationhead，不需要修改模型，通过使用prototypical objectimages的embeddings作为queries，模型能够进行image-conditioned one-shotobject detection。用imageembeddings作为queries，使得能够对用语言难以描述的object进行detection。</p></li><li><p><strong>Image-level contrastivePre-training</strong>：用相同的image-text dataset预训练image和textencoder，用对比损失和随机初始化来从零开始训练encoders，对于imagerepresentation，用一个多头的attention pooling(MAP)来汇聚tokenrepresentatin。这个text representation是textencoder的最后一个end-of-sequence得到的，或者，用了公开的预训练的CLIP模型。encoder-only架构的优势是，几乎所有的模型参数都能从image-levelpre-training中受益。detection-specific heads包含1.1%的模型参数</p></li><li><p><strong>Training theDetector</strong>：微调预训练模型用于分类是一个well-studies问题，分类器，特别是大的Transformers，需要仔细的tuned正则化和数据增强，来得到很好的效果，用于分类器训练的方法现在有很多了，这里，作者旨在提供一个类似的微调方法，用于开集检测。模型的检测训练过程和闭集的检测器一样，除了<strong>为每个image提供类别名称作为queries，分类头输出logitsover the per-image label space defined by thequeries，而不是一个固定的global label space</strong>。</p><p>采用DETR的bipartite matching loss，将其用于长尾/开集检测。由于需要对数据集进行详尽的注释，有大量类别的数据集是以federatedmanner进行标注的。这样的数据集有着non-disjoint labelspaces，意味着每个object有多个labels。因此，用了focal sigmoid交叉熵，而不是softmax交叉熵作为分类损失。进一步，因为不是所有的object类别在每个image中都有标注，federated数据集提供了positive和negative annotations for eachimage。在训练的时候，对于一个给定的图像，用所有的positive和negative标注作为queries，另外，随机地采样类别，和它们出现在数据中的频率成正比，将它们增加为pseudo-negatives，每个图片至少有50个negatives。</p><p>最大的feerated detection 数据集包含<spanclass="math inline">\(10^6\)</span>图像，相比于十亿量级的image-levelweaklabels还是比较小的，大的Transformers在这样的数据集上进行训练，需要仔细的正则化和数据增强。作者发现，对于检测的训练也一样，提供了用大的Transformer实现高性能的增强和正则化的详细分析。</p></li><li><p>对于图像模型，用标准的ViT，跟着命令法for model size, patchsize。对于textmodel，用一个类似于图像模型的Transformer，文本图像模型首先在image-level上进行预训练，然后在object-levelannotations上进行微调，预训练是从零开始。在预训练之后，tokenpooling去掉了，增加了检测头，对于每个outputtoken预测一个box，对于每个预测的box，增加了一个bias，因此每个box默认centeredon the image patch，对应于将token sequence弄成2Dgrid的时候的预测的box的token。模型因此预测和默认未知的不同，类似于RPN网络预测预定义anchors的偏置。虽然imagepatches和tokens representations later in the Transformernetwork没有严格的对应，增加box预测的偏置加速了训练，提高了最后的性能。</p></li><li><p>为了评估在没有见过的类别上的性能，去掉了稀少类别的标注，<spanclass="math inline">\(AP^{LVIS}_{rare}\)</span>评估模型的zero-shot性能，模型没有见过这些类别的标注。</p></li></ol><p><imgsrc="https://figures.semanticscholar.org/9dae204dad41633188022002a04c8aa67c79a4e1/3-Figure1-1.png"alt="Framework" /> <span class="math inline">\(Fig.1^{[1]}\)</span>左边：首先用图像文本对预训练一个图像和文本的encoder，类似于CLIP,ALIGN和LiT。右边：然后将预训练的encoders，通过去掉tokenpooling，在image encoder outputtokens上增加轻量的目标分类和定位头，迁移到开集目标检测，为了实现开集检测，querystrings用textencoder进行嵌入，用于分类，这些模型在标准的检测数据集上进行微调。推理的时候，用text-derivedembeddings用于开集检测，或者image-derived embeddings for few-shotimage-conditioned 检测。</p>]]></content>
    
    
    <summary type="html">&lt;h3
id=&quot;simple-open-vocabulary-object-detection-with-vision-transformers1&quot;&gt;Simple
Open-Vocabulary Object Detection with Vision
Transformers&lt;sup&gt;[1]&lt;/sup&gt;&lt;/h3&gt;
&lt;blockquote&gt;
&lt;p&gt;作者是来自Google的Matthias Minderer, Alexey
Gritsenko等人，论文引用[1]:Minderer, Matthias, et al. &quot;Simple
open-vocabulary object detection.&quot; European Conference on Computer
Vision. Cham: Springer Nature Switzerland, 2022.&lt;/p&gt;
&lt;/blockquote&gt;
&lt;h3 id=&quot;time&quot;&gt;Time&lt;/h3&gt;
&lt;ul&gt;
&lt;li&gt;2022.Jun&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id=&quot;key-words&quot;&gt;Key Words&lt;/h3&gt;
&lt;ul&gt;
&lt;li&gt;open-vocabulary detection&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id=&quot;总结&quot;&gt;总结&lt;/h3&gt;
&lt;ol type=&quot;1&quot;&gt;
&lt;li&gt;将简单的架构和大规模预训练结合起来，会导致图像分类上取得很大的提高。对于目标检测，预训练和scaling
方法还没有很好地建立，特别是长尾和开集，这些训练数据都很少。本文中，提出了一个很强的方法，将image-text
模型迁移到开集目标检测。用了一个标准的ViT架构和一些小的修改，contrastive
image-text pre-training和端到端的detection
fine-tunign。作者分析这个设置的scaling特性，展示了增加image-level的预训练和模型大小，能够对下游任务有持续的提高，作者提供了在零样本文本条件和单样本图像条件下的目标检测的适配策略和正则化方法。&lt;/li&gt;
&lt;/ol&gt;</summary>
    
    
    
    <category term="Papers" scheme="https://young-eng.github.io/YoungBlogs/categories/Papers/"/>
    
    
    <category term="Open-Set Object Detection" scheme="https://young-eng.github.io/YoungBlogs/tags/Open-Set-Object-Detection/"/>
    
  </entry>
  
  <entry>
    <title>Co-DETR</title>
    <link href="https://young-eng.github.io/YoungBlogs/2024/11/23/Co-DETR/"/>
    <id>https://young-eng.github.io/YoungBlogs/2024/11/23/Co-DETR/</id>
    <published>2024-11-23T09:05:55.000Z</published>
    <updated>2024-11-26T06:23:18.492Z</updated>
    
    <content type="html"><![CDATA[<h3 id="detrs-with-collaborative-hybrid-assignments-training1">DETRswith Collaborative Hybrid Assignments Training<sup>[1]</sup></h3><blockquote><p>作者是来自商汤的Zhuofan Zong，Guanglu Song和Yu Liu，论文引用[1]:Zong,Zhuofan et al. “DETRs with Collaborative Hybrid Assignments Training.”2023 IEEE/CVF International Conference on Computer Vision (ICCV) (2022):6725-6735.</p></blockquote><h3 id="time">Time</h3><ul><li>2023.Aug</li></ul><h3 id="key-words">Key Words</h3><ul><li>one-to-many label assignment</li></ul><h3 id="总结">总结</h3><ol type="1"><li>在本文中，作者观察到，太少的queries作为positive samples in DETR withone-to-one set matching 会导致sparse supervision on the encoder'soutput, 回影响encoer的discriminative featurelearning。为了缓解这个问题，提出了一个新的<strong>collaborative hybridassignments training scheme, 称之为Co-DETR</strong>，从versatile labelassignmentmanners学习更有效的基于DETR的检测器。这个新的训练策略能够通过训练multipleparallel auxiliary heads supervised by one-to-many label assigments suchas ATSS andFasterRCNN，简单地增强encdoer的学习能力。另外，通过其它auxiliaryheads中的positive coordinates，执行extra customized positivequeries，来提高decoder中的positivesamples的训练效率。推理的时候，这些auxiliaryheads被丢弃了，因此这个方法没有引入额外的参数和计算开销，不需要NMS。</li></ol><span id="more"></span><ol start="2" type="1"><li><p>在先前的目标检测工作中，一对多的labelassignment是它们的核心方法，每个groundtruthbox分配给检测器的输出的多个coordinates，和proposals，anchors，windowcenters共同作为监督目标。尽管它们的效果比较好，这些检测器严重依赖于很多手工设计的组件，例如NMS或者anchorgeneration。为了conduct一个更灵活的端到端的detector。有人提出了DETR，将目标检测视为一个集合预测问题，引入了一对一的setmatching。以这种方式，每个ground-truthbox将被分配给一个特定的query，不再需要很多手工设计的组件。这个方法引入了更加灵活的pipeline，使得很多DETR的变体能够进一步地提高。然而，普通的端到端的目标检测器仍然不如传统的一对多的labelassignments。</p></li><li><p>在本文中，<strong>作者尝试使DETR类型的检测器超过传统的检测器，同时保持它们端到端的特点。为了解决这个挑战，作者聚焦于一对一setmatching的intuitive drawback that it explores less positivequeries</strong>。这会导致严重的训练问题。<strong>作者从两个方面进行分析，一个是encoder的产生的latentrepresentation，然后就是decoder中的attentionlearning。首先比较Deformable DETR和one-to-many label assignment方法(where we simply replace the decoder with the ATSS head)之间的latentfeatures的discriminability score</strong>。每个spatial coordiante中的<span class="math inline">\(l^2\)</span> norm特征用来代表discriminability score。给定encoder的输出，<spanclass="math inline">\(\mathcal{F} \in \mathbb{R}^{C \times H \timesW}\)</span>，我们能够得到discriminability score map <spanclass="math inline">\(\mathcal{S} \in \mathbb{R}^{1 \times H \timesW}\)</span>。当对应区域的分数更高的时候，目标能更好的检测到。如图所示，在一些重要的位置的特征arefully activated in the 一对多的label assignment，然而less explored in一对一的set matching。对于decoder training的exploration，对于decodertraining的exploration，我们也展示了decoder中的交叉注意力分数的IoF-IoB曲线，基于DeformableDETR和Group-DETR的，这个讲more positivequeries引入到了decoder中。图2中显示，太少的positivequeries会影响attention learning，增加decoder中的positivequeries能够缓解这个。这个重要的观察驱使我们提出了一个简单高效的方法，一种协作混合的分配的训练方式，<strong>collaborativehybrid assignment training scheme,Co-DETR</strong>，co-DETR的keyinsight使用多样的一对多的labelassignments来提高encoder和decoder的训练的效率和有效性。更具体地，作者讲transformerencoder的输出和auxiliary heads进行了集成。这些heads能够通过versatile一对多的label assignments 例如ATSS，FCOS，FasterRCNN进行监督，不同的label assignments丰富了encoderoutputs的监督，使其能够be discriminativeenough来支持这些heads的训练收敛。为了进一步提高decoder的训练效率。作者对auxiliaryheads中的正样本的坐标进行编码，包括positive anchors和positiveproposals。然后送入到原始的decoder中作为多组的positivequeries，来预测pre-assigned 类别和bboxes。每个auxiliary head中的positivecoordinates作为independent group，和其它的group隔离开。versatile一对多的label assignments能够引入大量的(positive query, ground-truth)pairs，来提高decoder的训练效率。<strong>注意到，原始的decoder尽在推理的时候用到了，提出的trainingscheme尽在training的时候引入额外的overheads</strong>。</p></li><li><p><strong>一对多的labelassignment</strong>：对于目标检测中的一对多的label assignment，多个boxcandidates能够分配给相同的groundtruth box作为positive samples in thetraining。在经典的anchor-based detectors 例如Faster RCNN，RetinaNet中，sampleselection是通过预先定义的IoU的阈值和anchors与annotatedboxes之间的匹配的IoU决定。anchor-free的FCOS利用了centerpriors，将每个bbox附近的中心位置作为positives。另外，自适应的机制引入到了一对多的labelassignment，来克服固定labelassignments的局限。ATSS通过统计动态的top-k最近的anchors的IoU的值，来自适应的anchor选择。PAA以概率的方式自适应的将anchors分为positive和negativesamples。本文中，提出的collaborative hybrid assignmentscheme，能够通过auxiliary heads with 一对多的labelassignments来提高encoder representations。</p></li><li><p><strong>一对一 set matching</strong>：DETR将一对一的setmatching引入object detection，进行端到端的目标检测。这个一对一的setmatching首先通过匈牙利匹配计算global matching cost，然后为每个groundtruth box分配有着最小matching cost的正样本。DN-DETR展示了由于一对一setmatching的不稳定性，收敛慢，引入了denoisingtraining来缓解这个问题。DINO集成了DAB-DETR的queryformulation，引入了改进的contrastivedenoising来达到很好的效果。Group-DETR构建了group-wise 一对多的labelassignment来exploit 多个positive object queries，类似于hybrid matchignscheme in H-DETR。</p></li><li><p>跟着标准的DETR的路子，输入的图像送到backbone中，encoder产生latentfeatures。多个预定义的object queries在decoder中和它们进行interact viacross-attention。引入co-DETR在encoder中提高featurelearning，在decoder中通过collaborative hybrid assignmenttraining和customized positive queries generation来提高attentionlearning。</p></li><li><p><strong>Collaborative Hybrid AssignmentsTraining</strong>：为了缓解decoder中fewer positive queries造成的encoderoutput的sparse supervision，作者引入了versatile auxiliary headswith不同的一对多的label assignment，例如 ATSS, FasterRCNN。**不同的label assignments丰富了encoder outputs上的监督，能够使其bediscriminativeenough来支持这些heads的训练收敛，特别地，给定encoder的latent feature<span class="math inline">\(\mathcal{F}\)</span>，首先转换到特征金字塔via multi-scale adapter。定义 K个collaborative heads with 对应的labelassignment manners <span class="math inline">\(A_{k}\)</span>，对于第i个collaborative head, <span class="math inline">\({F_1, ...,F_j}\)</span>给到它，来得到预测 $_i，在第 i个head， <spanclass="math inline">\(A_i\)</span>用来计算supervised targets for thepositive and negative samples in <spanclass="math inline">\(P_i\)</span>，将G记为 groundtruthset，这个过程formulated as: <spanclass="math display">\[\mathbf{P}_{i}^{\{pos\}},\mathbf{B}_{i}^{\{pos\}},\mathbf{P}_{i}^{\{neg\}}=\mathcal{A}_{i}(\mathbf{\hat{P}}_{i},\mathbf{G}),\]</span>{pos}和{neg}表示pair set of (j, positive coordinates or negativecoordinates in <span class="math inline">\(\mathcal{F}_j\)</span>)determined by <span class="math inline">\(A_i\)</span>。j表示featureindex in <span class="math inline">\({F_1, ..., F_j}\)</span>. <spanclass="math inline">\(B^{pos}_i\)</span> 是spatial positivecoordinates的集合。<span class="math inline">\(P^{pos}_i\)</span>和<spanclass="math inline">\(P^{neg}_i\)</span> 是对应坐标的supervisedtargets，包括类别和regressed offsets。损失函数定义如下：</p><p><spanclass="math display">\[\mathcal{L}_{i}^{enc}=\mathcal{L}_{i}(\mathbf{\hat{P}}_{i}^{\{pos\}},\mathbf{P}_{i}^{\{pos\}})+\mathcal{L}_{i}(\mathbf{\hat{P}}_{i}^{\{neg\}},\mathbf{P}_{i}^{\{neg\}}),\]</span></p><p>注意到，对于负样本，回归损失没有用上，对于K个auxiliaryheads，优化的训练目标是： <spanclass="math display">\[\mathcal{L}^{enc}=\sum_{i=1}^K\mathcal{L}_i^{enc}\]</span></p></li><li><p><strong>Customized Positive QueriesGeneration</strong>：在一对一的set matching范式中，每个groundtruthbox会被唯一分配给一个特定的query作为supervised target。太少的positivequeries导致decoder中的交叉注意力学习不够。为了减缓这个，根据每个auxiliaryhead中的label assignment，产生足够的customized positivequeries。特别低，给定positive coordinates set，<spanclass="math inline">\(\mathbf{B}_{i}^{\{pos\}}\in\mathbb{R}^{M_{i}\times4}\)</span>在第i个auxiliary head，<spanclass="math inline">\(M_i\)</span>是positivesamples的数量，额外的positive queries <spanclass="math inline">\(\mathbf{Q}_{i} \in \mathbb{R}^{M_{i}\timesC}\)</span> 可以通过以下公式生成：</p><p><spanclass="math display">\[\mathbf{Q}_{i}=\mathrm{Linear}(\mathrm{PE}(\mathbf{B}_{i}^{\{pos\}}))+\mathrm{Linear}(\mathrm{E}(\{\mathcal{F}_{*}\},\{pos\})).\]</span></p><p>PE表示positional encodings，根据index pair从E选择对应的features</p><p>因此，这里有K+1组 queries，constribute to a single 一对一的setmatching branch，在训练中的K个branches with 一对多的 labelassignments。auxiliary 一对多的label assignment branches和L decoderslayers共享 相同的参数，auxiliary branch中的queries视为positivequeries，然后matching process被丢弃。具体地，第 <spanclass="math inline">\(l\)</span>层的decoder layer的loss in the <spanclass="math inline">\(i\)</span> auxiliary branch，可以被formulate为：<spanclass="math display">\[\mathcal{L}_{i,l}^{dec}=\widetilde{\mathcal{L}}(\widetilde{\mathbf{P}}_{i,l},\mathbf{P}_{i}^{\{pos\}}).\]</span><span class="math inline">\(\mathbf{P}_{i,l}\)</span>指第 <spanclass="math inline">\(i\)</span>个auxiliary branch的第 <spanclass="math inline">\(l\)</span>个decoderlayer的输出的预测。对于Co-DETR，训练目标为：</p><p><spanclass="math display">\[\mathcal{L}^{global}=\sum_{l=1}^{L}(\widetilde{\mathcal{L}}_{l}^{dec}+\lambda_{1}\sum_{i=1}^{K}\mathcal{L}_{i,l}^{dec}+\lambda_{2}\mathcal{L}^{enc}),\]</span><spanclass="math inline">\(\mathcal{L}_{l}^{dec}\)</span>表示原始一对一matchingbranch中的Loss，<span class="math inline">\(\lambda_1\)</span> 和 <spanclass="math inline">\(\lambda_2\)</span> 是平衡loss的参数。</p></li><li><p><strong>为什么Co-DETR有效</strong>：Co-DETR在DETR基础的检测器上有很大的提升。接下来，作者尝试调研它的有效性。</p><ul><li><strong>Enrich the encoder'ssupervisions</strong>：直觉上，太少的positive queries会导致sparsesupervisions，因为仅有一个query is supervised by regression loss foreach ground-truth。一对多的label assignment中的正样本receive morelocalization supervisions，来帮助增强latent featurelearning。为了进一步探索稀疏的监督如何阻碍了模型的训练。作者引入了IoF-IoB曲线，来量化encoderoutput的discriminability score，特别低，给定encoder的latent feature<spanclass="math inline">\(\mathcal{F}\)</span>，受特征可视化的启发，计算IoF和IoB。</li><li><strong>通过降低匈牙利匹配的不稳定性，提高交叉注意力学习</strong>：匈牙利匹配时一对一setmatching的核心，交叉注意力是用来帮助positive queries encoder abundantobjectinformation的一个重要操作。它需要大量的训练来实现。作者观察到匈牙利匹配引入了不可控的不稳定性，因为在训练过程中，同一张图像里的groundtruth分配给一个特定的positivequery是挑战的。作者发现他们的方式，能够导致一个更加稳定的matching过程。另外，为了量化交叉注意力是如何被优化的，计算了IoF-IoB曲线for attention score。类似于feature discriminability scorecomputation，对于attentionscore，设置不同的阈值，来得到多个IoF-IoB对。</li></ul></li><li><p>和其它方法的对比：Group-DETR，H-DETR,SQR通过重复的groups和groundtruthboxes的一对一的matchign来实现一对多的分配。Co-DETR显示地为groundtruth分配多个spatial coordintes aspositives。相应的，这些密集的supervision signals会直接递应用在latentfeaturemap上，使其更加的discriminative。相比之下，Group-DETR，H-DETR，SQR缺乏这个机制。虽然这些方法引入了positivequeries，通过匈牙利匹配的一对多的分配仍然会有一对一匹配的不稳定的问题。作者的方法受益于<strong>off-the-shelf一对多的分配的稳定性</strong>，集成了它们的specificmatching manner between positive queries and ground-truthboxes。Group-DETR,H-DETR没有reveal一对一的分配和传统一对多的分配的complementarities。作者给出了定量和定性的分析，为了帮助更好地理解它们之间的不同和互补，因此能够通过利用off-the-shelf一对多的分配设计，不需要额外特别的一对多的设计，提高DETR的学习能力。作者的方法只处理decoder中的positive coordinates。</p></li></ol><p><imgsrc="https://figures.semanticscholar.org/3d3a76a2fedccea741f34303b06358440dc2a212/3-Figure4-1.png"alt="Framework" /> <span class="math inline">\(Fig.1^{[1]}\)</span>Framework of Collaborative Hybrid Assignment Training. The auxiliarybranches are discarded during evaluation.</p>]]></content>
    
    
    <summary type="html">&lt;h3 id=&quot;detrs-with-collaborative-hybrid-assignments-training1&quot;&gt;DETRs
with Collaborative Hybrid Assignments Training&lt;sup&gt;[1]&lt;/sup&gt;&lt;/h3&gt;
&lt;blockquote&gt;
&lt;p&gt;作者是来自商汤的Zhuofan Zong，Guanglu Song和Yu Liu，论文引用[1]:Zong,
Zhuofan et al. “DETRs with Collaborative Hybrid Assignments Training.”
2023 IEEE/CVF International Conference on Computer Vision (ICCV) (2022):
6725-6735.&lt;/p&gt;
&lt;/blockquote&gt;
&lt;h3 id=&quot;time&quot;&gt;Time&lt;/h3&gt;
&lt;ul&gt;
&lt;li&gt;2023.Aug&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id=&quot;key-words&quot;&gt;Key Words&lt;/h3&gt;
&lt;ul&gt;
&lt;li&gt;one-to-many label assignment&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id=&quot;总结&quot;&gt;总结&lt;/h3&gt;
&lt;ol type=&quot;1&quot;&gt;
&lt;li&gt;在本文中，作者观察到，太少的queries作为positive samples in DETR with
one-to-one set matching 会导致sparse supervision on the encoder&#39;s
output, 回影响encoer的discriminative feature
learning。为了缓解这个问题，提出了一个新的&lt;strong&gt;collaborative hybrid
assignments training scheme, 称之为Co-DETR&lt;/strong&gt;，从versatile label
assignment
manners学习更有效的基于DETR的检测器。这个新的训练策略能够通过训练multiple
parallel auxiliary heads supervised by one-to-many label assigments such
as ATSS and
FasterRCNN，简单地增强encdoer的学习能力。另外，通过其它auxiliary
heads中的positive coordinates，执行extra customized positive
queries，来提高decoder中的positive
samples的训练效率。推理的时候，这些auxiliary
heads被丢弃了，因此这个方法没有引入额外的参数和计算开销，不需要NMS。&lt;/li&gt;
&lt;/ol&gt;</summary>
    
    
    
    <category term="Papers" scheme="https://young-eng.github.io/YoungBlogs/categories/Papers/"/>
    
    
    <category term="object detection" scheme="https://young-eng.github.io/YoungBlogs/tags/object-detection/"/>
    
  </entry>
  
  <entry>
    <title>BMViT</title>
    <link href="https://young-eng.github.io/YoungBlogs/2024/10/28/BMViT/"/>
    <id>https://young-eng.github.io/YoungBlogs/2024/10/28/BMViT/</id>
    <published>2024-10-28T09:27:50.000Z</published>
    <updated>2024-11-06T13:50:33.937Z</updated>
    
    <content type="html"><![CDATA[<h3id="multiscale-vision-transformers-meet-bipartite-matching-for-efficient-single-stage-action-localization1">MultiscaleVision Transformers meet Bipartite Matching for efficient single-stageAction Localization<sup>[1]</sup></h3><blockquote><p>作者是来自英国玛丽女王大学、三星剑桥AI center等机构的Ioanna Ntinou,Enrique Sanchez, Georgios Tzimiropoulos。论文引用[1]:Ntinou, Ioanna,Enrique Sanchez, and Georgios Tzimiropoulos. "Multiscale visiontransformers meet bipartite matching for efficient single-stage actionlocalization." Proceedings of the IEEE/CVF Conference on Computer Visionand Pattern Recognition. 2024.</p></blockquote><h3 id="time">Time</h3><ul><li>2024.May</li></ul><h3 id="key-words">Key Words</h3><ul><li>bipartite matching loss</li><li>Video Transformer with bipartite matching loss without learnablequeries and decoder</li></ul><h3 id="总结">总结</h3><ol type="1"><li>Actionlocalization是一个挑战行的问题，结合了检测和识别，通常是分开处理的，SOTA方法都是依赖off-the-shelfbboxes detection，然后用transformer model来聚焦于classification task。这样的两阶段的方法不利于实时的部署。另外，单节段的方法通过共享大部分的负载来实现这两个任务，牺牲性能换取速度。类似DETR的架构训练起来有挑战。本文观察到：一个直接的bipartitematching loss可以用在ViT的output tokens上，导致一个 backbone + MLP架构能够需要要额外的encoder-decoder head和learnablequeries来同时处理这两个任务。用单一的MViTv2-S架构 with bipartitematching 来执行两个tasks，超过了MViTv2-S trained with RoI align onpre-computed bboxes。用设计的tokenpooling和提出的训练的pipeline，<strong>B</strong>ipartite-<strong>M</strong>atchingVision Transformer, <strong>BMViT</strong>。实现了很好的结果。</li></ol><span id="more"></span><ol start="2" type="1"><li><p>STAD任务和目标检测类似，但是特殊之处在于，检测的目标通常是人，有很多个不同的类别有时同时发生。提出了额外的挑战：actions需要时序reasoning；如果人物没有执行目标的行为，persondetector可能导致假阳性。目前的SOTA方法有很高的mAP，是将detectiontask给到了预训练的FasterRCNN；然后只聚焦于网络模型和大的训练数据。本文提出的模型能够同时执行两个任务。因为和目标检测类似，最近的针对单节段的模型都是建立在strongbackbone上，能够为DETR提供强大的时序特征。基于DETR的方法展示了用于end-to-end的actionlocalization的高效性。它们的设计用了videobackbone和一个encoder-decoder的transformer。在网络设计上，是否还有提升？从最近的 <strong>Open-World Object Detection usingViT(OWL-ViT)</strong>得到了灵感。贡献如下：</p><ul><li>bipartite matching loss between saptio-temporal output embeddings ofa single transformer backbone and the GT instances in a video clip.</li></ul><p>在这个设定中，videoembeddings是独立的tokens，可以类似于DETR那样matched topredictions。也就意味着：learnable tokens是不需要的；transformerdecoder也是不需要的；video backbone和encoder可以merge into一个strongvideo transformer，这样一个简单的approach with a careful tokenselection使得能够训练MViTv2-S with a simple MLP head，来直接预测boundingboxes和actionclasses。不需要额外的elements或者数据，<strong>单节段的MViTv2-S超过了两阶段的MViTv2-Strained using RoI align and pre-computed boundingboxes</strong>。</p></li><li><p><strong>Two-stage</strong>:</p><ul><li><p>目前大多数现有的action detection的工作都是依赖于额外的persondetector for actorlocalization。典型的就是FasterRCNN-R101-FPN，在COCO上训练，然后在AVA进行微调，用在STAD任务中，通过引入off-the-shelfdetection,STAD的任务简化了成了一个actionclassification的问题。之前的方法SlowFast、MViT、VideoMAE、Hiera，这里的RoIfeatures都是直接用于actionclassification。然而，这样的features将信息限制在boundingbox之类。忽略的contextualinformation。为了解决这个问题，AIA和ACARN用了一个额外的module来捕捉actor和context/otheractor之间的interactin。另外，为了建模时序信息，MeMViT基于MViT引入了<strong>mermorymechanism</strong>。实现了很高的精度。这些方法对于部署不高效。</p></li><li><p><strong>Single-stage</strong>：受之前的工作的启发，一些工作开始尝试同时处理detection和classificationin a unified framework。一些工作借鉴了目标检测的思路，然后引入actiondetection。其它的通过joint actor prposal和action classificationnetworks来简化训练，或者将其吸引到TAL任务上；SE-STAD是基于FasterRCNN，将其引入actionclassification。Video action transformer是一个transformer风格的actiondetector，用来汇聚spatial-temporal context around targetactors。最近的工作利用DETR，用learnablequeries来建模action和bboxes。TubeR提出了一个基于DETR的架构，一些列的queries，coinedTubelet Queries，同时编码temporal dynamics of a specific actor'sbounding box as well as 对应的actions。TubeR用单一的DETRhead来建模TubeletQueries。类似地，STMixer，提出了一个自适应地，从multi-scalespatio-temporal feature space来采样discriminativefeatures，然后用自适应的策略under the guidance ofqueries来对它们进行编码。EVAD用了两个video action detectiondesigns：token dropout focusing on keyframe-centric spatiotemporalpreservation; Scene context refinement using RoI align operation and adecoder；和之前的工作相比，用一个decoder或者一个heavymodule，引入了interaction features of context or other actors。</p></li></ul></li><li><p>Action localization的目标是检测和分类videoclip中间帧的一些actions。因为video clip中，不是每个person都在执行actionof interest。将一个person和actor区分开，<spanclass="math inline">\(t\)</span>时刻的actor 定义为一个bbox：$<spanclass="math inline">\(b = [x_b, y_c, h, w]\)</span>。</p></li><li><p>DETR的在训练的过程中，<strong>用Hungarian 算法来将 <spanclass="math inline">\(N &lt; L\)</span> 的(L为图像中的objects)映射到最近的predictions，使得assignments能够最小化bbox和class cost；<span class="math inline">\(L - N\)</span>剩下的predictions分配给emptyclass。通过反向传播bbox和class error for those outputs matched to a GTobject and only the class error for assigned to the emptyclass</strong>。DETR有很好的特性 for actionlocalization。因为<strong>learnable object queries能够convey objectlocalization和action classification</strong>。</p></li><li><p>作者followOWL-ViT的工作，OWL-ViT引入了CLIP的encoder，最后的poolinglayer去掉了。每个output embeddings 被forward to一个小的head，包含了一个box MLP和class linear projection。因此，<spanclass="math inline">\(\hat{L} = h \times w\)</span> output tokens fromvision encoder，作为独立的output pairs <spanclass="math inline">\(&lt;\hat{b},p(\hat{a})&gt;\)</span>。这些pairs用DETR的Bipartite matchingloss和GT进行match。为了改进OWL-ViT，注意到MViT是一个natural pool ofspatio-temporal output embeddings that can be one-to-one matched totriplets。一个简单的用了bipartitematching进行训练的MViTv2-S架构比相同给的MViTv2-S精度要更高。称这个方法为<strong>Bipartite-MatchingVisionTransformer,BMViT</strong>。给定简单的MLPs的heads。不需要给backbone增加额外的复杂度。</p></li><li><p>对于一个 <span class="math inline">\(16 \times 256 \times256\)</span> 的视频输入，输出为 <span class="math inline">\(8 \times 8\times 8\)</span>的tokens，超过了基于DETR的架构，这个输出的tokens不回影响网络的复杂度，因为被3个MLPs独立地处理。FollowingOWL-ViT，增加了一个bias to predicted boundingboxes，使得每个默认是在image patch的中心，与2D grid的一致。"there is nostrict correspondence between image patches and tokens representations";然而，<strong>bias boxprediction能够加速训练和提高最后的性能</strong>。在3D时空设定中，沿着时间轴增加了相同的 2D bias to all tokens on the same 2Dgrid。</p></li><li><p>因为每个head会独立地处理output tokens来产生 <spanclass="math inline">\(\hat{b}, p(\alpha),p(\hat{a})\)</span>。可以选择哪些是能够更好地用于检测和识别的子任务。唯一的技术限制是每个head的输出需要和其它head的输出一对一的对应，来形成<strong>triplets(will be matched to the ground-truthinstances)</strong>。这是一个很重要的考虑，因为actor detection和actionclassification在定义上是opposed。当检测actors的时候，只需要<strong>中间帧</strong>的信息，actionrecognition的任务从使用时序支持上收益。因为我们没有局限于使用相同的输出tokensfor each task，考虑 <span class="math inline">\(w \times h\)</span>tokens和 <span class="math inline">\(t = \lfloor\frac{T}{2}\)</span>，来产生 <span class="math inline">\(\hat{L} = w\times h\)</span> bounding boxes <spanclass="math inline">\(\hat{b}\)</span>和 actor/no actor的概率 <spanclass="math inline">\(p(\alpha)\)</span>, 然后应用temporalpooling来产生一个 <strong>equivalent set of</strong> <spanclass="math inline">\(\hat{L}\)</span> tokens，能够用来计算action 的概率<span class="math inline">\(p(\hat{a})\)</span>。</p></li><li><p>首先，为了避免当使用MViTv2-S的时候的poordetection，去掉最后的pooling layer来增加output resolution，来产生outputtokens，然后用central tokens用于detection task。应用<strong>temporalpooling</strong>来形成equivalent spatio-temporaltokens用于分类任务。也要研究了alternative strategy，考虑boundingboxes和actor prediction, 对应于 <span class="math inline">\(t= \lfloor\frac{T}{2}\)</span> 和 <span class="math inline">\(t = \lceil\frac{T}{2}\)</span>。将其结果进行concatenate得到512个tokens，来进行actor detection task。在past和futuretokens上应用 temporal pooling w.r.t中间帧。就是说，将两者进行concatenate形成最后的512tokens。注意到actor和action tokens需要一对一的对应。是产生 <spanclass="math inline">\(\hat{L}\)</span> triplets的必要条件。</p></li><li><p>结论：Vision Transformer的输出的tokens能够独立地forwarded to对应的MLP heads，来产生固定序列的predictions类似于DETR，用Bipartitematchingloss来直接训练backbone，不需要牺牲性能来执行两个tasks是可能的。结果表明简单的模型能够实现和两阶段的方法类似的精度。</p></li></ol><p><imgsrc="https://figures.semanticscholar.org/60462808feec1aaecc49127b11eb794c3d8de7fd/2-Figure1-1.png"alt="Comparison" /> <span class="math inline">\(Fig.1^{[1]}\)</span>Comparison between existing works and our proposed approach. (a)Traditional two-stage methods work on developing strong visiontransformers that are applied in the domain of Action Localization byoutsourcing the bounding box detections to an external detector. ROIAlign is applied to the output of the transformer using the detectedbounding boxes, and the pooled features are forwarded to an MLP thatreturns the class predictions. (b) Recent approaches in one-stage ActionLocalization leverage on the DETR capacity to model both the boundingboxes and the action classes. A video backbone produces strongspatio-temporal features that are handled by a DETR transformer encoder.A set of learnable queries are then used by a DETR transformer decoderto produce the final outputs. (c) Our method builds a vision transformeronly that is trained against a bipartite matching loss between theindividual predictions given by the output spatio-temporal tokens andthe ground-truth bounding boxes and classes. Our method does not needlearnable queries, as well as a DETR decoder, and can combine thebackbone and the DETR encoder into a single architecture.</p><p><imgsrc="https://figures.semanticscholar.org/60462808feec1aaecc49127b11eb794c3d8de7fd/5-Figure2-1.png"alt="tokens" /> <span class="math inline">\(Fig.2^{[1]}\)</span> Theoutput spatio-temporal tokens are fed to 3 parallel heads. We use thecentral tokens to predict the bounding box and the actor likelihoodwhile averaging the output tokens over the temporal axis to generate theaction tokens. Each head comprises a small MLP that generates the outputtriplets. We depict the flow diagram for each head, following thestandard OWL-ViT head</p>]]></content>
    
    
    <summary type="html">&lt;h3
id=&quot;multiscale-vision-transformers-meet-bipartite-matching-for-efficient-single-stage-action-localization1&quot;&gt;Multiscale
Vision Transformers meet Bipartite Matching for efficient single-stage
Action Localization&lt;sup&gt;[1]&lt;/sup&gt;&lt;/h3&gt;
&lt;blockquote&gt;
&lt;p&gt;作者是来自英国玛丽女王大学、三星剑桥AI center等机构的Ioanna Ntinou,
Enrique Sanchez, Georgios Tzimiropoulos。论文引用[1]:Ntinou, Ioanna,
Enrique Sanchez, and Georgios Tzimiropoulos. &quot;Multiscale vision
transformers meet bipartite matching for efficient single-stage action
localization.&quot; Proceedings of the IEEE/CVF Conference on Computer Vision
and Pattern Recognition. 2024.&lt;/p&gt;
&lt;/blockquote&gt;
&lt;h3 id=&quot;time&quot;&gt;Time&lt;/h3&gt;
&lt;ul&gt;
&lt;li&gt;2024.May&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id=&quot;key-words&quot;&gt;Key Words&lt;/h3&gt;
&lt;ul&gt;
&lt;li&gt;bipartite matching loss&lt;/li&gt;
&lt;li&gt;Video Transformer with bipartite matching loss without learnable
queries and decoder&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id=&quot;总结&quot;&gt;总结&lt;/h3&gt;
&lt;ol type=&quot;1&quot;&gt;
&lt;li&gt;Action
localization是一个挑战行的问题，结合了检测和识别，通常是分开处理的，SOTA方法都是依赖off-the-shelf
bboxes detection，然后用transformer model来聚焦于classification task。
这样的两阶段的方法不利于实时的部署。另外，单节段的方法通过共享大部分的负载来实现这两个任务，牺牲性能换取速度。类似DETR的架构训练起来有挑战。本文观察到：一个直接的bipartite
matching loss可以用在ViT的output tokens上，导致一个 backbone + MLP
架构能够需要要额外的encoder-decoder head和learnable
queries来同时处理这两个任务。用单一的MViTv2-S架构 with bipartite
matching 来执行两个tasks，超过了MViTv2-S trained with RoI align on
pre-computed bboxes。用设计的token
pooling和提出的训练的pipeline，&lt;strong&gt;B&lt;/strong&gt;ipartite-&lt;strong&gt;M&lt;/strong&gt;atching
Vision Transformer, &lt;strong&gt;BMViT&lt;/strong&gt;。实现了很好的结果。&lt;/li&gt;
&lt;/ol&gt;</summary>
    
    
    
    <category term="Papers" scheme="https://young-eng.github.io/YoungBlogs/categories/Papers/"/>
    
    
    <category term="Action Detection" scheme="https://young-eng.github.io/YoungBlogs/tags/Action-Detection/"/>
    
  </entry>
  
  <entry>
    <title>MRSN</title>
    <link href="https://young-eng.github.io/YoungBlogs/2024/10/24/MSRN/"/>
    <id>https://young-eng.github.io/YoungBlogs/2024/10/24/MSRN/</id>
    <published>2024-10-24T14:00:47.000Z</published>
    <updated>2024-10-24T14:02:40.715Z</updated>
    
    <content type="html"><![CDATA[<h3id="mrsn-multi-relation-support-network-for-video-action-detection1">MRSN:Multi-Relation Support Network for Video ActionDetection<sup>[1]</sup></h3><blockquote><p>作者是来自南大的YinDong Zheng, Guo Chen, MingleiYuan等。论文引用[1]:</p></blockquote><h3 id="time">Time</h3><ul><li>2023.Apr</li></ul><h3 id="key-words">Key Words</h3><h3 id="总结">总结</h3><ol type="1"><li></li></ol>]]></content>
    
    
      
      
    <summary type="html">&lt;h3
id=&quot;mrsn-multi-relation-support-network-for-video-action-detection1&quot;&gt;MRSN:
Multi-Relation Support Network for Video Action
Detection&lt;</summary>
      
    
    
    
    <category term="Papers" scheme="https://young-eng.github.io/YoungBlogs/categories/Papers/"/>
    
    
    <category term="Action Detection" scheme="https://young-eng.github.io/YoungBlogs/tags/Action-Detection/"/>
    
  </entry>
  
  <entry>
    <title>STAR</title>
    <link href="https://young-eng.github.io/YoungBlogs/2024/10/24/STAR/"/>
    <id>https://young-eng.github.io/YoungBlogs/2024/10/24/STAR/</id>
    <published>2024-10-24T09:15:19.000Z</published>
    <updated>2024-11-07T07:41:38.392Z</updated>
    
    <content type="html"><![CDATA[<h3id="end-to-end-spatio-temporal-action-localisation-with-video-transformers1">End-to-EndSpatio-Temporal Action Localisation with VideoTransformers<sup>[1]</sup></h3><blockquote><p>作者是来自google的Alexey Gritsenko, XuehanXiong等人，论文引用[1]:Gritsenko, Alexey A. et al. “End-to-EndSpatio-Temporal Action Localisation with Video Transformers.” 2024IEEE/CVF Conference on Computer Vision and Pattern Recognition (CVPR)(2023): 18373-18383.</p></blockquote><h3 id="time">Time</h3><ul><li>2023.Apr</li></ul><h3 id="key-words">Key Words</h3><ul><li>without resorting to external proposals or memory banks</li><li>directly predicts tubelets even do not have full tubeletannotations</li></ul><h3 id="总结">总结</h3><ol type="1"><li>最好的时空行为检测使用额外的person proposals和复杂的external memorybanks。作者提出了一个端到端的、纯transformer的模型，能够直接输入一个视频，输出tubelets(一系列的bboxes和actionclasses at each frame). 这个灵活的模型能够用<strong>稀疏的bboxsupervision on individual frames or full tubeletannotations</strong>。在这两种情况下，预测连贯的tubelet作为输出。另外，这个模型不需要额外的对proposals的前处理或者NMS这样的后处理。</li></ol><span id="more"></span><ol start="2" type="1"><li><p>STAD/STAL任务通常有两种处理方式：</p><ul><li>在给定相邻帧作为时空上下文的时候，在单一的keyframe预测bbox和actions</li><li>预测一系列的bboxes和actions(tubes) for each actor at each frame inthe video. 最强的模型，是基于keyframe来做的，用一个两阶段的pipelineinspired byFastRCNN：首先用一个单独的检测器来得到proposals，proposals中的features然后根据actionsof interest进行汇聚和分类。这些模型需要很多的memorybanks，来包含其它帧的上下文信息。<strong>proposal-free</strong>算法，不需要额外的persondetectors，可以用于keyframe-level和tubelet-level，它们的性能落后于proposal-based的方法。这里，作者展示了端到端的可训练的模型超过了两阶段的approach。</li></ul></li><li><p>STAR包含一个pure-transformer，是基于DETR检测的模型，这个模型是端到端，不需要前处理或者后处理，相比于之前的大多数的工作。<strong>这个模型的初始阶段是一个visionencoder，紧接着是一个decoder，将可学习的latent queries(这个代表每个actorin the video in video)处理成outputtubelets(tubelet是一系列的bboxes和action classes at each time step ofinput videoclip)</strong>。这个模型是多种功能的，能够用<strong>fully-labeled tubeannotations</strong>，或者 <strong>sparse keyframeannotations</strong>进行训练。后者，这个网络仍然能够预测tubelets，学习将actor的检测联系起来。这个behaviour是由<strong>factorisedqueries，解码器的结构和tubelet matching in loss which all containtemporal inductivebiases</strong>促成的。两阶段的先得到detections计算proposals，然后进行行为的分类，这种方法在AVA这种数据集上表现很好。之前的一些proposal-free的方法基于目标检测的架构例如SSD,CenterNet, YOLO等，它们被proposal-based方法所超越；另外，这些方法包含了一个separatenetwork，一个是学习视频表征，一个是学习proposals forkeyframe，不能预测tubelets。这个工作最像TubeR，作者的这个模型是基于DETR，是一个纯的transformer，不同于TubeR，展示了这个模型也能够预测tubelets。</p></li><li><p><strong>Encoder</strong>: 这个模型包含一个visionencoder，紧接着是一个decoder将学习到的querytokens处理成tubelets。吸收temporal inductive biases intodecoder，来提高精度和tubelet prediction。这个模型是受DETR架构的启发，也是用set-basedloss和匈牙利匹配来训练。backbone输入一个视频，产生输入视频的featurerepresentation。用了ViViT factorised encoder，这个spatio-temporaldimensions依赖于patch size when tokenising theinput。为了保持spatio-temporalinformation，去掉了原始transformer中的spatial-和temporal-aggregationsteps，如果temporal patch size大于1，沿着时间轴对最后一个featuremap进行双线性上采样，保持原始的时序resolution。</p></li><li><p><strong>Tubelet Decoder</strong>：decoder处理视觉特征，<spanclass="math inline">\(x \in \mathbb{R}^{T\times h\times w\timesc}\)</span> along with learned queries。<span class="math inline">\(q\in \mathbb{R}^{T\times S\times d}\)</span> to outputs tubelets, <spanclass="math inline">\(y=(b,a)\)</span>是一系列的bboxes。<spanclass="math inline">\(b \in \mathbb{R}^{T\times S\times4}\)</span>和对应的actions， <span class="math inline">\(a \in\mathbb{R}^{T\times S\times C}\)</span>。这里 <spanclass="math inline">\(S\)</span>表示每帧的最大的数量的bboxes。<spanclass="math inline">\(C\)</span> 表示输出类别的数量。将learnedqueries用transformerdecoder解码成输出的detections。总的来说，这个decoder包含 <spanclass="math inline">\(L\)</span>layers，每个对queries执行自注意力操作，在queries和encdoeroutputs之间进行交叉注意力操作。这里作者修改了queries，自注意力和交叉注意力 for spatio-temporal localizationscenario，加入了额外的<strong>temporal inductivebiases</strong>，来提高精度。</p></li><li><p><strong>Queries</strong>：Qeries, q, in DETR，用encoderd visualfeatures解码成 bboxespredictions，类似于其它检测架构中的<strong>anchors</strong>。定义queries的最直接的方式是<strong>随机初始化</strong><spanclass="math inline">\(q \in \mathbb{R}^{T\times S\timesd}\)</span>。然而，作者找到了一个更有效的方式来factorise queries intoseparate learned spatial (<span class="math inline">\(q \in\mathbb{R}^{S\times d}\)</span>) and temporal(<spanclass="math inline">\(q \in \mathbb{R}^{T\timesd}\)</span>)。为了得到最后的tubelet queries，<strong>简单地重复spatialqueries across all frames, 然后将它们加到它们对应的temporal embedding ateach location中</strong>。如图所示，更准确地说，<spanclass="math inline">\(\mathbf{q}_{ij} =\mathbf{q}_i^t+\mathbf{q}_j^s\)</span>，这里<spanclass="math inline">\(i\)</span>和 <spanclass="math inline">\(j\)</span>分别表示temporal和spatialindices。这个factorised query representation表示相同的spatialembedding在所有帧中都在使用。直觉上来说，这个encourage <spanclass="math inline">\(i^{th}\)</span> spatial query embedding, <spanclass="math inline">\(q_{i}^s\)</span>来bind to the same location acrossdifferent frames of thevideo。因为物体在不同的帧之间会有位移，对于将bboxes连接到一起可能有帮助。</p></li><li><p><strong>Decoder layer</strong>：原始transformer中的decoderlayer包含自注意力 on the queries, <spanclass="math inline">\(q\)</span>，紧接着是 queries and the outputs ofthe encoder, x之间的交叉注意力，然后是一个MLPlayer。在这个模型中，将自注意力和交叉注意力在时间和空间上进行分解，引入了一个temporallocality inductivebias，增加了模型的效率。具体地说，应用MHSA，首先计算queries,keys和values。</p></li><li><p><strong>localisation and classificationheads</strong>：得到了网络最后的预测，<spanclass="math inline">\(y=(b,a)\)</span>，通过应用一个小的feed-forward网络来输出to decoder，这个bboxes序列，通过3层的MLP获得，一个单层的liearprojection用来得到class logits。当每一帧预测固定数量的 <spanclass="math inline">\(S\)</span> bboxes的时候，<spanclass="math inline">\(S\)</span>是比frame中的GT的数量的最大值都大，也引入了额外的classlabel，代表背景</p></li><li><p>模型直接预测bboxes和action classes at each frame of the inputvideo。在很多数据集上例如AVA，是稀疏标注，只标注了关键帧。为了利用可用的annotations，计算trainingloss only at the annotated frames of the video。set-based检测模型例如DETR能够以任意的order进行predictions，这就是为什么在计算trainingloss之前先将predictions和ground进行match。这里有两种matching的方式：</p><ul><li>独立地执行bipartite matching at each frame to align model'spredictions to the ground truth before computing the loss.</li><li>另一个是执行tubelet matching，所有的有着相同spatial index的queries<span class="math inline">\(q^s\)</span> 必须和相同的ground truth acrossall frames of the input video。直觉上来看，当有full tubeletannotations的时候，tubeletmatching提供了更强的监督信息，注意到不管执行哪种matching，这个损失的计算和整个的模型架构是一样的。</li></ul></li><li><p>这个思路是基于DETR的，不需要额外的proposals或者NMS来做后处理，用DETR的思路foractionlocalization已经被TubeR和WOO采用了，然而，不同之处在于：WOO不检测tubelets，只有中间的keyframe的actions。**将queries在空间和时间上进行分解，来提供inductivebiases。另外，预测action classes separately for each time step in thetubelet, 意味着每个queries binds to an actor in the video. 相反，TubR，对比之下，将queries进行参数化，以至于它们能够和separateactions进行关联(features在tubelet上进行average pool，然后linearlyclassified into a single actionclass)。这个choice也意味着TubeR需要一个额外的action switchhead来预测tubelets什么时候开始和结束，作者这里就不需要了，因为tubelet中不同的时间step有不同的actionclasses。另外，考虑两种不同的matching in the loss computation unlikeTubeR，tubelet matching设计用来预测更多的时序连续的tubelets。相比于TubeR，实验性地表明decoder的设计允许这个模型能够精确地预测tubelets，即使有weak,keyframe supervision。最后TubeR要求额外的复杂度(short-term contextmodule和额外的memory bank)，</p></li></ol><p><imgsrc="https://figures.semanticscholar.org/254ddb66ae10e6ad263e34a7bb9036e903f26be0/3-Figure2-1.png"alt="Overview" /> <span class="math inline">\(Fig.1^{[1]}\)</span> Ourmodel processes a fixed-length video clip, and for each frame, outputstubelets (i.e. linked bounding boxes with associated action classprobabilities). It consists of a transformer-based vision encoder whichoutputs a video representation, x RT h w d. The video representation,along with learned queries, q (which are factorised into spatial qs andtemporal components qt) are decoded into tubelets by a decoder of Llayers followed by shallow box and class prediction heads</p><p><imgsrc="https://figures.semanticscholar.org/254ddb66ae10e6ad263e34a7bb9036e903f26be0/4-Figure3-1.png"alt="Factorised SA" /> <span class="math inline">\(Fig.2^{[1]}\)</span>Our decoder layer consists of factorised self-attention(SA)(left) andcross-attention(CA)(right) operations designed to provide aspatio-temporal inductive bias and reduce computation. Both operationsrestrict attention to the same spatial and temporal slices as the querytoken, as illustrated by the receptive field(blue) for a given querytoken(magenta). Factorised SA consists of two operations, whilst inFactorised CA, there is one operation.</p>]]></content>
    
    
    <summary type="html">&lt;h3
id=&quot;end-to-end-spatio-temporal-action-localisation-with-video-transformers1&quot;&gt;End-to-End
Spatio-Temporal Action Localisation with Video
Transformers&lt;sup&gt;[1]&lt;/sup&gt;&lt;/h3&gt;
&lt;blockquote&gt;
&lt;p&gt;作者是来自google的Alexey Gritsenko, Xuehan
Xiong等人，论文引用[1]:Gritsenko, Alexey A. et al. “End-to-End
Spatio-Temporal Action Localisation with Video Transformers.” 2024
IEEE/CVF Conference on Computer Vision and Pattern Recognition (CVPR)
(2023): 18373-18383.&lt;/p&gt;
&lt;/blockquote&gt;
&lt;h3 id=&quot;time&quot;&gt;Time&lt;/h3&gt;
&lt;ul&gt;
&lt;li&gt;2023.Apr&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id=&quot;key-words&quot;&gt;Key Words&lt;/h3&gt;
&lt;ul&gt;
&lt;li&gt;without resorting to external proposals or memory banks&lt;/li&gt;
&lt;li&gt;directly predicts tubelets even do not have full tubelet
annotations&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id=&quot;总结&quot;&gt;总结&lt;/h3&gt;
&lt;ol type=&quot;1&quot;&gt;
&lt;li&gt;最好的时空行为检测使用额外的person proposals和复杂的external memory
banks。作者提出了一个端到端的、纯transformer的模型，能够直接输入一个视频，输出tubelets(一系列的bboxes和action
classes at each frame). 这个灵活的模型能够用&lt;strong&gt;稀疏的bbox
supervision on individual frames or full tubelet
annotations&lt;/strong&gt;。在这两种情况下，预测连贯的tubelet作为输出。另外，这个模型不需要额外的对proposals的前处理或者NMS这样的后处理。&lt;/li&gt;
&lt;/ol&gt;</summary>
    
    
    
    <category term="Papers" scheme="https://young-eng.github.io/YoungBlogs/categories/Papers/"/>
    
    
    <category term="Action Detection" scheme="https://young-eng.github.io/YoungBlogs/tags/Action-Detection/"/>
    
  </entry>
  
</feed>
